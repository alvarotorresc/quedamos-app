<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep links for reset password and join group -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="quedamos.alvarotc.com" />
                <!-- Host anterior: enlaces de invitación ya compartidos siguen abriendo la app -->
                <data android:scheme="https" android:host="quedamos-app-mobile.vercel.app" />
            </intent-filter>

        </activity>

        <activity
            android:name="app.quedamos.widget.WidgetConfigActivity"
            android:exported="false"
            android:theme="@android:style/Theme.DeviceDefault.Light.Dialog">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!--
            Nuestro servicio de FCM. Extiende el del plugin de Capacitor, así que
            registrarlo no le quita nada: todo lo que no sea el push data-only de los
            widgets sigue llegándole por super.onMessageReceived, y onNewToken se hereda
            tal cual.

            FCM resuelve UN solo servicio para com.google.firebase.MESSAGING_EVENT, así
            que dejar los dos declarados sería una carrera con el orden de resolución del
            PackageManager. El tools:node="remove" de abajo borra el del plugin durante el
            merge del manifiesto (el del módulo app manda sobre el de la librería) y deja
            exactamente un candidato; el android:priority es el cinturón por si el nombre
            de esa clase cambiara algún día y el remove dejara de encontrarla.
        -->
        <service
            android:name="app.quedamos.push.QuedamosMessagingService"
            android:exported="false">
            <intent-filter android:priority="1">
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <service
            android:name="com.capacitorjs.plugins.pushnotifications.MessagingService"
            tools:node="remove" />

        <receiver
            android:name="app.quedamos.widget.SemanaWidgetProvider"
            android:exported="false"
            android:label="@string/widget_semana_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_semana_info" />
        </receiver>

        <receiver
            android:name="app.quedamos.widget.MejorDiaWidgetProvider"
            android:exported="false"
            android:label="@string/widget_mejor_dia_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_mejor_dia_info" />
        </receiver>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
