# Launch Gate — quedamos-app

Fecha: 2026-09-03 · Alcance: L+A+P · Commit: b03440e (`origin/main`, auditado desde la rama `docs/launch-gate`) · Producción: https://quedamos.alvarotc.com (web) y https://quedamos.api.alvarotc.com (API) · Staging: no hay

## Veredicto: NO LISTO

Quedan 63 bloqueantes pendientes de 110 (20 cumplen, 8 no aplican, 19 dependen de una persona). Lo más grave: credenciales sin rotar desde abril y un beta-bot con `service_role` y un código filtrado (SEC-10, AUTH-11), backup de la base de datos sin verificar (DAT-01), ninguna página legal ni política de privacidad (LEG-01…04), y un VPS sin límites de recursos vigilado cada 2-4 horas en vez de cada 5 minutos (INF-07, OBS-01).

## Contexto

- Stack: Node 22 · pnpm 9.15 · Turborepo · NestJS 10.4 + Prisma 6.1 sobre Postgres gestionado por Supabase · React 18 + react-router 5 + Ionic 8 + Capacitor 7 + Vite 6 + Tailwind 3 · Supabase Auth (email + contraseña; la API verifica el JWT ES256 contra el JWKS del proyecto) · Firebase Cloud Messaging · Sentry solo en la API · Umami self-hosted.
- Infraestructura: web en Vercel (landing y SPA en el mismo build; `apps/mobile/vercel.json` con CSP, HSTS y rewrite SPA) · API en Docker (`ghcr.io/alvarotorresc/quedamos-api`, solo tag `:latest`) tras Caddy en un VPS Hetzner (`vps-prod`) compartido con Plane, Umami y FreshRSS · `docker-compose.yml` y `Caddyfile` viven en el VPS sin versionar · GitHub Actions (`ci.yml`, `deploy.yml`, `release.yml`) + Dependabot · página de estado Upptime en https://status.alvarotc.com.
- Condiciones activas: cobra no · subidas de ficheros no · OAuth no · idiomas sí (es, en) · notificaciones sí (push FCM web y Android; los únicos emails son los de Supabase Auth).
- Accesos disponibles durante la auditoría: SSH sí (solo lectura) · navegador sí (Playwright, solo Chromium; sin sesión: nada de cuentas ni formularios enviados contra producción) · `gh` sí · Lighthouse sí (Chrome local) · paneles de terceros no (Supabase, Vercel, Cloudflare, Sentry, Railway, Hetzner → HUMAN).
- Método: 11 agentes en paralelo, uno por bloque de secciones, con lectura del código en el worktree, `curl`/`dig`/`openssl`/`whois` contra producción (solo lectura), SSH de solo lectura al VPS, `pnpm audit`, gitleaks, Lighthouse y Playwright. Evidencias en `launch-gate/` (capturas, `console.txt`, Lighthouse en `lh-*.json.gz`, logs de lint/typecheck/tests, `links.txt`, `contrast.js`, `loadtest-perf09.sh`).

## Resumen

| Sección               | B hechos/pend./total | R hechos/pend./total | NA     | HUMAN  |
| --------------------- | -------------------- | -------------------- | ------ | ------ |
| LEG Legal             | 0/6/7                | 0/2/6                | 2      | 3      |
| DNS Dominio y TLS     | 2/1/5                | 1/3/4                | 0      | 2      |
| PAG Páginas y enlaces | 0/7/8                | 1/3/5                | 1      | 1      |
| SEO                   | 1/2/3                | 0/2/3                | 0      | 1      |
| SEC Seguridad         | 8/4/17               | 1/1/3                | 4      | 2      |
| AUTH Autenticación    | 1/3/9                | 0/1/2                | 2      | 4      |
| ABU Abuso y límites   | 3/3/7                | 1/1/2                | 0      | 1      |
| DAT Datos y backups   | 1/4/6                | 1/2/4                | 1      | 1      |
| PERF Rendimiento      | 1/4/5                | 3/2/6                | 0      | 1      |
| UX Experiencia        | 1/6/11               | 1/4/6                | 2      | 3      |
| A11Y Accesibilidad    | 1/3/4                | 0/2/4                | 1      | 1      |
| MAIL Emails y push    | 0/1/3                | 0/2/3                | 1      | 2      |
| INF Infraestructura   | 1/6/9                | 0/4/5                | 0      | 3      |
| OBS Observabilidad    | 0/4/4                | 0/4/4                | 0      | 0      |
| QA Calidad            | 0/7/8                | 0/1/3                | 1      | 2      |
| OPS Operativa         | 0/2/4                | 0/6/7                | 0      | 3      |
| **Total**             | **20/63/110**        | **9/40/67**          | **15** | **30** |

Lectura de la tabla: 177 ítems; bloqueantes 20 hechos, 63 pendientes, 8 NA, 19 HUMAN; recomendados 9 hechos, 40 pendientes, 7 NA, 11 HUMAN.

## Acciones prioritarias

Los 63 bloqueantes PENDING, ordenados por riesgo y agrupados en cinco niveles. Cada línea nombra el ítem y el arreglo concreto; la evidencia está en el detalle por sección.

### Nivel 1 — expone datos o tumba el servicio (esta semana)

1. **SEC-10** Rotar la `service_role` y la contraseña de la base de datos en Supabase, la clave privada de Firebase, el JWT secret y `VPS_SSH_KEY` (pendiente desde abril, sin evidencia de rotación); rotar o retirar `BETA_INVITE_CODE` (está en el historial público desde 8703860); restringir la API key web de Firebase en Google Cloud y cerrar la alerta #1 de secret scanning; `chmod 600 /opt/services/.env.*` (hoy 664).
2. **AUTH-11** Parar el beta-bot de Railway: crea cuentas ya confirmadas con `service_role` para cualquier email que llegue con el código filtrado (`tools/beta-bot/src/index.ts:306-313`). Borrar `qa-test-*@quedamos.local` y `QA Prod Group` de producción.
3. **SEC-11** 2 críticas + 22 high en dependencias de runtime (`firebase-admin` → protobufjs/websocket-driver; `supabase-js` → ws; lodash, js-yaml, multer, sentry): mergear #85, `pnpm update -r`, overrides nuevos en `package.json`, activar Dependabot alerts y security updates, y `pnpm audit --prod --audit-level=high` en `ci.yml`.
4. **DAT-01 + DAT-03** Confirmar plan y backups en Supabase (Free = sin backups automáticos) y añadir un `pg_dump` cifrado semanal desde el VPS a un destino remoto (también `umami-db`), con dead-man's switch (healthchecks.io) que avise si no llega.
5. **INF-07** VPS compartido sin cuotas: `daemon.json` con rotación de logs (`max-size`/`max-file`), `mem_limit`/`cpus` en `quedamos-api` (y en Plane/Umami), swap de 2 GB, `docker image prune` semanal (44 GB de imágenes, disco al 63 %), alerta de disco > 80 %.
6. **OBS-01 + OBS-05** Upptime comprueba cada 2-4,5 h (GitHub retrasa el cron), no cada 5 min: monitor externo real (UptimeRobot o Better Stack) sobre la web y `/health` con alerta al Telegram existente; runbook con «qué hacer» por alerta, reglas de Sentry, monitor de certificado y alertas de gasto.
7. **PAG-12** `/health` no consulta la base de datos y nunca devuelve 503 (`apps/api/src/health.controller.ts:11-16`): con Supabase caído el deploy y el HEALTHCHECK dan verde. `SELECT 1` con timeout, 503 si falla, y quitar `firebaseInitialized` del cuerpo público (ajustar `deploy.yml:85-92`).
8. **MAIL-01 + DNS-06** Los emails de alta y reset salen por el SMTP por defecto de Supabase (unos 3 por hora, «solo desarrollo»): SMTP propio con remitente `no-reply@alvarotc.com`, SPF con el `include` del proveedor, DKIM, DMARC `p=quarantine` y Reply-To real. Mientras tanto, SPF `-all` y DMARC `p=reject` (hoy `~all` y `p=none`; nadie envía como `@alvarotc.com`).
9. **ABU-04 + ABU-02 + ABU-06** Sin límite de push por destinatario: un miembro puede crear 10 quedadas por minuto y disparar 10 push por minuto a todo el grupo → contador por destinatario/hora en `sendToUser` y `collapseKey`. Sin cuotas por usuario o grupo (grupos, miembros, quedadas, propuestas) ni throttler por `req.user.id`. Sin timeouts en Caddy (`timeouts`, `transport`), sin `pool_timeout`/`connect_timeout` en `DATABASE_URL`, sin `statement_timeout`, sin timeouts explícitos de Node.

### Nivel 2 — legal y derechos de los usuarios (antes de abrir a desconocidos o publicar en Google Play)

10. **LEG-02** Política de privacidad `/privacidad` a partir del inventario del detalle (datos, finalidades, base jurídica, destinatarios Supabase/Vercel/Hetzner/Google-FCM/Sentry/hCaptcha/OSM-Nominatim/Open-Meteo/Google Fonts, plazos, derechos, transferencias a EE. UU. bajo DPF, fecha). Google Play la exige para el APK.
11. **LEG-01** Aviso legal `/legal` (titular, NIF, domicilio, email de contacto) enlazado desde el pie de la landing (`Cierre.tsx`), Splash y Perfil.
12. **LEG-04** Términos `/terminos` + casilla no premarcada en Register + `terms_version`/`terms_accepted_at` en `users` (migración nueva).
13. **LEG-03** Inventario de cookies y almacenamiento `/cookies` (no hace falta banner) y autoalojar Bricolage Grotesque y Geist Mono (`@fontsource`) para dejar de enviar la IP de cada visitante a Google Fonts (cierra también SEC-14).
14. **AUTH-10** No existe borrado de cuenta ni exportación: `DELETE /auth/me` (`auth.admin.deleteUser` + `prisma.user.delete`, las relaciones ya tienen `onDelete: Cascade`) con confirmación en Perfil, y `GET /auth/me/export` o procedimiento manual documentado.
15. **LEG-06** Atribución obligatoria de Open-Meteo (CC BY 4.0) y OpenStreetMap/Nominatim (ODbL) en la UI y en la página legal; dejar de usar Nominatim como autocompletado con debounce de 300 ms (`components/LocationSearch.tsx:51-55`), que su política prohíbe y puede acabar en bloqueo por IP.
16. **LEG-05** «© 2026 Álvaro Torres · MIT» en el pie de la landing y en Splash; `"license": "MIT"` en `packages/shared/package.json`.
17. **DAT-07** Firebase Analytics en Android sin consentimiento (cookies `_ga` en el WebView, `lib/firebase.ts:50-75`): quitarlo y enrutar `logEvent` a Umami también en nativo, o Consent Mode con recogida desactivada por defecto.

### Nivel 3 — operar sin miedo (despliegue, rollback, observabilidad)

18. **INF-03** La imagen solo se publica como `:latest`: `docker/metadata-action` con tag `sha-${{ github.sha }}` y labels OCI en `deploy.yml`; `commit` en `/health`.
19. **INF-04 + OPS-01** No hay runbook ni forma de volver atrás: `RUNBOOK.md` con desplegar, rollback (`image@sha256:` anterior + `docker compose up -d quedamos-api` + `curl /health`), restaurar backup, rotar cada secreto, modo mantenimiento, dónde están los logs y contactos de cada proveedor.
20. **INF-02** Sin validación de entorno: `validationSchema` (Joi) en `ConfigModule.forRoot` y `apps/api/.env.example` con las 7 variables que faltan (`NODE_ENV`, `PORT`, `FRONTEND_URL`, `CORS_ORIGIN`, `DATABASE_CONNECTION_LIMIT`, `SENTRY_TRACES_SAMPLE_RATE`, `ENABLE_NOTIFICATIONS_DEBUG`).
21. **INF-01** Sin staging: decidir por escrito (mínimo viable: previews de Vercel + segundo proyecto Supabase gratuito + API en compose local); retirar del `env_file` del VPS `SUPABASE_JWT_SECRET` y `SUPABASE_SERVICE_KEY` (la API no las usa) y borrar el `.bak` tras la rotación.
22. **INF-08** Sin alerta de caducidad de certificado: monitor SSL a 14 días (UptimeRobot/Better Stack) o job semanal `openssl x509 -checkend 1209600` → Telegram.
23. **OBS-02** Sentry sin `release` ni source maps en la API y sin SDK en el frontend: `SENTRY_RELEASE` = SHA + `sentry-cli sourcemaps upload` en `deploy.yml`; `@sentry/react` + `@sentry/vite-plugin` + `ErrorBoundary` en `apps/mobile`.
24. **OBS-03** Logs en texto, nivel DEBUG en producción, sin request-id, y Caddy sin `log`: `nestjs-pino` (`X-Request-Id`, `level: info`, `redact`), `log` JSON en el vhost de la API, rotación por `daemon.json`.
25. **DAT-04** CI no aplica las migraciones (solo lee el SQL): job con `services: postgres:15` + `prisma migrate deploy` + `prisma migrate diff --exit-code`; `ROLLBACK.md` para la única migración con datos (`20260902175057_unique_open_poll_per_day`) y backup previo documentado.
26. **PAG-02** Con el contenedor caído Caddy responde 502 vacío: `handle_errors` con `Retry-After` en el Caddyfile; `X-Request-Id` desde Caddy y `requestId`/`error` en `all-exceptions.filter.ts`.
27. **QA-07** El smoke tras deploy solo mira `/health`: `scripts/smoke.sh` con home 200, `/health`, `/join/<código inexistente>` 404 y `/groups` sin token 401, llamado desde `deploy.yml` y tras el deploy de Vercel.
28. **OPS-08** El README anuncia «Versión 1.0.0» pero el enlace de descarga sirve el APK v0.3.3 de abril (pre-rediseño): publicar la release `v1.0.0` con APK firmado (keystore de release pendiente) o retirar el enlace hasta entonces.

### Nivel 4 — seguridad y pruebas de la aplicación

29. **SEC-08 + QA-02** El código filtra por membresía en todas las rutas con id, pero no hay test negativo de «no miembro» en events, availability, polls y proposals (8 casos con `groupsService.findById.mockRejectedValue(new NotFoundException())`) ni e2e en el que A pide el grupo de B y recibe 404.
30. **QA-01 + QA-03** Sin e2e reales ni base de datos en CI (`apps/api/test/` solo tiene `jest-e2e.json`): smoke e2e con supertest + Postgres de CI (registro → grupo → disponibilidad → quedada).
31. **AUTH-06** `jwt.verify` sin `issuer` ni `audience` (`apps/api/src/auth/auth.service.ts:52`): añadir `issuer: \`${supabaseUrl}/auth/v1\``y`audience: 'authenticated'` + test.
32. **SEC-01** Falta `Cross-Origin-Opener-Policy: same-origin` en el bloque `/(.*)` de `apps/mobile/vercel.json` (las otras cinco cabeceras están).
33. **QA-04** 85 ficheros sin formatear (los commits van con `--no-verify`): `prettier --write "apps/**/*.{ts,tsx,css}"` sin tocar los JSON de i18n, `prettier --check` en CI y `"strict": true` en `apps/api/tsconfig.json`.
34. **QA-08** Sin guion de QA manual: `docs/qa-manual.md` con nombres con acentos y emojis ZWJ (`emoji` tiene `@MaxLength(10)` y un emoji de familia lo supera), textos de 5 000 caracteres, 0 y 10 000 elementos, miembro expulsado con la app abierta, token caducado al enviar, dos pestañas votando lo mismo.

### Nivel 5 — experiencia, rendimiento y accesibilidad

35. **UX-03** Sin ErrorBoundary ni rama `isError`: con la API caída la app muestra «no tienes grupos» o una pantalla en blanco (`GroupDetailPage.tsx:284`). `react-error-boundary` en `main.tsx` y `EmptyState` de error con «Reintentar» en Calendar, Plans, Group y GroupDetail.
36. **PERF-01 + PERF-04** LCP de 4,9 s en móvil por un único bundle de 600 KB br (objetivo 200 KB): `React.lazy` por página, `manualChunks` (firebase, hcaptcha, framer-motion, emoji-mart, ionic) e `import()` diferido de `lib/firebase.ts`. Los assets con hash se sirven con `max-age=0` (añadir `immutable` un año en `vercel.json`), la API no manda `Cache-Control: no-store` ni comprime (`encode zstd gzip` en Caddy).
37. **PERF-08** `GET /groups/:id/availability`, `/events` y `/proposals` devuelven todo el histórico sin límite: rango `from`/`to` obligatorio (máx. 92 días), `from=hoy` + paginación en eventos, `take` en propuestas.
38. **PERF-07** Faltan índices `GroupCity(groupId)` (se consulta en cada petición de tiempo) y `Event(date, status)` (cron horario); log de consultas > 200 ms en `PrismaService`.
39. **PAG-01 + UX-15** Sin página 404 (una ruta desconocida deja la pantalla en blanco), `/tabs/*` sin sesión no guarda el destino, `document.title` fijo en todas las vistas: `NotFoundPage` como ruta comodín, `savePendingRedirect` en `ProtectedRoute`, `useDocumentTitle` por página.
40. **PAG-10** El único pie (`Cierre.tsx`) no tiene aviso legal, privacidad, cookies, términos, contacto, estado ni versión, y las páginas de auth no tienen pie: componente `Footer` en landing, auth y 404.
41. **PAG-05 + PAG-06** No existen `robots.txt`, `sitemap.xml`, `favicon.ico`, `apple-touch-icon` de 180 px ni `manifest.webmanifest` con iconos 192/512 maskable.
42. **PAG-07** El enlace de descarga del README apunta al APK v0.3.3 (ver OPS-08) y nadie enlaza `status.alvarotc.com`.
43. **SEO-01 + SEO-02** Sin `meta description`, títulos por ruta ni `lang` sincronizado con el idioma elegido; sin Open Graph/Twitter Card ni imagen 1200×630 (pegar el enlace en WhatsApp muestra solo la URL).
44. **A11Y-03** 46 inputs sin `id`/`htmlFor` (los lectores solo leen el placeholder) y 5 botones «ojo» de contraseña sin nombre y con `tabIndex={-1}`.
45. **A11Y-02** `--app-text-dark` da 3,23:1 en oscuro y 2,45:1 en claro (mínimo 4,5:1) en 31 etiquetas de formulario y 14 placeholders; Lighthouse accesibilidad 87 en `/login`.
46. **A11Y-07** 184 `text-[Npx]` en 33 ficheros (incluidos 9 px): tokens rem `text-2xs`/`text-3xs` en `tailwind.config.js`.
47. **UX-06** Inputs de auth y perfil sin `autocomplete`/`inputmode`, sin validación al salir del campo ni error junto al campo (solo banner superior).
48. **UX-07** Cerrar una propuesta y borrar disponibilidad se ejecutan a un clic sin confirmación: `IonAlert` que nombre lo que se pierde.
49. **UX-09** Tres faltas de ortografía y dos «¿» que faltan en `es.json`; `error.message` crudo de Supabase mostrado al usuario (`lib/auth-errors.ts:19`, `ResetPasswordPage.tsx:94`).
50. **UX-01** `NotificationsSettingsPage` muestra todos los toggles activados y `ProfilePage` «0 grupos» mientras cargan: usar `isLoading` y skeletons.
51. **QA-11** Warning de `apple-mobile-web-app-capable` deprecado en cada carga: añadir `<meta name="mobile-web-app-capable" content="yes">` en `index.html`.

Recomendados pendientes (40) con su arreglo: en el detalle por sección (DNS-05/07/08, PAG-03/08/09, SEO-03/04, SEC-14, AUTH-09, ABU-08, DAT-06/09, PERF-05/11, UX-04/05/11/16, A11Y-04/05, MAIL-05/06, INF-05/11/12/14, OBS-04/06/07/08, QA-05, OPS-03/04/05/06/07/09, LEG-09/13).

## Tareas para una persona

Los 30 ítems HUMAN, agrupados por dónde se resuelven. Cada uno con lo que hay que hacer y el resultado esperado.

### Panel de Supabase

- **AUTH-01** Authentication → Sign In / Providers → Email: longitud mínima ≥ 8 (el mensaje de error mapeado sugiere que hoy es 6) y «Leaked password protection» activado; después relajar `lib/password-utils.ts` a solo longitud.
- **AUTH-02** Authentication → Attack Protection: «Enable Captcha protection» con hCaptcha activado (si está apagado, el token que envía el frontend se ignora) y Rate Limits del token ≤ 30 por 5 min. Probar 10 logins fallidos solo con una cuenta de pruebas propia.
- **AUTH-04** «Email OTP expiration» ≤ 3600 s. Con cuenta de pruebas, usar el enlace de reset dos veces: la segunda debe fallar.
- **AUTH-05** Authentication → Sessions: caducidad del access token ≤ 3600 s.
- **AUTH-11 (parte humana)** Authentication → Users: borrar `qa-test-*@quedamos.local` y el grupo «QA Prod Group»; confirmar con `select email from auth.users where email like 'qa-test-%'` → 0 filas.
- **MAIL-03** Authentication → Email Templates («Confirm signup», «Reset password», «Change Email Address») y URL Configuration: sin `localhost` ni `vercel.app`, Site URL `https://quedamos.alvarotc.com`, logo por HTTPS, pie con nombre y contacto, tildes correctas, legible en móvil.
- **MAIL-02** Con una cuenta de pruebas nueva, disparar confirmación (desde `/register`), reset (desde `/forgot-password`) y cambio de email (desde Perfil) hacia Gmail, Outlook e iCloud, y los dos primeros a mail-tester.com: bandeja de entrada, remitente correcto, puntuación ≥ 9/10. Anotar en `docs/qa-manual.md`.
- **DAT-02** Restaurar el último backup en un `postgres:15-alpine` local, arrancar la API contra él, `curl /health` y `GET /groups` con un JWT válido; anotar fecha, RTO y RPO en el RUNBOOK (comandos en el detalle).

### Otros paneles: Cloudflare, Vercel, GitHub, Sentry, Railway, Hetzner

- **DNS-01 / INF-08 (dominio)** Cloudflare → Domain Registration → `alvarotc.com`: auto-renew activado, tarjeta válida, avisos de caducidad (caduca el 2027-04-25).
- **DNS-03** Vercel → proyecto → Settings → Domains → `quedamos.alvarotc.com` en «Valid Configuration». El certificado se emitió el 2026-09-02: recomprobar hacia el 2026-11-01 que `notBefore` ha cambiado.
- **SEC-13** Railway: confirmar que el servicio del beta-bot está parado o borrado. 2FA activo en Supabase, Vercel, GitHub y Hetzner.
- **INF-09** 2FA y email de recuperación válido en Cloudflare, Hetzner, Vercel, Supabase, GitHub, Google/Firebase, Sentry, hCaptcha, Tally, Telegram y Railway; contraseña fuerte y única en Umami; nada compartido por chat.
- **SEC-16** Permisos mínimos: la cuenta de servicio de Firebase solo con «Firebase Cloud Messaging API Admin», DSN de Sentry de solo ingesta, la API sin ninguna `service_role`.
- **ABU-05 / INF-13** Alertas de gasto al 50/80/100 % y límite duro donde exista: Supabase (Spend cap), Vercel (Spend Management), Firebase (sin Blaze), Sentry, Hetzner, Railway. Escribir el coste mensual estimado (8-40 €) en el RUNBOOK.
- **SEO-05** Verificar `alvarotc.com` en Search Console y Bing Webmaster, enviar `https://quedamos.alvarotc.com/sitemap.xml` cuando exista (PAG-05) y confirmar que `quedamos-app-mobile.vercel.app` no tiene URLs indexadas (hoy el sitio no está indexado en absoluto).
- **PAG-11 / OPS-02** Enviar un mensaje de prueba por el formulario Tally y confirmar que la notificación llega a la bandeja que se revisa a diario; escribir «Respondemos en 48 h» junto al enlace en `Cierre.tsx` y `ProfilePage.tsx`; decidir si se publica un email de contacto (el aviso legal lo exige).
- **SEC-19 / INF-06 / INF-04 (Vercel)** Confirmar el firewall de red en Hetzner Cloud; programar el reboot pendiente del VPS (170 días, parches de kernel sin aplicar) y aprovecharlo para verificar que todos los servicios vuelven solos; probar una vez «Instant Rollback» en Vercel.

### Pruebas que exigen cuenta de pruebas o dispositivo físico

- **SEC-05** Con cuenta de pruebas: `head -c 200000 /dev/zero | tr '\0' a | curl -s -o /dev/null -w "%{http_code}\n" -X POST $API/groups -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" --data-binary @-` → 413, nunca 500.
- **SEC-06** En un grupo de pruebas, crear una quedada con título `<img src=x onerror=alert(1)>` y comprobar que se ve como texto en la tarjeta, el detalle y la notificación push.
- **ABU-01** 40 `POST $API/groups/join` seguidos con cuenta de pruebas → 429 con `Retry-After` a partir de la sexta. Nunca contra Supabase Auth.
- **SEC-20** Autorizar un ZAP baseline contra producción (comandos en el detalle; el throttler de 100 req/min puede falsearlo) o levantar un entorno local (`docker build` + `supabase start`) y escanearlo; guardar el informe en `launch-gate/` y corregir lo medio y alto.
- **PERF-09** Ejecutar `launch-gate/loadtest-perf09.sh` contra la API local (o autorizar producción exceptuando `/health` del throttler): p95 < 500 ms y 0 respuestas no-2xx.
- **UX-12** En un Pixel y un iPhone físicos: splash, login, calendario (semana y mes), crear quedada y perfil, en vertical y horizontal, con el teclado abierto y con zoom al 200 %; nada cortado ni tapado por la tab bar.
- **UX-13** Firefox y Safari de escritorio (dos últimas versiones) con DevTools abierto en `/`, `/login`, `/register`, calendario y quedadas: cero errores y sin diferencias visuales (fuentes, aro, hojas inferiores).
- **UX-14** Una persona ajena recibe solo el enlace de invitación y debe crear cuenta, confirmar el email y marcar disponibilidad sin ayuda; anotar dónde se atasca.
- **QA-09** DevTools con «Slow 3G» y «CPU 4× slowdown», con cuenta de pruebas: calendario, quedadas, grupo, perfil y crear quedada; siempre skeleton o spinner, nunca blanco.
- **A11Y-06** TalkBack (Android) u Orca/NVDA: registro, unirse por código y marcar disponibilidad sin mirar la pantalla; anotar cada control sin nombre o inalcanzable (aviso: hoy los inputs se anuncian solo por el placeholder).

### Documentos y decisiones

- **LEG-07** Aceptar o descargar el DPA en Supabase, Vercel, Hetzner, Google/Firebase, Sentry, hCaptcha y Tally; anotar la región del proyecto Supabase; listarlos en `/privacidad`.
- **LEG-08** Confirmar que existe el Registro de Actividades de Tratamiento o redactarlo con los 8 tratamientos propuestos en LEG-08.
- **LEG-12** Buscar «Quedamos» en TMview, EUIPO eSearch (clases 9 y 42) y OEPM; apps homónimas en Google Play; decidir si se reservan `quedamos.app`/`quedamos.es` y handles.
- **INF-10** Documento de continuidad fuera del repo público (proveedores, dónde viven la clave SSH, el keystore, `/opt/services/.env.*` y los secrets de Actions) y alguien de confianza que sepa dónde está.
- **QA-06** Revisar el diff `v0.3.3..origin/main` (286 commits; las 15 últimas PR se mergearon sin review) con la OWASP Top 10 a mano y dejar nota fechada.
- **OPS-10 / OPS-11** El día del lanzamiento, marcar con hora la lista de día 1 (Upptime en verde, registro real con email, pregunta → push, backup presente, Sentry limpio, Tally vacía); crear los recordatorios del primer mes y vaciar las 12 PR de Dependabot.

## Detalle por sección

### LEG — Legal y cumplimiento

- **LEG-01** `PENDING` — No existe aviso legal. `grep -rniE "aviso|legal|privac|cookie|términos|terms|gdpr|rgpd" apps/mobile/src apps/mobile/public README.md` → 12 hits, todos falsos positivos («Avisos» = notificaciones en `i18n/locales/es.json:402`, «Illegal constructor» en `lib/push-notifications.ts:223`, tests); rutas en `App.tsx:161-166` solo `/`, `/login`, `/register`, `/forgot-password`, `/reset-password`, `/tabs`. `curl -s $PROD` = 1307 B con 0 hits de `NIF|CIF|aviso|legal`; `curl -sI $PROD/aviso-legal` devuelve 200 pero con el mismo etag `"6efd9364…"` y 1307 B que `/` y que `/nonexistent-xyz-123` (rewrite SPA en `apps/mobile/vercel.json:32`), es decir, no hay página.
  Arreglo: página `/legal` (titular Álvaro Torres, NIF, domicilio o apartado postal, email de contacto) con `Route` en `App.tsx`, enlazada desde el pie de la landing (`components/landing2/Cierre.tsx:45-53`), `SplashPage` y `ProfilePage`.
- **LEG-02** `PENDING` — No hay política de privacidad (mismos greps que LEG-01: 0 hits; `curl` idem). Inventario de datos (base del arreglo): email + contraseña (Supabase Auth; hash en Supabase), nombre, avatarEmoji y timeSlots en `user_metadata` (`stores/auth.ts:85-90,146,166`) y en `users` (`prisma/schema.prisma:17-23`, creado en `auth/auth.service.ts:73-82`); disponibilidad por fecha (`schema.prisma:79-88`); quedadas con título, descripción, lugar + lat/lon, URL de reunión (`schema.prisma:99-116`); asistencias, votos y respuestas al aro; pertenencia y rol; ciudades del grupo (`schema.prisma:206-211`); tokens push FCM + plataforma (`schema.prisma:143-152`); hash de token de widget + lastUsedAt; `NotificationLog` con título/cuerpo/JSON (contiene nombres; sin purga: solo `widget-token.service.ts:86` purga tokens); preferencias de avisos; IP/User-Agent en logs de Caddy, Vercel y auth de Supabase.
  Destinatarios que reciben datos: Supabase (DB+Auth+Realtime; región: confirmar en panel, el host va tras Cloudflare), Vercel (web; edge `cdg1` según `x-vercel-id`), Hetzner (API + Umami en 178.104.74.7), Google/Firebase FCM (push), Sentry (errores de la API, con scrub de JWT/email/cookies en `api/src/instrument.ts:23-40,46-76`), hCaptcha/Intuition Machines (IP + huella del navegador en Login/Register/Forgot, `RegisterPage.tsx:6,243`), OSM Foundation/Nominatim (texto buscado + IP del usuario desde el navegador, `services/weather.ts:69`), Open-Meteo (lat/lon desde el VPS, `weather/weather.service.ts:86`), Google Fonts (IP del usuario en cada carga, `index.html:15-17`), Umami (cookieless, `lib/firebase.ts:67`), Tally (formulario externo, `lib/constants.ts:8`), Cloudflare (DNS). No existe borrado de cuenta en la app (`grep deleteAccount|deleteUser` = 0) ni exportación → el derecho de supresión solo puede ejercerse por email.
  Arreglo: página `/privacidad` con tabla dato → finalidad → base jurídica → destinatario → plazo (definir retención de `NotificationLog` y de cuentas inactivas), derechos y email para ejercerlos, transferencias a EE. UU. (Vercel, Google, Sentry, hCaptcha) amparadas en DPF, fecha de versión; misma URL es obligatoria en Google Play (política + formulario Data Safety) para el APK `app.quedamos`.
- **LEG-03** `PENDING` — Sin cookies propias: `curl -sI $PROD | grep -i set-cookie` → nada; `document.cookie` 0 usos en `apps/mobile/src`. Almacenamiento: localStorage `theme` (`public/theme-init.js`), `quedamos-lang` (`i18n/index.ts:20-21`), sesión Supabase `sb-*-auth-token` (`createClient` por defecto, `lib/supabase.ts:6`), IndexedDB de Firebase Messaging (`public/firebase-messaging-sw.js`). Terceros en `index.html:14-17`: Umami `analytics.alvarotc.com/script.js` (2688 B, sin `data-cookies`, sin `document.cookie`, solo `localStorage`/`umami.disabled` → cookieless) y Google Fonts (`fonts.googleapis.com` responde sin set-cookie, pero recibe la IP del usuario en cada carga); hCaptcha solo en las páginas de auth (`RegisterPage.tsx:6,243`, `LoginPage.tsx`, `ForgotPasswordPage.tsx`): su CDN devuelve cookie `__cf_bm` (Domain=hcaptcha.com, HttpOnly, ~30 min) de seguridad/antiabuso, exenta de consentimiento. No hay banner y no hace falta; falta el inventario publicado.
  Arreglo: página `/cookies` con esa tabla (sin banner); self-host de las fuentes (`@fontsource-variable/bricolage-grotesque` + `@fontsource/geist-mono`) para eliminar la transferencia de IP a Google (criterio LG München 3 O 17493/20) y retirar `fonts.googleapis.com`/`fonts.gstatic.com` de la CSP de `vercel.json:17`.
- **LEG-04** `PENDING` — No hay términos ni aceptación. `grep -niE "terms|tos|checked|checkbox|acept|privac|legal" apps/mobile/src/pages/RegisterPage.tsx` → 0 hits; el formulario solo tiene nombre, email, contraseña, confirmación y hCaptcha invisible (`RegisterPage.tsx:133-243`); `grep -rniE "terms|tos_|accepted" apps/api/prisma` → 0; `signUp` envía solo `data: { name }` (`stores/auth.ts:85-90`).
  Arreglo: página `/terminos` (servicio, usos prohibidos, suspensión, limitación de responsabilidad, cambios, ley española y fuero); casilla no premarcada que bloquee el envío; `options.data.termsVersion`/`termsAcceptedAt` en `signUp` y columnas `terms_version`/`terms_accepted_at` en `users` (migración nueva) volcadas al crear el usuario en `auth.service.ts:78-82`.
- **LEG-05** `PENDING` — `LICENSE` existe: MIT, línea 3 «Copyright (c) 2026 Álvaro Torres»; `license: MIT` en `package.json` raíz, `apps/api`, `apps/mobile` (`packages/shared/package.json` sin campo). Pero la web no indica ©: `grep -rn "©" apps/mobile/src` → 0; `curl -s $PROD | grep -o "©[^<]*"` → nada; el pie de la landing (`Cierre.tsx:45-53`) solo lleva GitHub, feedback e idioma.
  Arreglo: «© 2026 Álvaro Torres · Código MIT» en `Cierre.tsx` y en `SplashPage` (móvil); añadir `"license": "MIT"` a `packages/shared/package.json`.
- **LEG-06** `PENDING` — Dependencias limpias: `pnpm -r licenses list --prod --json` → 462 paquetes: MIT 307, Apache-2.0 113, ISC 16, BSD-3-Clause 16, BlueOak-1.0.0 5, BSD-2-Clause 3, Python-2.0 1, 0BSD 1; filtro `GPL|LGPL|AGPL|SSPL|MPL|CDDL|Unknown` → 0 (`npx license-checker --production --summary` por app: solo «UNLICENSED» los propios `@quedamos/api` y `@quedamos/mobile`, privados). Fuentes Bricolage Grotesque y Geist Mono vía Google Fonts (`index.html:17`), ambas SIL OFL 1.1 (upstream `ateliertriay/bricolage/OFL.txt`, `vercel/geist-font/LICENSE.txt`), sin atribución exigida; iconos `react-icons/hi2` (Heroicons 2, MIT) e `ionicons` (MIT); imágenes propias (`public/logo.png`, `assets/logo.svg`).
  Falta la atribución obligatoria de datos: Open-Meteo (CC BY 4.0, `weather/weather.service.ts:86`) y Nominatim/OSM (ODbL; su Usage Policy exige «© OpenStreetMap contributors», `services/weather.ts:69`) → 0 hits de `openstreetmap|open-meteo|ODbL` en i18n/UI. Además Nominatim se consulta como autocompletado con debounce de 300 ms (`components/LocationSearch.tsx:51-55`, `hooks/useCitySearch.ts`), práctica que la Usage Policy prohíbe («no autocomplete») y que puede acabar en bloqueo por IP.
  Arreglo: línea de atribución en el badge de tiempo/ajustes de ciudades y en la página legal; buscar ciudad al pulsar Enter/botón o subir el debounce a ≥1 s (o pasar a Photon/geocoding en la API con cache).
- **LEG-07** `HUMAN` — Encargados deducidos del código e infra: Supabase (DB/Auth/Realtime), Vercel (web), Hetzner (VPS API + Umami), Google/Firebase (FCM), Sentry (errores API), hCaptcha/Intuition Machines (antiabuso), Tally (feedback), Cloudflare (DNS/registrador); fuera de la UE: Vercel, Google, Sentry, hCaptcha (EE. UU., los cuatro en el DPF; confirmarlo). El cruce con la política de privacidad no es posible porque no existe (LEG-02).
  Instrucción: en cada panel aceptar/descargar el DPA (Supabase → Legal → DPA; Vercel → Legal → DPA; Hetzner → «Auftragsverarbeitung» firmado; Google Cloud/Firebase → Data Processing Terms; Sentry → DPA y elegir región EU si se recrea el proyecto; hCaptcha → DPA en dashboard; Tally → DPA) y anotar la región del proyecto Supabase; listar todos en `/privacidad`.
- **LEG-08** `HUMAN` — No existe RAT en el repo (`docs/` solo tiene `pendiente-iconos.md` y `state-flows.md`). Instrucción: confirmar si Álvaro tiene el Registro de Actividades de Tratamiento; si no, redactarlo a partir del inventario de LEG-02 con estos tratamientos: cuentas de usuario, grupos y calendario, quedadas/propuestas/aro, notificaciones push, analítica web (Umami), errores (Sentry), antiabuso (hCaptcha), soporte/feedback (Tally). Plantilla: finalidad, base, categorías de datos e interesados, destinatarios, plazo, medidas.
- **LEG-09** `PENDING` — Ni declarada ni comprobada: `grep -rniE "birth|\bage\b|\bedad\b|minAge|14 años" apps/mobile/src apps/api/src` (sin tests) → 1 hit, un comentario de `widget/widget-summary.service.ts:16` («age of the group»); el registro no tiene fecha de nacimiento ni casilla (`RegisterPage.tsx:133-243`) y no hay términos que fijen edad.
  Arreglo: declarar «solo para mayores de 14 años» en `/terminos` y añadir en Register una casilla no premarcada «Tengo 14 años o más» que bloquee el envío, guardando `ageConfirmedAt` en `user_metadata` y en `users` (sin pedir fecha de nacimiento, por minimización).
- **LEG-10** `NA` — Condición no cumplida: no se cobra (brief «Cobros: NO»; `grep -rniE "invoice|factura|stripe|paypal|checkout|pricing|precio" apps/mobile/src apps/api/src` → 0 hits).
- **LEG-11** `NA` — No es comercio electrónico ni un servicio de los listados en la Directiva 2019/882 / Ley 11/2023 (app gratuita de coordinación), y el titular es una persona física sin empleados: exención de microempresa para servicios (art. 4.5 de la Directiva). Evidencia de que no existe declaración: `grep -rniE "declaraci.n de accesibilidad|accessibility statement" apps/mobile/src README.md docs` → 0. Recomendación voluntaria: un párrafo de accesibilidad en `/legal` cuando exista; los criterios WCAG se cubren en A11Y.
- **LEG-12** `HUMAN` — Nombre exacto: «Quedamos» / «¿Quedamos?» (`index.html:11`), palabra común en español (distintividad débil). Clases de Niza sugeridas: 9 (software/app móvil) y 42 (SaaS). Dominio: `alvarotc.com` (whois: registrador Cloudflare, creado 2022-04-25, expira 2027-04-25, clientTransferProhibited) con subdominio `quedamos.alvarotc.com`; no hay dominio propio `quedamos.*`. Handles conocidos: GitHub `alvarotorresc/quedamos-app` (`NavIsla.tsx:6`), Telegram `@QuedamosBetaBot`, paquete Android `app.quedamos` (`android/app/build.gradle:8`); ningún otro perfil social en código ni README.
  Instrucción: buscar «quedamos» en TMview y EUIPO eSearch (clases 9 y 42) y en el Localizador de marcas de la OEPM; buscar apps homónimas en Google Play; decidir si se reserva `quedamos.app`/`quedamos.es` y handles `@quedamosapp` antes de publicar en Play.
- **LEG-13** `PENDING` — `grep -rliE "brecha|breach|AEPD|incident" docs/ *.md .github/` → 0 ficheros; README sin runbook (brief). Arreglo: `docs/incidentes.md` de una página: detección (Sentry, Upptime, aviso de usuario) → responsable único (Álvaro) valora en <24 h → contención (rotar service_role/DB/Firebase/VPS_SSH_KEY, revocar tokens push y widget) → notificación a la AEPD en 72 h por sede electrónica (formulario de brechas) si hay riesgo → aviso a usuarios por email si el riesgo es alto → registro interno (fecha, alcance, datos, medidas). Incluir contactos: Supabase support, Hetzner, Vercel, AEPD.

### DNS — Dominio, DNS y TLS

- **DNS-01** `HUMAN` — `whois alvarotc.com`: `Registry Expiry Date: 2027-04-25` (233 días, > 11 meses), `Domain Status: clientTransferProhibited`, `Registrar: Cloudflare, Inc.` (NS `jen/yichun.ns.cloudflare.com`). Lo medible pasa; auto-renovación y tarjeta solo se ven en el panel.
  HUMAN: Cloudflare dashboard → Domain Registration → Manage `alvarotc.com` → confirmar **Auto-renew ON**, método de pago válido y email de avisos de caducidad activo.
- **DNS-02** `DONE` — Canónica `https://quedamos.alvarotc.com` → `200`. `http://quedamos.alvarotc.com/` → `308 https://quedamos.alvarotc.com/`; legacy `https://quedamos-app-mobile.vercel.app/` → `308 https://quedamos.alvarotc.com/` (regla `redirects` de `apps/mobile/vercel.json`); `http://quedamos.api.alvarotc.com/` → `308 https://quedamos.api.alvarotc.com/`.
  `www.quedamos.alvarotc.com` → `dig @1.1.1.1` = `NXDOMAIN` (no existe variante www: aceptable, nunca se publicó). 308 en vez de 301: permanente y equivalente. Apex `alvarotc.com` (otra web del dueño, Vercel): http→308 https→308 `https://www.alvarotc.com/` → 200; no auditado a fondo.
- **DNS-03** `HUMAN` — Web: `openssl x509`: `notAfter=Dec 1 2026` (88 días), `issuer=Let's Encrypt CN=YR1`, cadena YR1→ISRG Root YR→ISRG Root X1, `Verify return code: 0 (ok)`, TLSv1.3. API: `notAfter=Oct 13 2026` (39 días), `issuer=Let's Encrypt CN=YE1`, cadena YE1→ISRG Root YE→X2→X1, `Verify return code: 0 (ok)`. TLS 1.0/1.1 rechazados por el servidor en ambos (`openssl -tls1_1 -cipher DEFAULT:@SECLEVEL=0` → `alert protocol version`; `curl --tlsv1.1 --tls-max 1.1` exit 35).
  Renovación Caddy COMPROBADA: `docker logs caddy` → `2026-05-25`/`2026-07-24 renewing certificate analytics`, `2026-07-14 plane`, **`2026-07-15 renewing certificate quedamos.api.alvarotc.com`**; crt.sh: `03-17→06-15 E8`, `05-16→08-14 E7`, `07-15→10-13 YE1` (2 renovaciones automáticas); volumen `services_caddy_data:/data` con `/data/caddy/certificates/acme-v02.api.letsencrypt.org-directory/{analytics,news,plane,quedamos.api}`; ARI: próxima renovación planificada `2026-09-12..14`.
  HUMAN (Vercel): cert de `quedamos.alvarotc.com` emitido **ayer** (`notBefore=Sep 2 13:32 2026`), sin historial en crt.sh aún → no hay evidencia de renovación. Vercel → proyecto `quedamos-app-mobile` → Settings → Domains → `quedamos.alvarotc.com` = "Valid Configuration" y cert gestionado por Vercel; recomprobar hacia `2026-11-01` que `notBefore` ha cambiado.
- **DNS-04** `DONE` — `curl -sI https://quedamos.alvarotc.com/` → `strict-transport-security: max-age=31536000; includeSubDomains` (1 año ≥ 15552000; `vercel.json`); `https://quedamos.api.alvarotc.com/health` → idéntico (helmet por defecto). `includeSubDomains` inocuo: `quedamos.alvarotc.com` no tiene subdominios (`www.quedamos` NXDOMAIN, sin wildcard) y `quedamos.api.alvarotc.com` tampoco (ojo: su padre es `api.alvarotc.com`, que no existe; no es hijo de `quedamos.`).
  Apex `alvarotc.com`: `strict-transport-security: max-age=63072000` **sin** `includeSubDomains` → no cascada a los subdominios, y es correcto así: `status.alvarotc.com` sirve `http://` con `200` sin redirigir (GitHub Pages `https_enforced=false` vía `gh api repos/alvarotorresc/upptime/pages`) y `analytics.alvarotc.com` no manda HSTS (Caddy no lo añade). No añadir `includeSubDomains` en el apex hasta activar "Enforce HTTPS" en Pages del repo upptime (mejora, no bloquea).
- **DNS-05** `PENDING` — `dig +short CAA alvarotc.com` → vacío; `dig +short CAA quedamos.api.alvarotc.com` → vacío. `quedamos.alvarotc.com` solo hereda el CAA del destino CNAME de Vercel (`ae7adc524f877edd.vercel-dns-017.com`: `0 issue "letsencrypt.org"|"globalsign.com"|"sectigo.com"|"pki.goog"`); la API y el resto de la zona admiten cualquier CA.
  Arreglo: Cloudflare DNS → añadir en `alvarotc.com` registros CAA `0 issue "letsencrypt.org"`, `0 issue "sectigo.com"` (fallback ZeroSSL de Caddy), `0 issue "pki.goog"` y `0 issue "globalsign.com"` (lista de Vercel), más `0 iodef "mailto:<buzón del dueño>"`. Registros DNS-only (sin proxy naranja) → no hacen falta las CAs de Cloudflare Universal SSL; si algún día se activa el proxy, añadir `digicert.com` y `ssl.com`.
- **DNS-06** `PENDING` — SPF: `"v=spf1 include:_spf.mx.cloudflare.net ~all"` (softfail, no `-all`). DMARC: `"v=DMARC1; p=none; rua=mailto:<gmail del dueño>"` (solo monitorización, no `reject`/`quarantine`). MX: `route1/2/3.mx.cloudflare.net` (Cloudflare Email Routing, solo recepción). Subdominio `quedamos.` sin SPF/DMARC propio (cubierto por el apex, hoy `p=none`).
  Remitente del producto: Supabase Auth; en el repo no hay SMTP ni librería de correo (`apps/api/package.json` sin mail deps; el front llama `supabase.auth.resetPasswordForEmail`/`signUp`; README solo dice "Solo hace falta un email") → remitente por defecto `noreply@mail.app.supabase.io`, DKIM del dominio propio no aplica. Confirmar en Supabase → Authentication → SMTP Settings que "Enable Custom SMTP" está OFF (si estuviera ON con dominio propio: DKIM del proveedor + `include` en SPF).
  Arreglo: Cloudflare DNS → SPF `v=spf1 include:_spf.mx.cloudflare.net -all` y `_dmarc` → `v=DMARC1; p=reject; sp=reject; adkim=s; aspf=s; rua=mailto:<mismo buzón>` (nadie envía como `@alvarotc.com`); si se responde desde Gmail con alias `@alvarotc.com`, añadir antes `include:_spf.google.com` + DKIM de Google y pasar por `p=quarantine` una semana.
- **DNS-07** `PENDING` — `dig @1.1.1.1` sobre 20 nombres + 5 hallados en CT (`crt.sh %.alvarotc.com`). Resuelven y responden bajo control: apex/`www`/`test`/`bito` → Vercel (200, webs propias), `quedamos` → Vercel (200), `status` → `alvarotorresc.github.io` (200 "Alvaro TC Status"), `analytics`/`quedamos.api`/`plane`/`news` → `178.104.74.7` (Caddyfile: umami, quedamos-api, plane, freshrss; 200/200/200/302), `aux`/`devtools`/`pokeutils` → `*.netlify.app` (200). NXDOMAIN (sin registro, sin riesgo): api, app, staging, mail, docs, freshrss, beta, dev, umami, vps, admin, www.quedamos; sin wildcard.
  Huérfano: **`uuid.alvarotc.com` CNAME `cname.vercel-dns.com`** → `curl` exit 60 `certificate has expired (10)` (cert LE R12 caducado que Vercel ya no renueva: dominio sin proyecto). Takeover mitigado porque el apex está en la misma cuenta Vercel (exige TXT `_vercel`), pero es un registro muerto. `status` CNAME a github.io sin TXT `_github-pages-challenge-alvarotorresc` (Pages "verified domain" no configurado): riesgo solo si se borrase el sitio upptime.
  Arreglo: Cloudflare DNS → borrar el CNAME `uuid` (o volver a asignar el dominio a su proyecto Vercel); opcional: GitHub → Settings → Pages → Verified domains → `alvarotc.com`; exportar la zona (Cloudflare → DNS → Records → Export) para confirmar que no hay más nombres que los sondeados.
- **DNS-08** `PENDING` — `curl -s https://quedamos.alvarotc.com/.well-known/security.txt` → `200 text/html` con el `index.html` de la SPA (rewrite `/(.*)` → `/index.html`): no existe el fichero. API → `404 {"message":"Cannot GET /.well-known/security.txt"}`. Apex → 308 a `www.alvarotc.com` → 404.
  Arreglo: crear `apps/mobile/public/.well-known/security.txt` (`Contact: mailto:<alias, p. ej. security@alvarotc.com vía Cloudflare Email Routing>`, `Expires: 2027-09-03T00:00:00.000Z`, `Preferred-Languages: es, en`, `Canonical: https://quedamos.alvarotc.com/.well-known/security.txt`); el mecanismo ya funciona: `/.well-known/assetlinks.json` se sirve `200 application/json` gracias al rewrite `/.well-known/(.*)` de `vercel.json`. API: en el Caddyfile del VPS `handle /.well-known/security.txt { redir https://quedamos.alvarotc.com/.well-known/security.txt 302 }` (Caddyfile no versionado en el repo).
- **DNS-09** `DONE` — Staging: no existe (`$STAGING` = NO HAY). Previews de Vercel (`gh api .../deployments` → 5 URLs `quedamos-app-mobile-*-alvarotorrescs-projects.vercel.app`, alias `git-main` y alias de proyecto): todas `302 https://vercel.com/sso-api?...` con cookie `_vercel_sso_nonce` (Deployment Protection / Vercel Authentication activa) y `x-robots-tag: noindex`; `/robots.txt` del preview también tras el SSO. El alias legacy de producción `quedamos-app-mobile.vercel.app` → `308` a la canónica (no expone contenido).

### PAG — Páginas, rutas y enlaces

- **PAG-01** `PENDING` — `curl -sI $PROD/esta-ruta-no-existe-4242 | head -1` → `HTTP/2 200`; `curl -s $PROD/esta-ruta-no-existe | grep -ciE "inicio|home|volver"` → `0` (devuelve el `index.html` de 1307 B por el rewrite `/(.*)`→`/index.html` de `apps/mobile/vercel.json:36`). En el router no hay catch-all ni página propia: `apps/mobile/src/App.tsx:161-167` y `:52-60` solo tienen rutas `exact` con path, ningún `<Route>` sin path/`NotFound` (`ls src/pages` no tiene NotFoundPage; grep `notfound|404` → 0). Una ruta desconocida renderiza un `IonRouterOutlet` vacío (pantalla en blanco; logueado: tab bar sin contenido). Arreglo: `NotFoundPage.tsx` (copia + enlace a `/`) montada como último `<Route>` sin path en ambos outlets; opcional `404.html` estático y rewrite acotado a `/(login|register|forgot-password|reset-password|tabs|join)(.*)` para que el servidor devuelva 404 real.
- **PAG-02** `PENDING` — API: cuerpo homogéneo estilo Nest (`{"message":"Cannot GET /ruta-inexistente","error":"Not Found","statusCode":404}`, 401 `Missing authorization header`), 500 → `{statusCode:500,message:'Internal server error'}` sin stack (stack solo a logger+Sentry, `apps/api/src/common/filters/all-exceptions.filter.ts:39-49`); falta `requestId` y `code` en todos, y `error` en el 500. Proxy: `ssh vps-prod cat /opt/services/caddy/Caddyfile` → bloque `quedamos.api.alvarotc.com { reverse_proxy quedamos-api:3001 }` sin `handle_errors` (con el contenedor caído Caddy responde 502 con cuerpo vacío). Web: estática en Vercel, sirve sus propias páginas de error (no hay `500.html` propio). Arreglo: en Caddy `header_up X-Request-Id {http.request.uuid}` + `handle_errors { header Retry-After 60; respond "{\"error\":\"unavailable\",\"code\":\"UPSTREAM_DOWN\"}" {err.status_code} }`; en el filtro incluir `requestId` (leer `x-request-id`) y `error` en el 500.
- **PAG-03** `PENDING` — `grep -rniE "mantenimiento|maintenance|retry-after|503" README.md .github/workflows/*.yml docs/*.md apps/mobile/vercel.json Dockerfile` → 0; en el VPS `grep -rniE "maint|handle_errors|503|retry-after" /opt/services/caddy/ docker-compose.yml` → 0. No hay runbook. Arreglo (API): en el Caddyfile `@maint file {root:/opt/services/caddy} maintenance.on` → `handle @maint { header Retry-After 600; respond "{\"error\":\"maintenance\"}" 503 }` y documentar `touch/rm maintenance.on` en README; web (Vercel) no admite bandera sin deploy: documentar el rewrite temporal a una `maintenance.html`.
- **PAG-04** `DONE` — `curl -s -w "\n%{http_code}" $API/auth/me` → `{"message":"Missing authorization header","error":"Unauthorized","statusCode":401}` 401; con `Bearer invalid` → `{"message":"Invalid token",...}` 401. 403 desde servicios con mensaje humano (`apps/api/src/events/events.service.ts:193` "Only the creator can edit this event"; proposals.service.ts:91-201). 429: `ThrottlerGuard` global (`app.module.ts:28-52`, 100/60 s por IP) + `@Throttle` por ruta (18 usos); throttler 6.5.0 devuelve `{"statusCode":429,"message":"ThrottlerException: Too Many Requests"}` con cabecera `Retry-After` (dist grep). No se probó el 429 en producción (carga masiva prohibida). Nit: quitar el prefijo `ThrottlerException:` con `errorMessage` en `ThrottlerModule.forRoot`.
- **PAG-05** `PENDING` — `curl -s -o /dev/null -w "%{http_code} %{content_type}" $PROD/robots.txt` → `200 text/html` (es el index.html: soft-404); igual para `/sitemap.xml`; `ls apps/mobile/public` → favicon.svg, firebase-messaging-sw.js, icon-256.png, icon-512.png, logo.png, theme-init.js, .well-known/ (ni robots ni sitemap). Arreglo: `apps/mobile/public/robots.txt` con `User-agent: *`, `Allow: /`, `Disallow: /tabs/`, `Sitemap: https://quedamos.alvarotc.com/sitemap.xml` y `public/sitemap.xml` con `/`, `/login`, `/register` (URLs https, sin barra final, sin host legacy).
- **PAG-06** `PENDING` — `/favicon.svg` 200 image/svg+xml OK; `theme-color` `#14120E` OK; pero `/favicon.ico`, `/apple-touch-icon.png`, `/manifest.json`, `/manifest.webmanifest`, `/site.webmanifest` → `200 text/html 1307 B` (soft-404, no existen); `curl -s $PROD | grep -oE '<link[^>]*(icon|manifest)[^>]*>'` → solo `rel="icon" favicon.svg` y `rel="apple-touch-icon" href="/icon-256.png"` (256×256 según `identify`, no 180; sin `rel="manifest"`; sin iconos 192/512 `maskable`). Iconos con la marca actual (comprobado visualmente `launch-gate/prod-icon-256.png` vs `favicon-svg-preview.png`, commits f8c8b25/adc9bef 2026-09-01/02). Arreglo: generar `favicon.ico` (16/32/48) y `apple-touch-icon.png` 180×180 desde `favicon.svg`, añadir `public/manifest.webmanifest` (`icons` 192 y 512 con `purpose: "any maskable"`, `theme_color`, `background_color`) y los tres `<link>` en `apps/mobile/index.html`.
- **PAG-07** `PENDING` — Inventario en `launch-gate/links.txt` (48 líneas): 12 rutas internas, 4 externos en `src` (GitHub, Tally, Umami, Google Fonts), 12 del README; todos `curl -L` → 200, host legacy 308→$PROD. `npx linkinator $PROD --recurse --concurrency 2` → 8 URLs, 0 fallos (`launch-gate/linkinator.csv`; solo ve el shell de la SPA). 0 enlaces 4xx/5xx, pero el enlace de descarga Android (`README.md:14` y `:24` «descarga el APK de la última release» → `releases/latest`) resuelve a `releases/tag/v0.3.3` (2026-04-09, `quedamos-v0.3.3.apk`, marca antigua) mientras `README.md:19` anuncia «Versión 1.0.0»; `gh release list` → última v0.3.3, no existe tag v1.0.0. Arreglo: crear tag/release `v1.0.0` (release.yml construye el APK) o apuntar el enlace a un tag fijo; enlazar `status.alvarotc.com` (hoy no aparece en web ni README).
- **PAG-08** `PENDING` — `grep -rn 'target="_blank"' apps/mobile/src | grep -v noopener` lista 7 líneas, pero es JSX multilínea: los 7 `<a>` llevan `rel` (5 `noopener noreferrer`: EventCard.tsx:309, EventDetailModal.tsx:101, ProposalCard.tsx:131, ProfilePage.tsx:406; 2 `noreferrer` solo, que implica noopener: landing2/NavIsla.tsx:68, Cierre.tsx:47/50). Falta: `apps/mobile/src/lib/maps-utils.ts:9` `window.open(getGoogleMapsUrl(...), '_blank')` sin `'noopener,noreferrer'`. Arreglo: tercer argumento `'noopener,noreferrer'` (una línea). Pestaña nueva solo en enlaces externos: correcto.
- **PAG-09** `PENDING` — `curl -sI $PROD/login/` → `HTTP/2 200` y `curl -sI $PROD/login` → `HTTP/2 200` (ninguna redirige: dos URLs para la misma página); `/Login` → 200 (el rewrite de Vercel no distingue mayúsculas y react-router sí → pantalla en blanco). Sin `canonical` (SEO-03). Enlaces internos consistentes: minúsculas y sin barra final (`grep -rnoE 'to="[^"]*[A-Z/]"'` → solo `to="/"`). Redirección de migración OK: host legacy `quedamos-app-mobile.vercel.app/tabs/calendar` → 308 a `$PROD/tabs/calendar` (`vercel.json:26-32`). Arreglo: `"trailingSlash": false` en `apps/mobile/vercel.json` (Vercel emite 308 `/login/`→`/login`) + canonical por ruta.
- **PAG-10** `PENDING` — Único pie: `apps/mobile/src/components/landing2/Cierre.tsx:45-70` → «Código abierto», «Ver el código en GitHub», «Reportar un error» (Tally) y selector Español/English. Sin aviso legal, privacidad, cookies, términos, contacto, estado ni versión: `grep -niE "aviso legal|privacidad|cookies|t[ée]rminos|contacto|versi[óo]n" apps/mobile/src/i18n/locales/es.json` → 0; `grep -nE "footer|legal|privac|version" src/pages/{SplashPage,LoginPage,RegisterPage}.tsx` → 0 (las páginas de auth no tienen pie); no existe 404. `for p in / /404-x /login; do curl -s $PROD$p | grep -ciE "aviso legal|privacidad|cookies|términos|contacto"; done` → 0 0 0. Arreglo: componente `Footer` (legal, privacidad, cookies, términos, contacto, `status.alvarotc.com`, versión inyectada con `define: { __APP_VERSION__ }` en vite.config) montado en LandingPage, Splash/Login/Register/Forgot y NotFound; las páginas legales las cubre LEG.
- **PAG-11** `HUMAN` — Agente: `grep -rnoE "mailto:[^\"']+" apps/mobile/src README.md apps/mobile/index.html` → 0 (no hay ninguna dirección de correo publicada); el único canal es el formulario Tally `https://tally.so/r/ODMzOa` (`src/lib/constants.ts:8`, enlazado en `landing2/Cierre.tsx:50` «Reportar un error», `pages/ProfilePage.tsx:404` y `README.md:157`; `curl -L` → 200). Instrucción: Álvaro envía un mensaje de prueba por el Tally, confirma que la notificación llega a la bandeja que revisa (ajustes de Tally → Notifications) y decide si publica además un email de soporte/contacto (necesario para LEG).
- **PAG-12** `PENDING` — `curl -s -w "\n%{http_code}" $API/health` → `{"status":"ok","firebaseInitialized":true}` 200. `apps/api/src/health.controller.ts:11-16`: no consulta la base de datos (solo `notificationsService.isFirebaseInitialized()`), nunca devuelve 503 (con Supabase caído seguiría 200 y el HEALTHCHECK del `Dockerfile:54` y `deploy.yml:82-92` darían verde); expone `firebaseInitialized` (detalle interno, público). Arreglo: inyectar `PrismaService` y hacer `SELECT 1` con timeout (~2 s), responder `503 {"status":"error"}` si falla; cuerpo público `{"status":"ok"}` y mover `firebaseInitialized` a log/Sentry o a un check autenticado (ajustar `deploy.yml:85-92`, que hoy exige ese campo).
- **PAG-13** `NA` — Sin terceros consumidores: la única integración externa al frontend es el widget Android propio (`apps/api/src/widget`, mismo repo). Swagger solo fuera de producción (`apps/api/src/main.ts:46-55`, `NODE_ENV !== 'production'`); `curl -sI $API/api/docs | head -1` → `HTTP/2 404`, `/api/docs-json` → 404. Sin `/v1/` ni `enableVersioning` (grep → 0). Nit: `setVersion('0.2.3')` en main.ts:50 desactualizado. Motivo NA: condición «si van a consumirla terceros» no cumplida.

### SEO — Buscadores y compartir

- **SEO-01** `PENDING` — `curl -s $PROD | grep -oE '<title>[^<]*|<meta name="description"[^>]*|<html[^>]*lang="[^"]*"'` → `<html lang="es">` y `<title>¿Quedamos?` (10 caracteres, `apps/mobile/index.html:11`); **no hay `meta description`** en ninguna página. Título único y estático para todas las rutas: `grep -rnE "document\.title|Helmet|useTitle" apps/mobile/src` → 0. `lang` fijo: al cambiar a inglés (`Cierre.tsx:61` `i18n.changeLanguage`) nadie actualiza `documentElement.lang` (grep → 0; `i18n/index.ts` sin listener). Arreglo: `<meta name="description">` (≤155) en index.html; hook `useSeo(title, description)` por página pública (Landing, Login, Register, Forgot, 404) con título ≤60 («¿Quedamos? — Quedar con la cuadrilla sin calendario»); `i18n.on('languageChanged', l => document.documentElement.lang = l)` en `i18n/index.ts`.
- **SEO-02** `PENDING` — `curl -s $PROD | grep -oE '<meta (property|name)="(og:|twitter:)[^>]*>'` → vacío (0 etiquetas OG/Twitter; tampoco en `index.html` ni en `src`). No existe imagen 1200×630: `identify apps/mobile/public/*.png` → icon-256 256×256, icon-512 512×512, logo 256×256. Pegar el enlace en WhatsApp/Slack muestra solo la URL. Arreglo: `public/og.png` 1200×630 (aro + «¿Quedamos?» sobre `#14120E`), y en index.html `og:type=website`, `og:site_name`, `og:title`, `og:description`, `og:url=https://quedamos.alvarotc.com/`, `og:image=https://quedamos.alvarotc.com/og.png` (+ `og:image:width/height`, `og:locale=es_ES`, `og:locale:alternate=en_US`), `twitter:card=summary_large_image`, `twitter:image`. Parte humana tras desplegar: previsualizar en WhatsApp y Slack.
- **SEO-03** `PENDING` — `curl -s $PROD/ | grep -oE '<link rel="canonical"[^>]*>|hreflang'` → vacío (igual en `/login` y `/register`: misma respuesta index.html); grep en `index.html`/`src` → 0. Dos idiomas pero servidos en la misma URL (elección cliente: `i18n/index.ts` `localStorage`/`navigator`), así que `hreflang` por URL no aplica salvo que se expongan `?lang=` o `/en`. Arreglo: canonical estático `https://quedamos.alvarotc.com/` en index.html y dinámico por ruta desde el hook `useSeo` (siempre https, sin barra final, nunca el host `vercel.app`); si se mantiene el idioma en cliente, anotar hreflang como NA y usar `og:locale`.
- **SEO-04** `PENDING` — `curl -s $PROD | grep -c 'ld+json'` → 0; `grep -rn "ld+json" apps/mobile --include='*.html' --include='*.tsx'` → 0. Hay landing (alcance L) así que aplica. Arreglo: `<script type="application/ld+json">` estático en `index.html` con `SoftwareApplication` (`name` «¿Quedamos?», `applicationCategory: "LifestyleApplication"`, `operatingSystem: "Android, Web"`, `offers: {price: 0, priceCurrency: "EUR"}`, `url`, `image`, `inLanguage: ["es","en"]`) y `WebSite`; la CSP `script-src 'self'` no afecta (JSON-LD no se ejecuta). Validar en Rich Results Test (HUMAN, recomendación).
- **SEO-05** `HUMAN` — Agente: la URL del sitemap sería `https://quedamos.alvarotc.com/sitemap.xml`, que hoy devuelve el index.html (200 text/html, no es un sitemap: PAG-05). Búsqueda web `site:quedamos-app-mobile.vercel.app` → sin resultados (nada del host legacy indexado); `site:quedamos.alvarotc.com` → sin resultados (el sitio no está indexado en absoluto). Instrucción: tras PAG-05/SEO-01, verificar la propiedad de dominio `alvarotc.com` en Search Console (registro TXT) y en Bing Webmaster (importar desde GSC), enviar el sitemap, confirmar 0 errores de cobertura y que `quedamos-app-mobile.vercel.app` no tiene URLs indexadas.
- **SEO-06** `DONE` — `grep -rniE "lorem|ipsum|todo:|fixme|texto de prueba|asdf" apps/mobile/src apps/mobile/public --include='*.{html,tsx,ts,json}'` (excluyendo tests) → solo un comentario de código `src/lib/push-notifications.ts:167` («TODO: use LocalNotifications plugin», no se publica); «placeholder» aparece solo en `placeholder=` de inputs, claves i18n `*Placeholder` (es.json:6-553, legítimas) y un comentario doc en `src/lib/tarjeta.ts:184` (la tarjeta pinta la URL real o deja el hueco vacío). `ls apps/mobile/public .github/readme | grep -iE "stock|sample|placeholder|test"` → 0. Capturas README (`.github/readme/*.png`, 6) commiteadas 2026-09-02 (73ce896), un día antes de los últimos cambios de UI (a1fc5be selector de idioma, 1b2a8c7 alerts; no afectan a lo capturado); iconos con la marca actual (visual). Juicio humano: revisar que las capturas muestren datos verosímiles y la pantalla actual tras el próximo cambio visual.

### SEC — Seguridad de la aplicación

- **SEC-01** `PENDING` — `curl -sI $PROD` devuelve 5/6: `x-content-type-options: nosniff`, `x-frame-options: DENY`, `referrer-policy: strict-origin-when-cross-origin`, `permissions-policy: camera=(), microphone=(), geolocation=()`, HSTS 1 año, y CSP = `default-src 'self'; script-src 'self' https://hcaptcha.com https://*.hcaptcha.com https://www.gstatic.com https://analytics.alvarotc.com; connect-src 'self' https://*.supabase.co wss://*.supabase.co https://quedamos.api.alvarotc.com https://nominatim.openstreetmap.org https://api.open-meteo.com https://hcaptcha.com https://*.hcaptcha.com https://firebase.googleapis.com https://firebaseinstallations.googleapis.com https://fcmregistrations.googleapis.com https://fcm.googleapis.com https://analytics.alvarotc.com; frame-src 'self' https://hcaptcha.com https://*.hcaptcha.com; img-src 'self' data: blob:; style-src 'self' 'unsafe-inline' https://hcaptcha.com https://*.hcaptcha.com https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'` (`apps/mobile/vercel.json:10-17`). `script-src` sin `unsafe-inline`/`unsafe-eval`/`*`: el tema inicial va en fichero externo `/theme-init.js` (`index.html:13`, comentario en `public/theme-init.js:2-4`) y Umami por `src`; `unsafe-inline` solo en `style-src` (Ionic/hCaptcha). Playwright en `$PROD/`: 0 errores de consola, 0 violaciones CSP (solo un aviso de `apple-mobile-web-app-capable` deprecado). **Falta `Cross-Origin-Opener-Policy` en la web** (la API sí la tiene por helmet: `cross-origin-opener-policy: same-origin`, más CORP, `frame-ancestors 'self'`, `script-src 'self'`; `main.ts:20-30`).
  Arreglo: añadir `{ "key": "Cross-Origin-Opener-Policy", "value": "same-origin" }` al bloque `/(.*)` de `vercel.json` (hCaptcha va en iframe, no en popup: no lo rompe); opcional `Cross-Origin-Resource-Policy: same-origin`.
- **SEC-02** `DONE` — `OPTIONS $API/groups` con `Origin: https://evil.example` → 204 sin `access-control-allow-origin`; con `Origin: https://quedamos.alvarotc.com` → `access-control-allow-origin: https://quedamos.alvarotc.com`, `access-control-allow-methods: GET,POST,PUT,PATCH,DELETE,OPTIONS`, `vary: Origin`; con `Origin: http://localhost` → sin ACAO (los orígenes de desarrollo solo entran si `NODE_ENV !== 'production'`, `common/frontend-url.ts:17-19`; en el VPS `NODE_ENV=production`, `docker-compose.yml:24`). Lista explícita en `corsOrigins()` (`frontend-url.ts:11-24`): `$PROD`, `LEGACY_FRONTEND_URL` (vercel.app, sigue aceptado: `ACAO: https://quedamos-app-mobile.vercel.app`; ese host solo hace 308 al dominio, riesgo bajo, pendiente de retirar en backlog), `https://localhost` (origen del WebView de Capacitor Android, necesario; sin cookies un `https://localhost` ajeno no obtiene el bearer del usuario) y `CORS_ORIGIN` opcional. Ningún `*` en `apps/api/src` (`grep -rn "\*" main.ts` solo en comentarios). Nota: `access-control-allow-credentials: true` se emite siempre (comportamiento del paquete `cors`), inerte sin ACAO y sin cookies; `allow-headers` refleja lo pedido (`authorization,content-type`).
- **SEC-03** `NA` — No hay cookie de sesión: `apps/mobile/src/lib/supabase.ts:6` `createClient(url, anonKey)` con opciones por defecto de supabase-js ^2.47 (`persistSession: true`, storage = `localStorage`, `flowType: 'implicit'`); la API recibe `Authorization: Bearer` (`apps/mobile/src/lib/api.ts:27`) y no emite `Set-Cookie` (`curl -sI $API/health`: ninguno). Riesgo residual: un XSS leería el token de `localStorage` (mitigado por CSP sin `unsafe-inline` y React); el paso a PKCE (`flowType: 'pkce'`) sigue pendiente en backlog (ver AUTH).
- **SEC-04** `NA` — Sin cookie de sesión el navegador nunca adjunta credenciales solas: cada petición mutante exige que JS del mismo origen lea el token de `localStorage` y ponga `Authorization` (`lib/api.ts:27`), así que CSRF no aplica. `credentials: true` en `enableCors` (`main.ts:35`) no habilita nada por cookie (la API no crea ninguna). Si algún día se introduce sesión por cookie, hay que revisar este ítem y SEC-03 a la vez.
- **SEC-05** `DONE` — 20 `@Body()` (auth 1, groups 4, notifications 4, polls 2, proposals 4, availability 2, events 3) y todos tipados con DTO; 21/21 `*.dto.ts` importan `class-validator`; `ValidationPipe({ whitelist, transform, forbidNonWhitelisted })` global (`main.ts:37-43`); `json({ limit: '100kb' })` (`main.ts:18`; Caddy solo `reverse_proxy`, sin `request_body`, basta con Express); profundidad JSON sin límite explícito pero acotada por los 100 kB (`JSON.parse` de V8 lanza RangeError → 400 de body-parser). Longitudes: nombre 100/emoji 10 (`create-group.dto.ts:6,12`, `update-profile.dto.ts:7,13`), título 200/descripción 1000/ubicación 200/meetingUrl 500 con `IsUrl` http(s) (`create-event.dto.ts:21-76`, `create-proposal.dto.ts:16-50`), ciudad 200 + lat/lon acotados (`add-city.dto.ts:6-16`), slots `ArrayMaxSize(10)` + `MaxLength(50, {each})` (`create-availability.dto.ts:26-28`), tokens 500, fechas `Matches(YYYY-MM-DD)`+`IsISO8601`, horas `HH:MM`, enums `IsIn`. Params: todos los ids con `ParseUUIDPipe`, `:date` con `ParseCalendarDatePipe` (`availability.controller.ts:50,60`), `:code` con regex en `invite.controller.ts:25-27`; `@Query()` con DTO (weather, widget). Menor: `attendeeIds` sin `@ArrayMaxSize` (`create-event.dto.ts:78-82`; lo acota el body y la comprobación de miembros en `events.service.ts:82-91`). Prueba 413 en vivo no ejecutada (es un POST): ver HUMAN.
- **SEC-06** `DONE` — `grep -rnE "innerHTML|dangerouslySetInnerHTML|insertAdjacentHTML|document\.write" apps/mobile/src apps/mobile/public` → 0. Texto de usuario va por JSX escapado: `EventCard.tsx:246` `{event.title}`, `:334` `{event.description}`, `EventDetailModal.tsx:66,114,118`; los `href` con datos de usuario pasan por `sanitizeUrl` (solo `http:`/`https:`, `lib/url-utils.ts:6-18`) en `EventCard.tsx:307`, `EventDetailModal.tsx:99`, `ProposalCard.tsx:129`; Maps con `encodeURIComponent` (`lib/maps-utils.ts:5`). Push: título/cuerpo viajan como cadenas planas a FCM (web con payload `data`-only, `notifications.service.ts:405-419`); el SW usa `showNotification(title, { body })` sin tocar el DOM y valida UUIDs antes de componer URLs (`firebase-messaging-sw.js:26,48,64-92`). Prueba con `<img src=x onerror=alert(1)>` en producción no ejecutada (crea registros): ver HUMAN.
- **SEC-07** `DONE` — `grep -rnE "\$queryRaw|\$executeRaw|queryRawUnsafe|executeRawUnsafe" apps/api/src` → 0; todo acceso por Prisma Client (sin `contains`/buscadores de texto libre). Listados acotados por grupo y membresía (`findMany` con `groupId` tras `findById`), `take: 50` en polls (`polls.service.ts:145`) y `take: 20` en logs de notificaciones (`:227`); availability/events sin paginación pero limitados por tamaño de grupo (ver ABU si se quiere `take`/rango de fechas).
- **SEC-08** `PENDING` — Código correcto en todas las rutas con id: `groupsService.findById(groupId, userId)` = `findFirst({ where: { id, members: { some: { userId } } } })` → 404 para no miembros (`groups.service.ts:102-117`) y se llama en `events.service.ts:28,43,69`, `availability.service.ts:16,26,62,98,149,195`, `polls.service.ts:76,140,154,212,229`, `proposals.service.ts:30,67,80,136,186,284`, `weather.controller.ts:30,63`, `widget-summary.service.ts:46`; propiedad: solo el creador edita/borra/cancela/confirma evento (`events.service.ts:192,280,306,336`), propuesta (`proposals.service.ts:90,200`) y cierra pregunta (`polls.service.ts:218`); `isAdmin` (`groups.service.ts:386-394`) para rol/expulsar/ciudades/regenerar código (`:402,451,552,585,608`), borrar solo creador (`:526`), creador no abandona (`:205`), `leave` no miembro → 404 (`:196`); `/notifications/*` y `/auth/me` acotados por `user.id`; `/widget/summary` con `WidgetTokenGuard` + `findById`. Tests negativos existentes (unitarios con Prisma mockeado): `groups.service.spec.ts:276,613,769,794,864,925,1005,1033`; `events.service.spec.ts:788,1049,1103,1172`; `proposals.service.spec.ts:406,488,536`; `polls.service.spec.ts:278`; `widget-summary.service.spec.ts:49`; `weather.controller.spec.ts:150`. RLS: 15 sentencias en 4 migraciones (`20260318120000_enable_rls_all_tables`, `20260829182010`, `20260901093000`, `20260902064300`) y `rls-migration.spec.ts:16-38` exige `ENABLE ROW LEVEL SECURITY` en las 15 tablas, sin políticas → deny-by-default para anon/authenticated vía PostgREST/Realtime; la API conecta como propietario/`postgres` (`DATABASE_URL` en `.env.example`), que salta RLS, así que RLS protege solo el acceso directo con anon key, no la API.
  Falta: (1) ningún test de «no miembro» (findById rechazando) en events, availability, polls y proposals (solo comprueban que `findById` se llama); (2) sin e2e HTTP: nadie prueba que el usuario A recibe 404 pidiendo el grupo de B. Endpoints sin test negativo de autorización: `GET /groups/:id`, `GET /groups/:id/members`, `GET /groups/:id/invite`, `DELETE /groups/:id/cities/:cityId`, `GET|POST /groups/:groupId/events`, `GET /groups/:groupId/events/:eventId`, `POST .../events/:eventId/respond`, `GET|GET me|POST|PUT :date|DELETE :date /groups/:groupId/availability`, `GET|POST /groups/:groupId/polls`, `POST .../polls/:pollId/respond`, `GET|POST /groups/:groupId/proposals`, `POST .../proposals/:proposalId/vote`, `GET /groups/:groupId/weather/forecast`. Arreglo: en cada spec de esos módulos un `it('rejects a non-member')` con `groupsService.findById.mockRejectedValue(new NotFoundException())` sobre findAll/create; y un e2e supertest (`apps/api/test/`) con dos JWT de prueba (A pide `/groups/:idDeB` → 404).
- **SEC-09** `NA` — No hay subidas de ficheros: `grep -rnE "multer|FileInterceptor|multipart|@UploadedFile|busboy|formidable" apps/api/src apps/mobile/src` → 0 hits (exit 1). Nota: `multer` llega transitivo por `@nestjs/platform-express` (sin ruta que lo use) y `pnpm audit` lo marca high (<2.2.0; el override actual solo fuerza >=2.1.1) → ver SEC-11.
- **SEC-10** `PENDING` — `.env` ignorado OK (`git check-ignore` → `.gitignore:13:.env` para apps/api, apps/mobile y raíz; `tools/beta-bot/.gitignore:2`); en todo el historial solo se añadieron `*.env.example` (0 `.pem/.key/.jks/google-services.json`). GitHub: `secret_scanning: enabled`, `push_protection: enabled`, pero `dependabot_security_updates: disabled` y **alerta de secret scanning #1 ABIERTA** (Google API Key, desde 2026-02-12, validity unknown) en `apps/mobile/public/firebase-messaging-sw.js:6`, commit 0fadcbd, aún en HEAD. gitleaks (mirror, 488 commits, `--all`): 8 hallazgos = 1 `gcp-api-key` (ese mismo; clave web de Firebase, pública por diseño pero sin restringir/resolver), 4 falsos positivos (`generic-api-key` hCaptcha _sitekey_ en Login/Register/ForgotPassword a2fa071 — pública; `generic-api-key` en 8703860 `tools/beta-bot/.env.example:3` y `README.md:68` = placeholder `SUPABASE_SERVICE_KEY=eyJ...` truncado de 39/60 chars) y 2 `private-key` (72ee96f/0b3f29c `notifications.service.spec.ts:68`, PEM mock sin bloque base64 ≥100 chars).
  Filtrado real: **BETA_INVITE_CODE** en claro en 8703860 (`tools/beta-bot/README.md:69,178` + `.env.example`) y 4e4abea; borrado en 7f18044 pero sigue en el historial público; el bot lo lee de `process.env.BETA_INVITE_CODE` (`src/index.ts:20`), así que basta cambiarlo en Railway (o apagar el bot tras v1.0.0). Aparece también en el `CLAUDE.md` local del checkout principal (no trackeado). Rotación pendiente sin evidencia de haberse hecho: secrets de Actions con `updated_at` 2026-03-12/17 (`VPS_SSH_KEY` 2026-03-17); `/opt/services/.env.quedamos` (claves DATABASE_URL, DIRECT_URL, SUPABASE_SERVICE_KEY, SUPABASE_JWT_SECRET, FIREBASE_PRIVATE_KEY…) mtime 2026-09-02, modo **664** (debería ser 600).
  Arreglo: (1) rotar BETA_INVITE_CODE o retirar el bot; (2) ejecutar la rotación del backlog (service_role + password DB en Supabase, clave privada Firebase, JWT secret, `VPS_SSH_KEY`) y actualizar `.env.quedamos` + secrets de Actions; (3) restringir la API key web de Firebase en Google Cloud (referrers `quedamos.alvarotc.com` + paquete Android/SHA-256, solo APIs FCM/Installations) y resolver la alerta #1; (4) `chmod 600 /opt/services/.env.*`; (5) opcional: `.gitleaks.toml` con allowlist para los 4 FP y `gitleaks` en CI.
- **SEC-11** `PENDING` — Lockfiles: `pnpm-lock.yaml` + `tools/beta-bot/package-lock.json` (fuera del workspace y de CI). `.github/dependabot.yml` OK (actions/npm/docker semanal, grupos minor+patch, majors ignorados; actions fijadas a SHA40). Pero: **12 PRs de Dependabot abiertas** (#85 grupo de 36 minor/patch; majors #83/#77/#74 Nest 12, #71 react-router 7, #72 vitest 4, #80, #78, #69, #66, #63, #61), Dependabot _alerts/security updates_ **desactivados** (`vulnerability-alerts` → 404), y CI sin paso de audit. `pnpm audit --audit-level=high`: **133 vulns (11 low / 54 mod / 64 high / 4 critical)**; `--prod`: 49 (1 / 24 / **22 high / 2 critical**).
  Críticas en runtime: `protobufjs` <7.5.5 y `websocket-driver` <0.7.5 (ambas vía `firebase-admin@14.3.0` en la API). High en runtime: `ws` <8.21.0 (`@supabase/supabase-js@2.100.1`, bundle web), `@grpc/grpc-js`, `form-data`, `fast-xml-builder` (firebase-admin), `lodash` 4.17.23 (`@nestjs/config`), `js-yaml` (`@nestjs/swagger`), `multer` (`platform-express`), `brace-expansion` (`@sentry/nestjs`), `deepmerge-ts/defu/effect` (`prisma`). Dev: `vitest@2.1.9` (critical), `tar` vía `@capacitor/cli` (critical), `vite` 6.4.1, `nanoid`, `postcss`. Ya existen 25 `pnpm.overrides` en `package.json`, pero desfasados. Abandono: `react-router-dom@5.3.4` (última publicación del 5.x **2022-10-02**, ~4 años; lo impone `@ionic/react-router` 8) — riesgo aceptado pero anotar; `react-scripts` no se usa; `@ionic/react`, `@nestjs/common`, `@capacitor/core` publicados en 2026-08/09.
  Arreglo: activar Dependabot alerts + security updates (Settings → Code security); mergear #85 y `pnpm update -r`; añadir overrides `protobufjs>=7.6.1`, `websocket-driver>=0.7.5`, `ws>=8.21.0`, `tar>=7.5.21`, `lodash>=4.18.0`, `js-yaml>=4.3.1`, `multer>=2.2.0`, `brace-expansion>=5.0.9`; subir `firebase-admin`, `vitest` ≥3.2.6, `vite` ≥6.4.3; añadir `pnpm audit --prod --audit-level=high` a `ci.yml`; meter `tools/beta-bot` en CI o retirarlo.
- **SEC-12** `DONE` — Sondas en `$PROD` (`.env .git/HEAD .git/config backup.sql dump.sql database.sqlite .DS_Store phpinfo.php server.js.map`) devuelven 200 pero siempre el `index.html` de la SPA (`content-type: text/html`, 1307 B, `<!doctype html>`; rewrite `/(.*)` → `/index.html` en `vercel.json:32`), ningún fichero real; `$PROD/assets/` sin «index of»; en `$API` todas 404. Source maps: el bundle `assets/index-Bf_fI5DM.js` tiene 0 `sourceMappingURL`, `$PROD/<js>.map` devuelve el HTML de la SPA (no existe), `vite.config.ts` sin `sourcemap` (default off). Swagger: `$API/api/docs` → 404 (solo si `NODE_ENV !== 'production'`, `main.ts:46`); `$API/notifications/debug` → 401 sin auth y además 404 en producción salvo `ENABLE_NOTIFICATIONS_DEBUG=true` (`notifications.controller.ts:59-68`); `docker-compose.yml:24` `NODE_ENV=production`. Errores: 500 genérico `Internal server error` con stack solo en log y Sentry (`all-exceptions.filter.ts:38-48`). Nota para PAG/SEO (no seguridad): la web responde 200 a cualquier ruta (soft-404).
- **SEC-13** `HUMAN` — No hay panel admin: `$PROD/admin` → 200 pero es la SPA (no existe ruta `admin` en `App.tsx`), `$API/admin` → 404, no hay `sitemap.xml` (devuelve la SPA). Los «admin» son roles de grupo: `PATCH :id/members/:userId/role` y `DELETE :id/members/:userId` (`groups.controller.ts:93-110`) exigen `isAdmin` (`groups.service.ts:386-394,402-404,451-453`), no se degrada al creador ni se queda el grupo sin admin (`:415-425`), no se expulsa a un admin ni al creador (`:468-474`); `DELETE /groups/:id` solo el creador (`:526`). Beta-bot: no corre en el VPS (`docker ps` → quedamos-api, umami, umami-db, caddy, freshrss, plane-\*; ningún bot), README lo despliega en Railway con `SUPABASE_SERVICE_KEY` = service_role (`tools/beta-bot/README.md:75,101-130`, `src/index.ts:19,34`). Sin 2FA en código (no aplica: no hay admin propio).
  HUMAN: (1) confirmar en Railway que el servicio del beta-bot está parado/borrado y rotar la service_role (backlog); (2) confirmar 2FA activo en Supabase Dashboard, Vercel, GitHub y Hetzner, y contraseñas únicas en gestor.
- **SEC-14** `PENDING` — `curl -s $PROD | grep -oE '<(script|link)[^>]*(src|href)="https?://[^"]*"[^>]*>'` → 4 etiquetas: Umami `https://analytics.alvarotc.com/script.js` (subdominio de `$DOMAIN`, self-hosted: cuenta como propio; SRI rompería en cada actualización de Umami), 2 `preconnect` (no cargan recurso) y la hoja `https://fonts.googleapis.com/css2?family=Bricolage+Grotesque...&family=Geist+Mono...` sin `integrity` (imposible: Google sirve CSS distinto por User-Agent). Además el SW hace `importScripts` de `www.gstatic.com/firebasejs/12.9.0/*` (versión fijada; `importScripts` no soporta SRI) y hCaptcha se inyecta dinámicamente.
  Arreglo (R): autoalojar las dos fuentes (`@fontsource-variable/bricolage-grotesque`, `@fontsource/geist-mono`, import en `main.tsx`) y quitar `fonts.googleapis.com`/`fonts.gstatic.com` de la CSP; de paso desaparece la transferencia de IPs a Google (ver LEG). Umami/Firebase/hCaptcha: sin SRI por diseño del proveedor.
- **SEC-15** `NA` — No se cobra: `grep -rniE "stripe|paddle|payment|checkout" apps/api/src apps/mobile/src packages` → solo `stripes` (CSS) en `landing2/Cuadrilla.test.tsx`; sin dependencias de pago en ningún `package.json`; `grep -rniE "card_number|cardNumber|cvc|cvv"` → 0.
- **SEC-16** `DONE` — Único cliente HTTP propio: Open-Meteo, `fetch(url, { signal: AbortSignal.timeout(10_000) })` (`weather.service.ts:26,95`), sin reintentos (0 ≤ 3), caché 30 min con tope de tamaño (`:24,120-125`), cuerpo no válido → 503 `weather_unavailable` (`parseDaily`), y en el controlador cada ciudad va en `Promise.allSettled`: la que falla se omite con `warn` y el endpoint responde con el resto (`weather.controller.ts:40-52`). JWKS: `jwks-rsa` con `cache: true, cacheMaxAge: 600000` (`auth.service.ts:27-31`); fallo → 401 `Invalid token` (`:59-61`), nunca 500 (sin `timeout` explícito: usa el default de 30 s de la librería). FCM: `sendEachForMulticast` dentro de try/catch que devuelve `failed` sin propagar (`notifications.service.ts:423,449-455`), sin timeout explícito (default del SDK), pero los envíos son fire-and-forget con `.catch` desde los servicios (`groups.service.ts:445`), así que ninguna respuesta HTTP espera a FCM. Nominatim solo lo llama el frontend (CSP `connect-src`), no la API.
  Recomendado: `timeout: 5000` en `JwksClient`. Permisos mínimos de claves → HUMAN.
- **SEC-17** `DONE` — Sin middleware de log de peticiones (`grep -rn "morgan|pino|NestMiddleware|req\.body|req\.headers" apps/api/src` → 0): ningún body ni cabecera se escribe; 46 llamadas a `logger.*`, ninguna con password/token/authorization: `auth.service.ts:60` loguea solo el mensaje de `jsonwebtoken` (no el token), errores FCM con 8 caracteres del token (`notifications.service.ts:428-430`), UUIDs de usuario en `auth.service.ts:105,112`. Sentry `beforeSend` (`instrument.ts:23-77`) redacta JWT (`eyJ…`), `qw_…`, `bearer …`, `token|password|code|…=` en query, emails, cabeceras `authorization/cookie/x-api-key` y borra `cookies`; sin `sendDefaultPii`. Logs reales: `docker logs quedamos-api --since 24h` → 104 líneas, `credential_hits=0`, `email_hits=0`. Frontend: `console.txt|debug` → 0; solo `console.warn` (`usePushNotifications.ts:73`) y `console.error` (`push-notifications.ts:82`) sin secretos. Nota: el `Logger` de Nest no tiene lista de redacción; depende de la disciplina (razonable para 46 llamadas).
- **SEC-18** `DONE` — Campos sensibles del modelo: `widget_tokens.token_hash` (solo SHA-256 del `qw_` aleatorio de 24 bytes, `widget-token.service.ts:27-39`, TTL 90 días y máximo 5 por usuario `:19-20`); `push_tokens.token` FCM en claro (necesario para enviar; sin la cuenta de servicio de Firebase no sirve para nada, y esa clave vive en `FIREBASE_PRIVATE_KEY` de entorno, no en la DB: `notifications.service.ts:26,43-44`); ningún otro secreto de terceros en `schema.prisma`; cifrado de disco a cargo de Supabase. Android: el token del widget está en `SharedPreferences` `MODE_PRIVATE` sin cifrar (`WidgetPrefs.kt:16-17,22-28`), pero `allowBackup="false"`, `fullBackupContent="false"` (`AndroidManifest.xml:5-6`) y `data_extraction_rules.xml` excluye `sharedpref` de copia en nube y transferencia entre dispositivos: no sale del sandbox. Recomendación no bloqueante: `EncryptedSharedPreferences` para `KEY_TOKEN` (defensa extra en dispositivos rooteados).
- **SEC-19** `DONE` — Desde fuera `nc -zv -w 3 178.104.74.7 …`: abiertos solo 22/80/443, TIMEOUT en 3000/3001/5432/6379/8080/8000/3306. En el VPS (`ss -tlnp` no loopback): solo 0.0.0.0/[::] :22 :80 :443; `docker ps`: solo `caddy` publica 80/443(+udp), `umami-db` `PortBindings={}` y `quedamos-api` sin puertos (Caddy → red `proxy`); compose: único `ports:` es caddy. `Dockerfile:48 USER node`; `docker inspect quedamos-api` → `User=node Privileged=false`. `sudo -n sshd -T`: `passwordauthentication no`, `permitrootlogin no`, `kbdinteractiveauthentication no`, `pubkeyauthentication yes`. `fail2ban` active/enabled (jail sshd: 3 baneados ahora, 38 en total); `unattended-upgrades` active/enabled (`APT::Periodic::Unattended-Upgrade "1"`, última ejecución 2026-09-03 09:33; 0 security pendientes); `ufw` active: default deny incoming, ALLOW 22/80/443 v4+v6; Ubuntu 24.04.4 LTS; sin socket docker montado; sin daemon TCP; Caddy admin :2019 no expuesto.
  Mejoras no bloqueantes: **reboot pendiente** (`/var/run/reboot-required`; corre kernel 6.8.0-90 con 6.8.0-138 instalado, uptime 170 días → parches de kernel/libc6 sin aplicar en memoria); `.env.quedamos`/`.env.umami` modo 664 → `chmod 600`; `quedamos-api` sin límites (`Memory=0 NanoCpus=0`); `umami-db` comparte la red `proxy` con caddy, freshrss, plane-proxy y quedamos-api (aislarla en una red solo umami↔umami-db). HUMAN opcional: confirmar en el panel de Hetzner Cloud si hay Firewall a nivel de red (no verificable por SSH).
- **SEC-20** `HUMAN` — No hay staging y no hay ningún informe previo (`git ls-files | grep -iE "zap|nuclei|pentest|qa-report"` vacío; `launch-gate/` solo tiene linkinator/favicons; los informes locales sin trackear de abril/agosto no ejecutaron escáner). No ejecutado contra producción sin permiso. Instrucción: autorizar y correr `docker run --rm -t -v "$PWD/launch-gate:/zap/wrk" ghcr.io/zaproxy/zaproxy:stable zap-baseline.py -t https://quedamos.alvarotc.com -I -r zap-web.html` y `… -t https://quedamos.api.alvarotc.com -I -r zap-api.html` (ojo al throttler 100 req/60 s por IP: `-z "-config spider.maxDuration=2"`), o levantar un entorno local (`docker build -t quedamos-api .` + `supabase start`) y escanearlo; guardar los informes en `launch-gate/` y corregir todo lo medio/alto.

### AUTH — Autenticación y cuentas

- **AUTH-01** `HUMAN` — Hashing: NA (Supabase Auth, bcrypt). Frontend `apps/mobile/src/lib/password-utils.ts:11-14`: mínimo 8 ✓, sin máximo ✓, pero reglas arbitrarias (mayúscula + dígito + símbolo obligatorios) y 0 hits de HIBP/pwned en `apps/mobile/src`; `lib/auth-errors.ts:7` mapea "Password should be at least 6 characters" → el mínimo del servidor parece ser 6 (el bot de beta crea cuentas saltándose el frontend). `/auth/v1/settings` no expone la política.
  HUMAN: panel Supabase → Authentication → Sign In / Providers → Email: "Minimum password length" ≥ 8 y "Leaked password protection" (HIBP, requiere plan Pro) activado; después relajar `password-utils.ts` a solo longitud (≥ 8, opcional zxcvbn).
- **AUTH-02** `HUMAN` — Login en Supabase: "Invalid login credentials" para ambos casos (`lib/auth-errors.ts:4`); hCaptcha invisible en Login (`pages/LoginPage.tsx:47,128`), Registro (`RegisterPage.tsx:67,243`) y Reset (`ForgotPasswordPage.tsx:28,102`), token enviado en `stores/auth.ts:79,90,135`. Sin bloqueo progresivo (Supabase no lo ofrece). `curl $API/auth/me` sin token → 401.
  HUMAN: panel → Authentication → Attack Protection: "Enable Captcha protection" = hCaptcha ON (si está OFF el token se ignora y `POST /auth/v1/token` es atacable sin captcha); Authentication → Rate Limits: token ≤ 30/5 min por IP. Prueba de 10 logins fallidos solo con cuenta de pruebas propia, nunca con cuentas reales.
- **AUTH-03** `DONE` — `curl -H "apikey: <anon>" <SUPABASE_URL>/auth/v1/settings` → `"mailer_autoconfirm": false`, `"disable_signup": false`: sin confirmar el email Supabase no emite JWT ("Email not confirmed", `auth-errors.ts:5`), y la API solo acepta JWT ES256 del proyecto (`auth.service.ts:49-52`), así que ninguna ruta sensible es alcanzable sin verificar. Registro con email existente: con confirmación activa Supabase devuelve usuario ofuscado sin error y `RegisterPage.tsx:75-76` muestra la misma pantalla "email enviado" → sin enumeración.
  Caveat (ver AUTH-11): `tools/beta-bot/src/index.ts:306-313` crea cuentas con `email_confirm: true` vía service_role para cualquier email que teclee quien tenga el código de beta (filtrado).
- **AUTH-04** `HUMAN` — Reset gestionado por Supabase (token de un solo uso; caducidad = "Email OTP expiration", 3600 s por defecto). Frontend: `stores/auth.ts:131-137` `resetPasswordForEmail` con captcha y `redirectTo /reset-password`; `ResetPasswordPage.tsx:43-57` espera `PASSWORD_RECOVERY`/sesión (8 s → pantalla "caducado"), `:69` cierra la sesión a medias antes de reenviar; `ForgotPasswordPage` muestra "Email enviado" exista o no → sin enumeración. Cambio de contraseña: `updateUser({password})` (`auth.ts:141`) sin `signOut({scope:'others'})` (0 hits de `scope:`); GoTrue revoca los refresh tokens de las otras sesiones al cambiarla, pero los access tokens ya emitidos siguen válidos hasta `exp` (la API no consulta revocación).
  HUMAN: panel → Authentication → Sign In / Providers → Email → "Email OTP expiration" ≤ 3600 s; con cuenta de pruebas propia: usar el enlace de reset dos veces → la segunda debe fallar.
- **AUTH-05** `HUMAN` — `lib/supabase.ts:6` `createClient` sin opciones → defaults de auth-js 2.100.1 (`GoTrueClient.js:20-24`): `persistSession: true`, `autoRefreshToken: true`, `flowType: 'implicit'` (PKCE pendiente, BACKLOG). `signOut()` sin argumentos = `scope: 'global'` (`GoTrueClient.js:3141`) → revoca todos los refresh tokens del usuario en servidor; el store además desregistra push y revoca el token de widget (`stores/auth.ts:96-104`, `lib/widget-bridge.ts:48-51`). No hay botón "cerrar todas las sesiones" en Profile (0 hits), pero el logout ya es global. JWT stateless: un access token copiado sirve hasta `exp` aunque se cierre sesión.
  HUMAN: panel → Authentication → Sessions: "Access token (JWT) expiry" ≤ 3600 s, "Refresh token reuse interval" 10 s; valorar "Single session per user" solo si se quiere forzar un dispositivo.
- **AUTH-06** `PENDING` — Bien: `auth.service.ts:49-52` `jwt.verify(..., { algorithms: ['ES256'] })` con JWKS del proyecto (`:27-31`, cache 10 min, rotación por `kid`), `exp` comprobado por jsonwebtoken 9.0.3 (`verify.js:185-190`); producción: sin token 401, `alg none` → `{"message":"Invalid token"} 401`, HS256 con clave falsa 401, ES256 autofirmado 401; JWKS expone 1 clave `ES256`. Widget: `randomBytes(24)` CSPRNG, guardado como sha256 (`widget-token.service.ts:27-39`), TTL 90 d (`:18`), máx 5/usuario, `DELETE /widget/token` revoca, purga diaria; `qw_deadbeef` → 401.
  Falta: no se pasan `issuer` ni `audience` a `jwt.verify` (riesgo bajo: la clave es del propio proyecto). Arreglo: `{ algorithms: ['ES256'], issuer: \`${supabaseUrl}/auth/v1\`, audience: 'authenticated' }`en`apps/api/src/auth/auth.service.ts:52`+ test en`auth.service.spec.ts`.
- **AUTH-07** `NA` — Sin OAuth/social: `/auth/v1/settings` → `external.email: true`, `external.phone: false`, resto de proveedores `false`, `saml_enabled: false`.
- **AUTH-08** `NA` — 0 hits de `mfa|totp|webauthn|passkey` en `apps/mobile/src` y `apps/api/src`; la app no expone MFA de Supabase. Motivo: no existe rol de administrador global ni panel de administración en la app (los "admins" son por grupo, `groups.service.ts:404-474`) y los datos son de baja sensibilidad (disponibilidad y quedadas). Recomendación fuera de alcance del código: 2FA en las cuentas de Supabase, Vercel, GitHub y Hetzner del propietario (sección OPS/INF).
- **AUTH-09** `PENDING` — Cambio de email: `ProfilePage.tsx:157` → `stores/auth.ts:154-161` `updateUser({email}, {emailRedirectTo})` (Supabase envía confirmación; el aviso a la dirección antigua depende de "Secure email change" en el panel). Cambio de contraseña: `ProfilePage.tsx:167-190` pide solo nueva + confirmación, sin contraseña actual; 0 hits de `reauthenticate` → si se activa "Secure password change" en Supabase el cambio fallaría ("reauthentication needed"). Supabase no envía email de "contraseña cambiada".
  Arreglo (R): antes de `updateUser({password})` llamar a `supabase.auth.reauthenticate()` y pedir el código (`nonce`) o verificar la actual con `signInWithPassword`; en panel activar "Secure email change" y "Secure password change" (Authentication → Sign In / Providers → Email).
- **AUTH-10** `PENDING` — Borrado de cuenta: 0 hits de `deleteAccount|delete_account|eliminar cuenta|auth.admin.deleteUser` en `apps/` (ts/tsx/json, i18n incluido); `auth.controller.ts` solo tiene `GET/PATCH /auth/me`; ninguna ruta `@Delete` de la API toca usuarios. Exportación de datos: 0 hits (`exportar|mis datos|export`). Sin runbook con procedimiento manual (brief).
  Arreglo (B): `DELETE /auth/me` en la API (Supabase `auth.admin.deleteUser` con service_role + `prisma.user.delete`, todas las relaciones ya tienen `onDelete: Cascade` en `schema.prisma`; reasignar o borrar grupos fundados), pantalla en Profile con confirmación escrita, y `GET /auth/me/export` (JSON con perfil, grupos, disponibilidad, eventos) o, como mínimo, procedimiento manual documentado.
- **AUTH-11** `PENDING` — Código: `prisma/` solo contiene `schema.prisma` y `migrations/`; `"prisma".seed` = null y `db:seed` (`apps/api/package.json`) no tiene fichero; Dockerfile:57 `npx prisma migrate deploy && node dist/main` y CI sin `seed` (0 hits); los hits de `seed|fixture|example.com|quedamos.local` son solo tests (`*.spec.ts`, `*.test.tsx`) y un comentario. Pero: (a) usuarios `qa-test-*@quedamos.local` y grupo `QA Prod Group` siguen en producción (BACKLOG); (b) `tools/beta-bot` sigue corriendo en Railway y crea cuentas confirmadas con service_role para cualquier email (`src/index.ts:306-313`) con un código de invitación filtrado en el historial público.
  Arreglo: parar/retirar el beta-bot (o rotar el código y la service_role, ya en BACKLOG). HUMAN: panel Supabase → Authentication → Users: borrar `qa-test-*@quedamos.local` y el grupo `QA Prod Group`; confirmar con SQL de solo lectura `select email from auth.users where email like 'qa-test-%'` → 0 filas.

### ABU — Abuso, límites y costes

- **ABU-01** `DONE` — `ThrottlerGuard` global `APP_GUARD` 100 req/60 s por IP (`apps/api/src/app.module.ts:28-33,48-53`); `trust proxy` 1 (`main.ts:16`) y Caddy `reverse_proxy quedamos-api:3001` (Caddyfile en VPS; Caddy añade `X-Forwarded-For` por defecto, un solo salto). 16 `@Throttle` por ruta: `POST /groups/join` 5, `GET /join/:code` 10, `POST events` 10, availability 20, proposals 10/20/5, polls 10/20/10, `PATCH /auth/me` 10, register-token 10, `notifications/test` 5, `widget/token` 5, weather 30. `@nestjs/throttler` 6.5.0 (`throttler.guard.js:121`) añade `Retry-After` en el 429; en producción `curl -sI $API/health` → `x-ratelimit-limit: 100`, `x-ratelimit-remaining: 99`, `via: 1.1 Caddy`. Sin rate limit en Caddy ni Vercel (solo DDoS de plataforma); login/registro/reset → límites de Supabase + hCaptcha (AUTH-02).
  HUMAN: prueba de 40 peticiones solo con cuenta de pruebas propia contra una ruta autenticada de la API (`for i in $(seq 1 40); do curl -s -o /dev/null -w "%{http_code}\n" -H "Authorization: Bearer $TEST_TOKEN" -X POST $API/groups/join -H 'content-type: application/json' -d '{"inviteCode":"00000000"}'; done | sort | uniq -c` → 429 a partir de la 6.ª) y confirmar `Retry-After` con `-i`; nunca contra Supabase Auth con cuentas reales.
- **ABU-02** `PENDING` — Existen: push tokens ≤ 10/usuario (`notifications.service.ts:79,102-113`), widget tokens ≤ 5/usuario (`widget-token.service.ts:19,44-54`), ciudades ≤ 5/grupo (`groups.service.ts:41,560-563` → 400), 1 pregunta abierta por grupo y día (`polls.service.ts` → 409) y 1 push de pregunta/grupo/día, `MaxLength` en todos los DTO de texto, disponibilidad única por usuario/grupo/fecha. Faltan: máximo de grupos por usuario, miembros por grupo, eventos y propuestas por grupo/usuario y día (`groups.service.ts:27-30`, `events.service.ts:67`, `proposals.service.ts` sin `count`): solo el throttle por IP (10/min → 14 400 eventos/día por IP, cada uno con push al grupo). BACKLOG: "rate limits por usuario pendientes (51 rutas)".
  Arreglo: cuotas con 4xx claro (p. ej. 20 grupos/usuario, 50 miembros/grupo, 30 eventos y 20 propuestas abiertas/grupo, 50 creaciones/usuario/día) y `ThrottlerGuard` derivado que use `req.user.id` como tracker en rutas autenticadas.
- **ABU-03** `DONE` — Los tres formularios públicos (login, registro, reset) van directos a Supabase con hCaptcha invisible (`LoginPage.tsx:128`, `RegisterPage.tsx:243`, `ForgotPasswordPage.tsx:102`; token en `stores/auth.ts:79,90,135`) más los rate limits de Supabase Auth; honeypot 0 hits (inútil aquí: el servidor es Supabase, no la API). `GET /join/:code` público con regex + `@Throttle` 10/min (`invite.controller.ts:23-26`); `POST /groups/join` autenticado 5/min. Feedback en Tally (externo, con su propia protección). Que hCaptcha esté exigido en servidor es el HUMAN de AUTH-02.
- **ABU-04** `PENDING` — Push: `sendToUser`/`sendToGroup` (`notifications.service.ts:159-187,234-279`) no consultan ningún contador por destinatario/hora; solo respetan preferencias por tipo. Deduplicación existente: recordatorio 24 h reclamado con `updateMany` condicional sobre `reminderSentAt` antes de enviar (`event-reminder.service.ts:29-32,71-75`), recordatorio semanal cron único `0 20 * * 0` a usuarios sin disponibilidad (`weekly-reminder.service.ts:17-40`), 1 push de pregunta/grupo/día (`polls.service.ts`). `POST /notifications/test` solo a los tokens del propio usuario (`:189-201`) y 5/min. Destinatarios verificados: tokens registrados por usuario autenticado. Emails: los envía Supabase (límite "emails per hour" y SMTP en panel → HUMAN, sección MAIL).
  Arreglo: en `sendToUser`/`sendToTokens` contar `notificationLog` del destinatario en la última hora (`createdAt >= now-1h`) y saltar por encima de N (p. ej. 20), con `collapseKey` por tipo; hoy un miembro puede disparar 10 eventos/min → 10 push/min a todo el grupo.
- **ABU-05** `HUMAN` — Proveedores deducidos del código/env: Hetzner VPS (cuota fija; vigilar tráfico), Supabase (DB + Auth + Realtime; plan y "Spend cap"), Vercel (Hobby sin facturación por exceso / Pro con "Spend Management"), Firebase Cloud Messaging (`FIREBASE_*`; mantener plan Spark sin cuenta de facturación), Sentry (`SENTRY_DSN`; free con "Spend allocation" OFF), hCaptcha (free), Open-Meteo (free no comercial, 10 000 req/día; cache 30 min en la API), Nominatim (free, política 1 req/s; llamado desde el navegador con debounce 300 ms `hooks/useCitySearch.ts:8,19-27`, el header `User-Agent` de `services/weather.ts:70` lo descarta el navegador → identifica el `Referer` quedamos.alvarotc.com), Umami self-hosted (sin coste), Railway (beta-bot), GitHub Actions (repo público, gratis).
  HUMAN: confirmar alertas al 50/80/100 % y límite duro donde exista: Supabase (Billing → Spend cap ON o alertas), Vercel (Usage → alertas / Spend Management), Firebase (sin Blaze), Sentry (Quotas), Hetzner (límite de tráfico), Railway (límite de uso).
- **ABU-06** `PENDING` — Valores: body JSON 100 kb (`main.ts:18`); Caddyfile sin bloque `timeouts` ni `transport` → Caddy 2.11.2 por defecto `read_body`/`read_header`/`write` sin límite, `idle` 5 min, `reverse_proxy` sin `response_header_timeout`; Node 22.22 sin ajustes en `main.ts` → defaults `headersTimeout` 60 s, `requestTimeout` 300 s, `keepAliveTimeout` 5 s; Prisma `connection_limit=5` vía `DATABASE_CONNECTION_LIMIT` (`prisma.service.ts:4,14-19`), URL con `?schema=public&pgbouncer=true` sin `pool_timeout` (10 s por defecto), `connect_timeout` (5 s) ni `statement_timeout`; salientes: Open-Meteo `AbortSignal.timeout(10_000)` (`weather.service.ts:26,95`), JWKS jwks-rsa `timeout: 30000` (default), firebase-admin sin timeout explícito; crons sin timeout (lotes de 10).
  Arreglo: Caddyfile global `servers { timeouts { read_header 10s read_body 30s write 60s idle 2m } }` y en el vhost `reverse_proxy { transport http { dial_timeout 5s response_header_timeout 30s } }`; `DATABASE_URL` con `&pool_timeout=10&connect_timeout=5`; `statement_timeout` en el rol de Supabase (`alter role ... set statement_timeout='15s'`); `server.headersTimeout/requestTimeout` explícitos en `main.ts`.
- **ABU-07** `DONE` — Sin cobros; la creación de recursos no tiene coste. Doble envío bloqueado en cliente en los 4 formularios de creación: `CreateEventModal.tsx:66,208` (`createEvent.isPending` → `disabled`), `CreateProposalModal.tsx:33,103`, `AskGroupSheet.tsx:44,76`, `GroupPage.tsx:244,278` (crear/unirse a grupo). Idempotencia en servidor donde importa: pregunta abierta única por día (409), disponibilidad `upsert`, conversión propuesta→evento con `updateMany` condicional (`proposals.service.ts:219-221`), recordatorio reclamado por `reminderSentAt`. Sin `Idempotency-Key` en `POST /groups` y `POST /events` (0 hits de `idempot` fuera de comentarios/tests): un reintento tras timeout puede duplicar un evento; riesgo bajo, sin coste, borrable por el usuario.
- **ABU-08** `PENDING` — `curl -sI -A "" https://quedamos.alvarotc.com | head -1` → `HTTP/2 200`; ídem contra `$API/health` → 200 (no se bloquea UA vacío). `robots.txt`: la web devuelve el `index.html` de la SPA (200 `text/html`, no existe) y la API 404. Vercel solo aporta mitigación DDoS de plataforma; Caddy sin módulo de rate limit y sin directiva `log` → no hay logs de acceso: `docker logs caddy --since 48h` = 96 líneas, todas de mantenimiento TLS; la API no loguea peticiones (0 líneas con 429/401 en 48 h). La revisión de logs a 48 h es imposible hoy.
  Arreglo (R): `robots.txt` (SEO) ; en el vhost de la API `log { output file /data/access-api.log { roll_size 10mb roll_keep 5 } format json }` y, opcional, `@nobot header !User-Agent` → `respond 403`; revisar a las 48 h con `docker exec caddy sh -c "grep -oE '\"client_ip\":\"[^\"]+\"' /data/access-api.log | sort | uniq -c | sort -rn | head"`.
- **ABU-09** `DONE` — Rutas públicas de la API: solo `GET /health` (sin `Cache-Control`, correcto para un health check; `via: 1.1 Caddy`) y `GET /join/:code` (302). No hay listados públicos. El único upstream costoso (Open-Meteo) va tras `AuthGuard` + `@Throttle` 30/min (`weather.controller.ts:25,57`) y cache en memoria: `CACHE_TTL = 30 * 60 * 1000` (30 min), clave lat/lon a 2 decimales + días, `MAX_CACHE_SIZE = 500` con evicción FIFO (`weather.service.ts:24,56-57,77-83,117-121`). Se pierde al reiniciar el contenedor (aceptable con 1 instancia).

### DAT — Datos, privacidad y backups

- **DAT-01** `PENDING` — La DB de la app vive en Supabase (gestionada): en el repo no hay ningún mecanismo (`grep -rniE "backup|pg_dump|restic|rclone|snapshot" README.md docs/ .github/` → 0 hits útiles; BACKLOG solo cita `.env.quedamos.bak`). En el VPS: `crontab -l` → «no crontab for alvarotc», `sudo -n crontab -l` → «no crontab for root», `/etc/cron.d` solo `freshrss-backup` (otro servicio), `/opt/backups/` solo `freshrss/`. `quedamos-api` es stateless (compose sin `volumes:`), pero `umami-db` (volumen `services_umami_db_data`, analytics) no tiene backup. Ficheros subidos: NA (no hay).
  Arreglo: (1) HUMAN confirmar en Supabase → Project Settings → Database → Backups el plan (Free = sin backups automáticos; Pro = diarios 7 d, PITR opcional) y activar Pro/PITR si es Free; (2) `pg_dump` semanal con `DIRECT_URL` desde el VPS cifrado (`age`) a `rclone` remoto (otro proveedor), retención 14 d/8 sem; (3) `docker exec umami-db pg_dump` en el mismo cron.
- **DAT-02** `HUMAN` — Nunca se ha restaurado (sin runbook ni anotación; `docs/` = `pendiente-iconos.md`, `state-flows.md`). Procedimiento exacto para Álvaro: descargar el backup del panel de Supabase (o `pg_dump --no-owner --no-privileges "$DIRECT_URL" > dump.sql` desde el worktree con el `.env` del VPS), `docker run -d --name pg-restore -e POSTGRES_PASSWORD=x -p 5433:5432 postgres:15-alpine`, `psql postgresql://postgres:x@localhost:5433/postgres -f dump.sql`, arrancar `apps/api` con `DATABASE_URL`/`DIRECT_URL` apuntando a `localhost:5433`, `curl /health` y `GET /groups` con un JWT válido; anotar fecha, RTO (minutos) y RPO (antigüedad del backup) en el RUNBOOK.
- **DAT-03** `PENDING` — No hay job de backup propio y por tanto ninguna alerta: `grep -rlE "hc-ping|healthchecks|ntfy|telegram" /etc/cron.d /opt/services/docker-compose.yml` → vacío; Supabase no avisa si un backup gestionado falla ni si el proyecto se pausa (Free). Arreglo: al crear el cron de DAT-01, cerrar con `curl -fsS https://hc-ping.com/<uuid>` (healthchecks.io, gratis) que alerte por Telegram/email si no llega en 25 h; para Supabase, check semanal manual apuntado en OPS-11.
- **DAT-04** `PENDING` — Versionadas: 15 migraciones en `apps/api/prisma/migrations` (2026-02-08 → 2026-09-02). Despliegue automático: `Dockerfile:57` `CMD ["sh","-c","npx prisma migrate deploy && node dist/main"]`; último arranque en prod: «15 migrations found… No pending migrations to apply». Falta: CI sin job desde DB vacía (`ci.yml` sin `services:`, los specs `rls-migration.spec.ts`/`open-poll-unique.spec.ts` solo leen el SQL); ninguna nota de rollback (`grep -rniE "rollback" apps/api/prisma docs/ README.md` → 0); la única migración con datos (`20260902175057_unique_open_poll_per_day`: `UPDATE … SET status='closed'` + índice único parcial) no tiene vuelta atrás ni backup previo documentado. Riesgo del `migrate deploy` en cada arranque: una réplica y `restart: unless-stopped`, aceptable, pero un despliegue con la DB caída deja el contenedor en bucle de reinicio sin la versión anterior (imagen solo `:latest`).
  Arreglo: job `migrations` en `ci.yml` con `services: postgres:15` + `prisma migrate deploy` + `prisma migrate diff --exit-code`; fichero `apps/api/prisma/migrations/ROLLBACK.md` con el SQL inverso de cada migración destructiva; paso «backup antes de migrar» en `deploy.yml` (o en el RUNBOOK) cuando la migración toque datos.
- **DAT-05** `DONE` — Inventario (DTOs + formularios): registro `RegisterPage.tsx:134-218` nombre, email, contraseña (+confirmación) + hCaptcha, `stores/auth.ts:85-92` envía `data:{name}`; perfil nombre, email nuevo, contraseña, `avatarEmoji` (`update-profile.dto.ts`); grupo nombre + emoji (`create-group.dto.ts`), ciudades nombre/lat/lon → tiempo (`add-city.dto.ts`, Open-Meteo); quedada título, descripción, sitio (+lat/lon), fecha, hora, fin, online, `meetingUrl`, asistentes (`create-event.dto.ts`); disponibilidad fecha/tipo/franjas/horas; push `token`+`platform`; widget `groupId`+semana; propuestas/sondeos sin datos personales. Nada de DNI, teléfono ni dirección; todos con finalidad de producto. Pendiente en LEG-02: política que nombre finalidades y terceros (Supabase, Firebase/FCM, Open-Meteo, Nominatim desde el navegador `services/weather.ts:69`, hCaptcha, Umami, Sentry).
- **DAT-06** `PENDING` — Automatizado: widget tokens TTL 90 d, máx. 5, cron diario 04:00 (`widget-token.service.ts:18-19,86-93`); push tokens borrados al recibir `registration-token-not-registered`/`invalid-registration-token` (`notifications.service.ts:434-446`) y tope 10 por usuario (:79); recordatorios `event-reminder.service.ts:32` (cada hora) y `weekly-reminder.service.ts:17`. Sin retención: `notification_logs` (título, cuerpo, `data` JSON por usuario; `grep notificationLog … delete|purge` → 0), disponibilidad/sondeos pasados, cuentas inactivas; logs Docker `{"Type":"json-file","Config":{}}` sin `max-size` (sin `/etc/docker/daemon.json`; hoy 27 KB tras 17 h, pero sin tope). Arreglo: cron `@Cron` diario que borre `notification_logs` > 90 d y `availability`/`availability_polls` > 12 meses; `logging: {driver: json-file, options: {max-size: 10m, max-file: 5}}` en el compose del VPS; plazos escritos en la política.
- **DAT-07** `PENDING` — Web OK: Umami 3.0.3 self-hosted, `apps/mobile/index.html:14` solo `data-website-id` (sin `data-cookies`); el `script.js` servido no contiene `document.cookie` (solo `localStorage` para `umami.disabled`), payload `{website,screen,language,title,hostname,url,referrer,tag}`; eventos `logEvent` con parámetros no personales (`screen_name`, `type`, `response`, `method`; `useEvents.ts:116`, `useAvailability.ts:32`). Android NO: `lib/firebase.ts:50-75` envía los mismos eventos a Firebase Analytics (GA4 vía SDK JS con `measurementId`, cookies `_ga` en el WebView, sin consentimiento, sin `setAnalyticsCollectionEnabled`, `grep consent|setUserId` → 0) y además el script de Umami también carga en el WebView. Arreglo: eliminar `firebase/analytics` y enrutar `logEvent` a `umami.track` también en nativo (ya cargado), o pedir consentimiento en el primer arranque y arrancar con recogida desactivada (Consent Mode). Menor: la ruta `/join/<código>` llega a Umami como `url` (código de invitación en analytics) → `data-exclude-search` no basta; enmascarar con `data-before-send`.
- **DAT-08** `NA` — Condición no cumplida: no hay staging ni script de copia de producción a desarrollo (`grep -rniE "pg_dump|copia|anonymi|scrub|faker" README* docs/` → 0; no existe `scripts/`); README.md:129 pide «una base de datos desechable» para migraciones.
- **DAT-09** `PENDING` — DB en otra máquina (pooler de Supabase, eu-west-1). `.env.example:1-2`: `DATABASE_URL …?schema=public&pgbouncer=true`, `DIRECT_URL …?schema=public` — sin `sslmode`; en prod (`grep -c "sslmode=require" .env.quedamos` → 0) tampoco: Prisma negocia TLS por defecto (`prefer`) pero no lo exige. `prisma.service.ts` añade `connection_limit` (5 por defecto). Usuario: `grep -c "^DATABASE_URL=postgresql://postgres[.:]"` → 1 (rol `postgres` del proyecto: BYPASSRLS, dueño de todo; la RLS de `20260318120000_enable_rls_all_tables` no le aplica). Arreglo: `sslmode=require` en ambas URLs y activar «Enforce SSL on incoming connections» en Supabase → Database → Settings; crear rol `quedamos_api` con `GRANT SELECT,INSERT,UPDATE,DELETE ON ALL TABLES` + `USAGE ON SEQUENCES` (+ `BYPASSRLS` explícito o políticas) y dejar `postgres` solo para `DIRECT_URL`/migraciones — o documentar la decisión en el RUNBOOK.
- **DAT-10** `DONE` — Contenedor en UTC: `docker exec quedamos-api date +%Z` → `UTC +0000`, sin `TZ` en env ni Dockerfile (el host está en Europe/Madrid, irrelevante para el contenedor); Supabase Postgres en UTC. Instantes `@db.Timestamptz` (schema.prisma:22-281); días de disponibilidad/quedadas/sondeos `@db.Date` a medianoche UTC por diseño (`common/date-utils.ts:1-29`, `startOfTodayUTC`), correcto para «día» sin zona; horas de quedada como texto de reloj (`time`, `endTime`). Frontend: claves `YYYY-MM-DD` parseadas a medianoche local (`lib/date-utils.ts:27`) y `toLocaleDateString(locale)` en WeekView/MonthView/EventCard. Caveat documentado: recordatorios y sondeos fijan `Europe/Madrid` (`event-reminder.service.ts:11`, `polls.service.ts:20`, `weekly-reminder.service.ts:17` 20:00 UTC) — válido para grupos en España; zona por grupo queda para v1.x.

### PERF — Rendimiento

- **PERF-01** `PENDING` — Lighthouse (Chrome `/usr/bin/google-chrome`, JSON en `launch-gate/lh-*.json`): `/` móvil (= Splash) perf **73**, LCP **4917 ms**, FCP 4790, CLS 0, TBT 3 ms, TTI 4865; `/login` móvil perf **73**, LCP **4924 ms**, CLS 0, TBT 19 ms; landing escritorio perf **96**, LCP 1212 ms, CLS 0 (la landing no se sirve en móvil). Puntuación ≥ 70 en app se cumple, pero LCP casi duplica los 2,5 s: `#root` vacío hasta que llega el único bundle de 600 KB br (2,48 MB sin comprimir), FCP = LCP. INP no medible en laboratorio (TBT 3-19 ms, buen indicio).
  Arreglo: `React.lazy` por página en `apps/mobile/src/App.tsx:9-21` + `build.rollupOptions.output.manualChunks` en `apps/mobile/vite.config.ts` (firebase, hcaptcha, framer-motion, emoji-mart, ionic) y `import()` diferido de `lib/firebase.ts`; opcionalmente pintar el Splash estático en `index.html`.
- **PERF-02** `DONE` — No se sirve ninguna imagen raster: el logo va inline como `data:image/svg+xml` (0 B de red en `network-requests` de los 3 JSON); `modern-image-formats`, `uses-responsive-images`, `offscreen-images`, `prioritize-lcp-image` = N/A y `unsized-images` score 1 en las 3 páginas. En código solo 2 `<img>` (`pages/SplashPage.tsx:15`, `components/DesktopFrame.tsx:16`), ambos con `alt` y tamaño fijo por Tailwind (`w-16 h-16`, `w-14 h-14`), sin `width`/`height` HTML pero sin CLS (CLS 0). `public/`: icon-256 11 KB, icon-512 23 KB, logo.png 9 KB (`identify` 256/512 px), solo iconos de app; nada > 300 KB.
- **PERF-03** `DONE` — Google Fonts con `display=swap` y `preconnect` a googleapis/gstatic (`apps/mobile/index.html:15-17`), 2 familias; la CSS servida (`launch-gate/google-fonts.css`) declara 15 `@font-face` por subsets, el navegador solo bajó los latinos: Bricolage Grotesque variable **76,9 KB** + Geist Mono **23,1 KB** (2 ficheros ≤ 4, ninguno > 100 KB; `lh-landing-desktop.json` network-requests). Mejora no bloqueante: sin `preload` del woff2 y Bricolage pide `opsz 12..96, wght 400..800` completo (self-host + subset bajaría ~40 KB).
- **PERF-04** `PENDING` — HTML: `content-encoding: br`, `cache-control: public, max-age=0, must-revalidate` ✓. Pero los assets con hash `/assets/index-Bf_fI5DM.js` y `index-COYQQUl_.css` devuelven **el mismo `max-age=0, must-revalidate`** (sin `immutable`/1 año: revalidación en cada carga; `apps/mobile/vercel.json` no define cabeceras para `/assets/`). API: **ninguna `Cache-Control`** en `/health` ni en `/auth/me` (401) y 0 hits de `no-store` en `apps/api/src`; además sin compresión: Caddyfile del VPS solo `reverse_proxy quedamos-api:3001` (sin `encode`) y `main.ts` sin `compression`. `firebase-messaging-sw.js` con `max-age=0, must-revalidate` ✓.
  Arreglo: en `vercel.json` `{"source":"/assets/(.*)","headers":[{"key":"Cache-Control","value":"public, max-age=31536000, immutable"}]}`; en `apps/api/src/main.ts` `app.use((_, res, next) => { res.setHeader('Cache-Control', 'no-store'); next(); })`; en el bloque `quedamos.api.alvarotc.com` del Caddyfile `encode zstd gzip`.
- **PERF-05** `PENDING` — Un solo bundle: `index-Bf_fI5DM.js` **2 483 389 B sin comprimir / 604 493 B gzip / 599 602 B br** (curl `size_download`), CSS 47,5 KB → 10,9 KB br. Es 3× el objetivo de 200 KB gzip de app y 12× el de 50 KB de landing (la landing carga el mismo bundle: `unused-javascript` 375 183 B en `lh-landing-desktop.json`). `total-byte-weight`: `/` 624 KB, `/login` 1,39 MB (hCaptcha añade 467 KB + 190 KB + 101 KB). Sin `lazy(`/`import(` en `App.tsx`/`pages`, `vite.config.ts` sin `manualChunks` (solo chunks automáticos de Ionic `p-*.js` de 1-3 KB). `legacy-javascript` 0 B, `duplicated-javascript` 0 B (sin polyfills ni duplicados).
  Arreglo: el mismo que PERF-01 (lazy por ruta + manualChunks + import diferido de firebase/hcaptcha/emoji-mart).
- **PERF-06** `DONE` — Web: `curl -sI --http2` → `HTTP/2 200`, `server: Vercel`, `x-vercel-cache: HIT`, `x-vercel-id: cdg1::…` (edge París; CDN delante). API: `HTTP/2 200` con `alt-svc: h3=":443"` y `curl --http3` → `HTTP/3 200` (`via: 1.1 Caddy`). RTT a la API desde España: connect 40 ms, TTFB 128-136 ms (3 muestras); sin CDN delante de la API, innecesario con usuarios en ES.
- **PERF-07** `PENDING` — Índices (`apps/api/prisma/schema.prisma`): availability `@@index([groupId,date])` :95, events :124, group_members/event_attendees/widget_tokens `@@index([userId])` :75/:139/:171, proposals/polls `@@index([groupId,status])` :240/:273, notification_logs `[userId, createdAt desc]` :202, garantizados por `apps/api/src/common/prisma/hot-path-indexes.spec.ts:18-31`. Todos los listados usan `include`/`select` (events.service.ts:30-40, availability.service.ts:18-23, proposals.service.ts:69-77, groups.service.ts:92-99 y 356-359): sin N+1 (los únicos bucles con await son `updateMany` condicionales en `groups.service.ts:287-297` y los crons).
  Huecos: `group_cities.group_id` **sin índice** (schema :206-217; `findMany where groupId` en groups.service.ts:579, se consulta en cada petición de tiempo); el cron horario filtra `date+status+reminderSentAt` sin índice que empiece por `date` (event-reminder.service.ts:42-47, seq scan de `events`); consultas lentas **no registradas**: Prisma sin `log` (`prisma.service.ts`), sin `log_min_duration_statement` (Supabase gestionado), solo trazas Sentry al 20 % (`instrument.ts` `tracesSampleRate 0.2`). `EXPLAIN ANALYZE` → HUMAN (sin acceso a la DB).
  Arreglo: `@@index([groupId])` en `GroupCity` y `@@index([date, status])` en `Event` (migración nueva); `new PrismaClient({ log: [{ emit: 'event', level: 'query' }] })` + `$on('query', e => e.duration > 200 && logger.warn(...))`.
- **PERF-08** `PENDING` — `grep findMany` (32 usos): listados expuestos **sin límite**: `GET /groups/:id/availability` devuelve toda la disponibilidad histórica del grupo (`availability.service.ts:18-23`, `where: { groupId }`, sin rango ni `take`; el controlador no acepta `@Query`), `GET /groups/:id/events` todos los eventos incluidos pasados con asistentes+usuarios (`events.service.ts:30-40`), `GET /groups/:id/proposals` todas (`proposals.service.ts:69-77`); miembros y grupos por usuario acotados por tamaño real pero sin máximo. Acotados: polls `take: 50` (`polls.service.ts:145`), logs `take: 20` (`notifications.service.ts:227`), ciudades ≤ 5 (`MAX_CITIES_PER_GROUP`), widget por ventana de fechas. No existe ningún DTO de paginación (`@Query` solo en widget y weather).
  Arreglo: `GetAvailabilityQueryDto { from, to }` (máx. 92 días, por defecto mes actual ±1); eventos `?from=hoy` por defecto y `?past=1&take≤50`; proposals `take: 100`. Riesgo real bajo hoy (grupos de < 10 personas), crece con el histórico.
- **PERF-09** `HUMAN` — Script preparado en `launch-gate/loadtest-perf09.sh` (`autocannon -c 20 -d 300 $API/health` + listados autenticados con `TOKEN`/`GROUP`; 20 conexiones > 3× el tráfico de beta). No hay staging: ejecutarlo contra la API local con `.env` de desarrollo, o autorizar expresamente una pasada en producción (el `ThrottlerGuard` global de 100 req/min/IP devolverá 429 y falseará el p95 salvo que se exceptúe `/health`). Objetivo p95 < 500 ms y 0 respuestas no-2xx; línea base de una petición: TTFB 128-136 ms.
- **PERF-10** `DONE` — Pool explícito: `buildDatasourceUrl` añade `connection_limit=${DATABASE_CONNECTION_LIMIT || 5}` a `DATABASE_URL` (`apps/api/src/common/prisma/prisma.service.ts:4-19`); `.env.example` usa el pooler de Supabase (`?pgbouncer=true`, puerto 6543, modo transacción) y `DIRECT_URL` para migraciones. Memoria (una lectura SSH, solo lectura): `docker stats` quedamos-api **78,5 MiB**, 17 PIDs, 0 % CPU tras 17 h 23 min (`StartedAt 2026-09-02T22:22Z`, `restarts=0`, `health=healthy`); host 7,7 GB con 5 GB disponibles. Aviso: el contenedor no tiene límite (`mem_limit=0`) → añadir `mem_limit: 512m` en el compose del VPS; repetir `docker stats` a las 48 h para confirmar la tendencia.
- **PERF-11** `PENDING` — Sin cola (grep: solo `@nestjs/schedule`). Los push se lanzan fire-and-forget desde el handler (`this.notificationsService.sendToGroup(...).catch(logger.error)`: `events.service.ts:163-182`, `groups.service.ts:182/243`, `polls.service.ts:133`, `proposals.service.ts:61`; 24 sitios, ninguno con `await`) → la petición HTTP no se bloquea; los emails los envía Supabase; los crons son idempotentes por «claim» (`updateMany reminderSentAt` con `count === 1`, `event-reminder.service.ts:71-76`; semanal en lotes de 10 con `allSettled`, `weekly-reminder.service.ts:51-73`). Falta: sin reintentos ante fallos transitorios de FCM (`sendBatch` captura y marca `failed`, `notifications.service.ts:421-458`), sin dead-letter más allá de los contadores en `notification_logs`, crons sin lock multi-instancia (reconocido en `weekly-reminder.service.ts:16`). Riesgo bajo con una sola instancia.
  Arreglo: reintento (2 intentos, backoff 1-3 s) en `sendBatch` para `messaging/internal-error|unavailable`; conservar `notification_logs` como DLQ consultable.

### UX — Experiencia: estados, copies y formularios

- **UX-01** `PENDING` — Cubiertas: arranque `App.tsx:198-207` (IonSpinner mientras Supabase resuelve sesión), `CalendarPage.tsx:241-253` y `376-380` (SkeletonCard ×3), `PlansPage.tsx:188-199` (spinner) y `342-346` (SkeletonCard), `GroupPage.tsx:122-125`, `GroupDetailPage.tsx:269-278`, `JoinGroupPage.tsx:108-112`. Sin tratamiento y con parpadeo: `NotificationsSettingsPage.tsx:16,22-23` destructura solo `data` y `isEnabled` devuelve `?? true` mientras carga → todos los toggles aparecen activados y luego saltan; `ProfilePage.tsx:93-94,229` muestra «0 grupos»/«N de N» hasta que llegan `useGroups`/`useNotificationPreferences`. Ningún skeleton espera ~300 ms (aparecen al instante). Arreglo: usar `isLoading` de ambas queries y pintar `SkeletonCard`/tiles vacíos hasta tener datos. Navegador (sin sesión): `ux-splash-390.png`; Slow 3G → QA-09.
- **UX-02** `DONE` — `EmptyState` con texto + acción en cada lista: calendario sin disponibilidad `CalendarPage.tsx:449-455` («Estoy disponible»), sin grupos `CalendarPage.tsx:281-283` («Ir a grupos»), quedadas `PlansPage.tsx:351-358` (ir al calendario), propuestas `PlansPage.tsx:360-367` (crear), grupos `GroupPage.tsx:127-135` (crear / «tengo un código»); pasadas solo se listan si `past.length > 0` (`PlansPage.tsx:412`); ciudades sin datos `GroupDetailPage.tsx:474` (`group.tiles.noCity`); la pregunta solo se monta con pendientes (`MazoGate.tsx:97`); miembros nunca vacío (fundador). Componente `ui/EmptyState.tsx:5-14` exige título+descripción y admite acción secundaria. Recorrido con cuenta nueva → HUMAN (UX-14).
- **UX-03** `PENDING` — Solo mutaciones tienen error humano (toast vía `lib/mutation-utils.ts:5-16`, claves `errors.*`). Las queries de página ignoran `isError`: `CalendarPage.tsx:47,75`, `PlansPage.tsx:52,77,106`, `GroupPage.tsx:27`, `GroupDetailPage.tsx:81` → con la API caída (`retry: 1`, `main.tsx:22`) `groups` queda `undefined` y se pinta el vacío engañoso «no tienes grupos» (`CalendarPage.tsx:261`, `GroupPage.tsx:126`) o página en blanco (`GroupDetailPage.tsx:284` `if (!group) return null`); sin botón reintentar. ErrorBoundary global: 0 hits. Arreglo: `react-error-boundary` en `main.tsx` con pantalla propia + «Recargar»; en las 4 páginas, rama `isError` → `EmptyState` de error con `refetch`.
- **UX-04** `PENDING` — `grep -rniE "navigator.onLine|offline|@capacitor/network" apps/mobile/src` = 0 hits (solo «online» como tipo de quedada). React Query pausa las queries sin red pero no hay aviso ni recuperación visible; los formularios conservan estado local (React state) así que no se pierde lo escrito. Arreglo: hook `useNetworkStatus` (`window` online/offline o `@capacitor/network`) + banner fijo en `App.tsx` y `queryClient.invalidateQueries()` al reconectar.
- **UX-05** `PENDING` — Cubierto: pasadas plegadas (`PlansPage.tsx:412-428`), pestañas con vacío propio, gating `isAdmin` (`GroupDetailPage.tsx:281-282,437,458,538,620`) y action sheet por rol (`238-259`). Huecos: «Salir del grupo» se ofrece también al fundador (`GroupDetailPage.tsx:611-619`, sin comprobar `createdById`) aunque el rol no puede abandonar → solo verá `errors.leaveGroupFailed`; sesión caducada: `stores/auth.ts:121-127` deja `user=null` y `App.tsx:89` redirige a `/` sin mensaje ni `returnTo`, `lib/api.ts:32-35` no trata 401. Sin paginación (grupos pequeños; aceptable). Arreglo: ocultar «Salir» al fundador con nota; en `SIGNED_OUT` con usuario previo → toast «Tu sesión ha caducado» + `savePendingRedirect(pathname)`.
- **UX-06** `PENDING` — Navegador 390: `/login` email `autocomplete=""`/`inputmode=""`, password sin `autocomplete`; `/register` 4 inputs y `/forgot-password` igual; ningún input asociado a `<label for>` (`labelled:false` ×7); Chrome avisa «suggested: current-password / new-password» (`console.txt`). Escribir `correo-invalido` + Tab: sin error junto al campo (`visibleErrorBoxes: []`, solo `:invalid` nativo) — `ux-login-390-invalid-email.png`; el error va solo al banner superior (`LoginPage.tsx:82-86`). Bien: doble envío bloqueado (`LoginPage.tsx:130`, `ForgotPasswordPage.tsx:104`, `RegisterPage.tsx:247` + botón «Crear cuenta» deshabilitado hasta validar, `CreateEventModal.tsx:66-69,208`, `AvailabilityModal.tsx:126,135`, `GroupPage.tsx:244,268`), valores conservados al fallar, `inputMode="numeric"` en el código (`GroupPage.tsx:267`). Arreglo: `autoComplete`/`inputMode` en los 10 inputs de `Login/Register/Forgot/Reset/ProfilePage` (`email`, `current-password`, `new-password`, `name`), `id`+`htmlFor`, `onBlur` con `aria-invalid` y `<p role="alert">` bajo el campo.
- **UX-07** `PENDING` — Con `IonAlert` que nombra la pérdida: salir (`GroupDetailPage.tsx:632-641`, «no podrás volver sin invitación»), regenerar código (`642-651`), eliminar grupo (`660-669`, «se eliminarán todas las quedadas»), expulsar (`670-688`), eliminar quedada (`PlansPage.tsx:576-590`), cancelar quedada (`625-640`), confirmar (`647`). A un clic, sin confirmación: cerrar propuesta (`ProposalCard.tsx:234` → `PlansPage.tsx:262-267` `closeProposal.mutate` directo; irreversible, termina la votación), borrar disponibilidad (`AvailabilityModal.tsx:133` `onClick={handleDelete}`), quitar ciudad (`GroupDetailPage.tsx:202-206,597`), quitar admin (`251-255`, reversible). Sin deshacer en ninguna. Arreglo: `IonAlert` antes de cerrar propuesta («Nadie más podrá votar») y de borrar disponibilidad (con la fecha); toast con «Deshacer» opcional.
- **UX-08** `DONE` — Perfil: banner de éxito `ProfilePage.tsx:276-279` tras nombre (`135`), email enviado (`158`), contraseña (`180`), franjas (`200`). Grupo: copiar código cambia icono + `aria-label` «Código copiado» 2 s (`GroupDetailPage.tsx:419-423`), regenerar muestra «código regenerado» (`445-449`), compartir tarjeta → sheet nativo o `showInfo` «enlace copiado» en web (`lib/share-tarjeta.ts:23-29`). Avisos: toggle optimista + toast en fallo (`NotificationsSettingsPage.tsx:65-72`). Toda mutación fallida → toast (`lib/mutation-utils.ts`). Confirmación visual con sesión → HUMAN (cuenta de pruebas).
- **UX-09** `PENDING` — 530 cadenas extraídas (`launch-gate/copies-es.txt`); sin `es_ES` en hunspell (solo `en_*`) → `cspell 8.19.4 + @cspell/dict-es-es`: faltas reales `accion` (`plans.deleteConfirm` «Esta accion no se puede deshacer»), `Descripcion` (l.455), `excursion` (l.454 «Cena, excursion, cine…»); resto falsos positivos (Emoji, Monachil, Pádel, nombres de interpolación). Sin «¿» de apertura: `plans.cancelConfirm` «Cancelar esta quedada?», `plans.confirmEventConfirm` «Marcar esta quedada como confirmada?». Texto técnico posible: `lib/auth-errors.ts:19` devuelve el `error.message` crudo de Supabase (inglés) si no está mapeado; `ResetPasswordPage.tsx:94` muestra `err.message`. Navegador: 0 `undefined|null|NaN|[object` en landing/splash/login/register/forgot. Botones genéricos: solo «Guardar» ×2, «Cancelar» ×2 (con contexto). Juicio humano: anglicismos deliberados «email», «Haz click», «app», «online». Arreglo: corregir las 5 cadenas en `es.json` (y revisar `en.json`), fallback de `translateAuthError` → `common.unexpectedError` y `t('resetPassword.genericError')` en `:94`.
- **UX-10** `NA` — Condición no cumplida: no se cobra (brief).
- **UX-11** `PENDING` — `diff` de claves `es.json`/`en.json`: 0 diferencias; 15 valores idénticos y legítimos (marca, nombres, «Email», «Admin», «Online», placeholders). Navegador: primera carga en inglés por `navigator.language=en-US` (detección `localStorage → navigator`, `i18n/index.ts:18-22`); tras «Español» en el pie: `rawKeys: []`, ninguna palabra inglesa residual (`ux-landing-1280-es.png` vs `ux-landing-1280.png` en EN). Fechas por locale con `toLocaleDateString` en 15 puntos (`WeekView.tsx:69,77,175`, `MonthView.tsx:54,178`, `EventCard.tsx:91`, `ProposalCard.tsx:81`, `Mazo.tsx:209,214`…). Hueco: `<html lang="es">` fijo (`index.html:2`) aunque la UI esté en inglés (`lang` siguió «es» con `aria-pressed` en English) → lectores de pantalla/guionado incorrectos. Arreglo: en `src/i18n/index.ts` `i18n.on('languageChanged', l => document.documentElement.lang = l)` y aplicarlo al iniciar. Layout +30 % → juicio humano (EN suele ser más corto).
- **UX-12** `HUMAN` — Adelanto headless sin scroll horizontal: 390 px `scrollWidth 390/390` (`ux-splash-390.png`, `ux-login-390.png`, `ux-register-390.png`, `ux-forgot-390.png`, `ux-join-nosession-390.png`) y 640 px (zoom 200 %) `640/640`, `anyOverflow: []` (`ux-root-640.png`, `ux-login-640.png`); `viewport-fit=cover` + `env(safe-area-inset-*)` (`index.html:5`, `index.css:10-11`). Instrucción: en Pixel y iPhone físicos recorrer splash, login, calendario semana/mes, crear quedada y perfil en vertical y horizontal, con teclado abierto (login, crear quedada) y zoom 200 %; confirmar que la tab bar no tapa botones ni se corta nada bajo la barra de gestos.
- **UX-13** `HUMAN` — Chromium (Playwright): 11 navegaciones en 8 pantallas con 0 errores, 0 `pageerror`, 0 respuestas ≥400; único warning repetido `<meta apple-mobile-web-app-capable>` deprecado (`console.txt`). Firefox y WebKit no disponibles en el MCP. Instrucción: abrir en Firefox y Safari de escritorio (últimas 2 versiones) `/`, `/login`, `/register`, `/tabs/calendar` y `/tabs/plans` con una cuenta de pruebas, DevTools abierto: cero errores/warnings de consola y sin diferencias visuales (fuentes Bricolage/Geist Mono, aro SVG, hojas inferiores).
- **UX-14** `HUMAN` — Código: tras registrar, pantalla «Revisa tu email» con aviso de spam y «Ya lo he confirmado, entrar» (`RegisterPage.tsx:89-101`, `register.success.*`); tras login → `/tabs/calendar` (`App.tsx:59`) que sin grupos muestra «noGroups» + «Ir a grupos» (`CalendarPage.tsx:261-283`) y `GroupPage.tsx:127-135` ofrece «Crear grupo» / «Tengo un código»; una invitación previa se conserva por `pendingRedirect` (verificado en navegador: `/join/1234-0000` → `/login?redirect=/join/1234-0000` + localStorage). Instrucción: que una persona ajena reciba solo el enlace de invitación, cree cuenta, confirme el email y marque disponibilidad sin ayuda; anotar dónde se atasca (confirmación de email, código 8 dígitos, primer toque en el calendario).
- **UX-15** `PENDING` — Navegador: `/join/1234-0000` sin sesión → `/login?redirect=/join/1234-0000`, `pendingRedirect` guardado y enlace de registro con `redirect` (`ux-join-nosession-390.png`) ✓; `/tabs/calendar` sin sesión → `/` (Splash) sin guardar destino (`pendingRedirect: null`; `App.tsx:89` `<Redirect to="/">`) ✗; `/esta-ruta-no-existe-123` → página en blanco, sin catch-all (`App.tsx:157-165`, `ux-404-390.png`) ✗; `document.title` fijo «¿Quedamos?» en las 5 vistas (`grep document.title|Helmet|useTitle` = 0) ✗. Atrás: `IonBackButton` (`NotificationsSettingsPage.tsx:31`) e Ionic router; refresco: rewrite SPA (`vercel.json`), borradores de modales no persisten (aceptable, formularios cortos). Arreglo: en `ProtectedRoute` `savePendingRedirect(pathname+search)` antes de redirigir (o `/login?redirect=`); hook `useDocumentTitle` por página («Calendario · ¿Quedamos?»); ruta comodín con página 404 (PAG-01).
- **UX-16** `PENDING` — Oscuro por defecto y claro con tokens completos (`index.css:5-60` `:root`/`.light`), sin destello (`public/theme-init.js`), toggle en Perfil (`ProfilePage.tsx:302-308` → `stores/theme.ts`). Navegador: forzado `localStorage.theme='light'` → `html.light`, fondo `#F5F1E8`, landing coherente (`ux-landing-1280-light.png`, `-light-footer.png`) vs oscuro (`ux-landing-1280-es.png`). Huecos: `color-scheme` no declarado (computed «normal») → `input type=date/time` de `CreateEventModal.tsx:218,327,341`, scrollbars y selects nativos se pintan claros en modo oscuro; `<meta name="theme-color">` fijo `#14120E` (`index.html:6`) en modo claro. Arreglo: `:root{color-scheme:dark}` `.light{color-scheme:light}` en `index.css` y actualizar `theme-color` en `applyTheme`. Pantallas con sesión en claro → HUMAN.
- **UX-17** `NA` — Sin contenido imprimible (no hay facturas, informes ni recibos): `grep -rn "@media print" apps/mobile/src` = 0 y no hace falta. Las exportaciones existentes están cuidadas: tarjeta compartible canvas 1080 px con layout del lienzo aprobado y aro coherente (`lib/tarjeta.ts:1-40`), envío nativo/web con fallback descarga+portapapeles (`lib/share-tarjeta.ts:1-29`), ICS con `TZID Europe/Madrid` y escapes RFC (`lib/ics-utils.ts:8-16`).

### A11Y — Accesibilidad

- **A11Y-01** `DONE` — (parte técnica; parte navegador Tab/Escape: ver UX). `outline: none` solo en `:focus:not(:focus-visible)` con anillo `:focus-visible { outline: 2px solid var(--app-primary); outline-offset: 2px }` (`apps/mobile/src/index.css:90-96`); `outline-none` una vez, en el contenedor de scroll de la landing con `tabIndex={-1}` y foco programático (`pages/LandingPage.tsx:44-45`, intencionado). Resto de `tabIndex={-1}`: los 5 botones «ojo» de contraseña (Login 115, Register 171/229, Reset 156/211) → no alcanzables por teclado (menor; ver A11Y-03). Diálogos: `ui/Sheet.tsx:38` envuelve `IonModal` (11 consumidores) y `IonAlert` ×8 + `IonActionSheet` ×1 (`GroupDetailPage.tsx:632-670`, `PlansPage.tsx:576-647`) → Ionic gestiona trampa de foco, Escape/atrás y devolución del foco; ningún modal a mano (`role="dialog"` 0 hits; `fixed inset-0` solo `Mazo.tsx:247`, estado de pantalla completa, no diálogo). Lighthouse `tabindex` y `aria-hidden-focus` pasan en las 3 URL.
  Parte navegador: `DONE` — Landing 1280: 8 Tabs en orden DOM lógico (Código abierto → Abrir Quedamos ×3 → GitHub → Reportar error → Español → English), anillo `:focus-visible` 2 px `#F2EFE7` en todos (`index.css:90-96`; `outline:none` solo en `:focus:not(:focus-visible)`), `ux-landing-1280-focus-tab8.png`. Login 390: Volver → email → contraseña → ¿Olvidaste? → Entrar → Crear una, todos con foco visible (`ux-login-390-focus-tab6.png`); Escape sin efecto (no hay sheet abierta). Menor: el ojo de mostrar contraseña lleva `tabIndex={-1}` (`LoginPage.tsx:115`, `RegisterPage.tsx:171,229`, `ResetPasswordPage.tsx:156,211`) → inaccesible por teclado. Sheets/alerts: `IonModal` (`ui/Sheet.tsx:37`) e `IonAlert` (Ionic gestiona Escape y foco) → probar con sesión (A11Y-01 parte código/HUMAN).
- **A11Y-02** `PENDING` — Lighthouse accesibilidad: landing escritorio **100**, `/` móvil **94**, `/login` móvil **87** (< 95). `color-contrast` falla en ambas páginas de app: `#6b6558` (`--app-text-dark`) sobre `#14120e` = **3,23:1** en `p.text-text-dark` (tagline del Splash) y en los dos `label.text-xs` del login. `launch-gate/contrast.js` sobre los tokens de `index.css`: oscuro → text 16,3 ✓, text-muted 5,32 ✓, **text-dark 3,23 ✗, muted-panel 3,37 ✗, apagado 2,65 ✗**; claro → text-muted 4,92 ✓, **text-dark 2,45 ✗, muted-panel 2,20 ✗**; bordes al 12 %/32 % = 1,36/2,67:1 (< 3:1 para componentes). `text-text-dark` se usa 99 veces en 23 ficheros: 31 de las 42 etiquetas de formulario y 14 placeholders. «Nunca solo color»: los colores de miembro van con nombre/inicial en `Avatar`; no verificable en calendario sin navegador (UX).
  Arreglo: `--app-text-dark: #8C8577` (≈4,6:1) en oscuro y `#6E6858` en claro (o usar `text-text-muted` en labels/placeholders), reservar los valores actuales para elementos decorativos; `--app-border-strong` a alpha ≥ 0,45 donde delimita inputs.
- **A11Y-03** `PENDING` — Lighthouse: `image-alt`, `link-name`, `aria-*` pasan; **`button-name` falla en `/login`** (`<button type="button" … tabindex="-1">` solo con icono). Código: 2 `<img>`, ambos con `alt`; botones solo-icono revisados 16, todos con `aria-label` salvo los 5 «ojo» de contraseña: `pages/LoginPage.tsx:112-118`, `pages/RegisterPage.tsx:168-174` y `226-232`, `pages/ResetPasswordPage.tsx:153-159` y `208-214`. Campos: **46 `<input>` frente a 42 `<label>`, 0 `htmlFor`, 0 `id`, 0 `aria-label`/`aria-labelledby` en inputs** (13 ficheros: Login/Register/Forgot/Reset/Profile/GroupPage/GroupDetail, Create/Edit Event y Proposal, ConvertProposal, LocationSearch): las etiquetas son hermanas visuales (`LoginPage.tsx:89-96`), y axe da `label` por bueno solo por el `placeholder`, que desaparece al escribir.
  Arreglo: `id` + `htmlFor` (o envolver el input en el `<label>`) en los 13 ficheros; en los 5 toggles `aria-label={showPassword ? t('auth.hidePassword') : t('auth.showPassword')}` + `aria-pressed` y quitar `tabIndex={-1}`.
- **A11Y-04** `PENDING` — Landing: `<nav>` (`landing2/NavIsla.tsx:52`), `<main>` (`pages/LandingPage.tsx:48`), un `<h1>` (`landing2/HeroPregunta.tsx:75`) y un `<h2>` por sección ✓; sin `<header>` ni `<footer>` (el pie de `Cierre.tsx` es un `div`; solo `role="group"` para el idioma). App: un `<h1>` por página (Splash, Login, Register, Forgot, Reset, Calendar, Plans, GroupDetail, Profile, NotificationsSettings) ✓, pero `components/DesktopFrame.tsx:17` añade un segundo `<h1>` a ≥ 1024 px alrededor de cualquier página, y `components/Mazo.tsx:289` pinta un `<h1>` en overlay sobre `CalendarPage` (cuyo h1 sigue en `CalendarPage.tsx:318`) → dos h1 simultáneos; sin `<main>` en las páginas (Ionic `ion-content`). Lighthouse `heading-order` y `landmark-one-main` pasan en las 3 URL. Riesgo bajo.
  Arreglo: h1 de DesktopFrame → `<p>`; h1 de Mazo → `<h2>`; envolver el pie de `Cierre.tsx` en `<footer>`; `role="main"` en el `IonContent` de cada página.
- **A11Y-05** `PENDING` — 29 ficheros con `transition`/animaciones, 15 con framer-motion, 1 `@keyframes shimmer` (`index.css:99`), sin `<video>` ni parpadeos. `@media (prefers-reduced-motion: reduce)` global anula animaciones y transiciones CSS (`index.css:120-128`); `useMotionSafe()` (`lib/motion.ts`, sobre `useReducedMotion` de framer) se usa en 12 de los 15 ficheros con framer-motion; **`pages/GroupDetailPage.tsx`, `pages/PlansPage.tsx` y `ui/DotLoader.tsx` animan igual con reduced-motion** (framer no obedece la media query CSS) y no hay `MotionConfig`. Riesgo bajo.
  Arreglo: `<MotionConfig reducedMotion="user">` alrededor de `<App />` en `apps/mobile/src/main.tsx` (cubre los 15 ficheros de una vez).
- **A11Y-06** `HUMAN` — Instrucción: en Android activar TalkBack (Ajustes → Accesibilidad) o en escritorio Orca/NVDA/VoiceOver, y sin mirar la pantalla completar: (1) registro en `/register` (nombre, email, contraseña, captcha), (2) unirse a un grupo por código en «Grupo → Unirse», (3) marcar disponibilidad de un día en el calendario (abrir la hoja, elegir «Todo el día», guardar). Anotar cada control que se lea sin nombre o que no se pueda alcanzar. Aviso previo de esta auditoría: los inputs se anuncian solo por el placeholder y los botones «ojo» no tienen nombre (A11Y-03). El árbol de accesibilidad de las pantallas clave lo aporta el agente de navegador (UX).
- **A11Y-07** `PENDING` — Viewport servido: `width=device-width, initial-scale=1.0, viewport-fit=cover` (sin `user-scalable=no` ni `maximum-scale`) ✓, Lighthouse `meta-viewport` pasa; en la APK el zoom lo decide el WebView, no aplica. Tamaños: solo 3 `font-size: …px` en CSS (`index.css:201/208/248`, sobreescrituras de `ion-alert`) ✓, pero **184 `text-[Npx]` de Tailwind en 33 ficheros** (10px ×74, 11px ×39, 9px ×10, 13px ×16, 15px ×9, 17/22/27px ×13): ignoran la preferencia de tamaño de texto del navegador/SO (el zoom de página sí funciona) y hay etiquetas de 9 px (`EventCard.tsx:238`, `WeatherWidget.tsx:75`, `AvailabilityModal.tsx:187`). Zoom 200 % / ancho 640 px sin scroll horizontal → lo verifica el agente UX; el veredicto final depende de ese resultado.
  Arreglo: `fontSize: { '2xs': ['0.6875rem','1rem'], '3xs': ['0.625rem','0.875rem'] }` en `tailwind.config.js` y `sed` de `text-[11px]`→`text-2xs`, `text-[10px]`→`text-3xs`; eliminar los 9 px.
- **A11Y-08** `NA` — Condición no cumplida (decisión final: HUMAN, cruzar con LEG-11). 0 menciones a «accesibilidad/accessibility» en `landing2`, `pages` e `i18n` (no hay declaración). La EAA (Directiva (UE) 2019/882, traspuesta en la Ley 11/2023, exigible desde el 28-06-2025) cubre servicios tasados (comercio electrónico, banca, telecomunicaciones, libros electrónicos, transporte…): una app gratuita de coordinación sin ventas ni contratos a distancia no es «servicio de comercio electrónico»; y aunque lo fuera, el art. 4 de la Ley 11/2023 (art. 4.5 de la Directiva) exime a las microempresas que prestan servicios. Álvaro debe confirmar que no hay entidad ni actividad económica asociada; si algún día se monetiza, publicar la declaración enlazada desde el pie de `Cierre.tsx`.

### MAIL — Emails y notificaciones

- **MAIL-01** `PENDING` — La app no envía emails: 0 hits de `smtp|resend|brevo|sendgrid|postmark|mailgun|noreply|from:|replyTo` en `apps/`, `tools/`, `docs/`, README y BACKLOG (los únicos «resend» son push). Todo lo manda Supabase Auth (`signUp` `stores/auth.ts:85`, `resetPasswordForEmail` `:133`, `updateUser({email})` `:158`), luego el proveedor es el SMTP configurado en el panel; no hay rastro de SMTP custom en el repo → probable SMTP por defecto de Supabase (`noreply@mail.app.supabase.io`, ~3 emails/h, «solo desarrollo»). DNS (`dig`): `_dmarc.alvarotc.com` = `v=DMARC1; p=none; rua=mailto:<gmail>`; SPF = `v=spf1 include:_spf.mx.cloudflare.net ~all` (solo Cloudflare Email Routing, recepción); sin DKIM en selectores `default|google|resend|supabase|mail|cf2024-1|k1` (ver DNS-06). Arreglo: SMTP custom (Resend/Brevo) en Supabase → Project Settings → Authentication → SMTP Settings con `Sender email` `no-reply@alvarotc.com`, añadir `include:` del proveedor al SPF, DKIM (CNAME/TXT del proveedor), DMARC `p=quarantine`, y Reply-To a un buzón real (p. ej. `hola@alvarotc.com` reenviado por Cloudflare Email Routing). HUMAN: confirmar en el panel qué SMTP está activo hoy.
- **MAIL-02** `HUMAN` — Emails del sistema (todos de Supabase Auth): (1) confirmación de alta («Confirm signup», `signUp` `stores/auth.ts:85`), (2) recuperación de contraseña («Reset password», `resetPasswordForEmail` `:133`, enlace a `$PROD/reset-password`), (3) cambio de email («Change Email Address», `updateUser({email})` `:158`, enlace a `$PROD/tabs/profile`; con «Secure email change» activo llegan 2 correos), (4) reset lanzado por el beta-bot (`tools/beta-bot/src/index.ts:320`). No hay magic link, invitación, factura ni bienvenida propias. Instrucción: con una cuenta de pruebas nueva (no las `qa-test-*@quedamos.local`) disparar (1) desde /register, (2) desde /forgot-password y (3) desde Perfil, cada uno hacia Gmail, Outlook e iCloud; anotar bandeja (inbox/spam), remitente mostrado, y enviar (1) y (2) a la dirección de mail-tester.com → puntuación ≥ 9/10. Registrar resultados con fecha en `docs/qa-manual.md`.
- **MAIL-03** `HUMAN` — Las plantillas «personalizadas» que cita CLAUDE.md no están en el repo ni en el checkout: 0 ficheros con `ConfirmationURL|.SiteURL|.TokenHash`, no existe `supabase/` ni carpeta `emails|templates` → viven solo en el panel. Enlaces que generarán: `$PROD/reset-password` 200, `$PROD/tabs/profile` 200, `$PROD/` 200 (curl hoy). Supabase envía solo HTML (sin parte `text/plain` propia) y no permite pie con baja: enlace de baja NA (transaccional). Instrucción: Authentication → Email Templates, para «Confirm signup», «Reset password», «Change Email Address»: sin `localhost`/`vercel.app` en cuerpo ni en Authentication → URL Configuration (Site URL = `https://quedamos.alvarotc.com`, Redirect URLs incluyen `https://quedamos.alvarotc.com/**`), logo cargando por HTTPS, pie con nombre y dirección/contacto, textos ES con tildes, y probar en móvil (responsive).
- **MAIL-04** `NA` — La aplicación no envía correo (ningún transporte propio; `grep -rniE "bounce|complaint|suppress" apps/api/src` → 0 hits reales). Los rebotes los gestiona el SMTP de Supabase (interno) o el proveedor custom (lista de supresión propia, p. ej. Resend/Brevo la aplican antes de enviar); un webhook en la API no tendría a qué llamar porque Supabase Auth es quien decide el envío. Si se configura SMTP custom (MAIL-01): activar la suppression list del proveedor y revisar su panel de bounces la primera semana (HUMAN).
- **MAIL-05** `PENDING` — Preferencias: `NotificationPreference` (`apps/api/prisma/schema.prisma:175-184`, `@@id([userId,type])`, default `true`) con 18 tipos (`notifications/dto/update-preference.dto.ts:3-29`); comprobación antes de enviar en `sendToUser` (`notifications.service.ts:160-168` → `isNotificationEnabled` `:150-156`) y filtro de deshabilitados en `sendToUsers`/`sendToGroup` (`:250-251`, `:301-302`); todos los envíos pasan tipo: events `:169,180,270,294,326,472`, groups `:179,240,306,346,440,506`, proposals `:58,177,275`, polls `:130,193`, `event-reminder.service.ts:87`, `weekly-reminder.service.ts:61`. Dedup: recordatorio 24 h reclama con `updateMany({reminderSentAt:null})` antes del push (`event-reminder.service.ts:29-32,72-73`); `poll_completed` con `updateMany count 0 → no reenvía` (`polls.service.spec.ts:224`); semanal sin marca (cron dominical, salta usuarios con disponibilidad, `weekly-reminder.service.ts:17-45`; riesgo bajo). Tokens: `@@unique([userId,token])` (`schema.prisma:157`), tope LRU 10 por usuario (`notifications.service.ts:79,100-115`), borrado en `messaging/registration-token-not-registered|invalid-registration-token` (`:434-443`); un dispositivo con token rotado puede recibir doble hasta que FCM rechace el viejo (menor). **Permiso push al cargar**: `usePushNotifications()` en `AppTabs` (`App.tsx:47`) lanza `registerForPush()` en un `useEffect` en cuanto hay `userId` (`hooks/usePushNotifications.ts:23-47`) → `Notification.requestPermission()` (`lib/push-notifications.ts:121`) y `PushNotifications.requestPermissions()` (`:44`) saltan tras el login sin acción del usuario; ningún toggle en NotificationsSettingsPage lo dispara. Arreglo: pedir permiso solo desde un botón/toggle «Activar notificaciones» (ajustes o banner post-login) y en el montaje/resume registrar únicamente si `Notification.permission === 'granted'` (como ya hace el retry en `:107-114`).
- **MAIL-06** `PENDING` — Existe: caída → Upptime `status.alvarotc.com` (notificaciones: ver OBS-01); error nuevo → Sentry solo en la API (`apps/api/src/instrument.ts:41-45`, activo si `SENTRY_DSN`), alerta por email «issue alert» por defecto → HUMAN confirmar en Sentry → Alerts que la regla «new issue» notifica a Álvaro; el frontend no tiene Sentry, sus errores no avisan (ver OBS-02). No existe: backup fallido (no hay pipeline propio de backup, ver DAT-03; Supabase gestiona los suyos sin aviso configurado), pago fallido (NA, no se cobra), registro nuevo (el beta-bot solo usa `ADMIN_TELEGRAM_ID` para proteger comandos, `tools/beta-bot/src/index.ts:124-125,188,202,241`; `createUser` `:306` no avisa a nadie; sin Auth Hook/webhook en Supabase). Arreglo (opcional, primera semana): Supabase Database Webhook sobre `auth.users` INSERT → Telegram (bot existente) o email, o un cron diario en la API que cuente `users.createdAt > now()-1d` y lo mande por Telegram.

### INF — Infraestructura y despliegue

- **INF-01** `PENDING` — Sin staging (`grep -ni staging BACKLOG.md` → 0 hits; ni siquiera está en el backlog). Entornos reales: local (`apps/api/.env.example`, `apps/mobile/.env.example`) y producción (Vercel dashboard + `/opt/services/.env.quedamos` vía `env_file` en compose L21-22, más `environment:` NODE_ENV y PORT L7-9). Claves del env_file (solo nombres): CORS_ORIGIN, DATABASE_URL, DIRECT_URL, FIREBASE_CLIENT_EMAIL/PRIVATE_KEY/PROJECT_ID, FRONTEND_URL, SENTRY_DSN, SUPABASE_JWT_SECRET, SUPABASE_SERVICE_KEY, SUPABASE_URL.
  Cruce con la API: usadas y no definidas → DATABASE_CONNECTION_LIMIT, SENTRY_TRACES_SAMPLE_RATE, ENABLE_NOTIFICATIONS_DEBUG (las tres con default en código, OK). Definidas y NO usadas → SUPABASE_JWT_SECRET y SUPABASE_SERVICE_KEY (0 referencias en `apps/api/src` fuera de `common/test-utils.ts:307`): dos secretos muertos en el VPS; además hay `/opt/services/.env.quedamos.bak-2026-09-02` con credenciales antiguas.
  Justificación para salir sin staging: un desarrollador, DB gestionada (Supabase), Vercel ya genera previews por PR (deployments `Preview` a1fc5be/f3f2e06 por `vercel[bot]`). Mínimo viable: preview de Vercel + segundo proyecto Supabase gratuito (o Supabase branching) y `VITE_API_URL` apuntando a la API en compose local. Quitar del env_file las dos claves no usadas y borrar el `.bak` tras la rotación pendiente. Decide Álvaro.
- **INF-02** `PENDING` — `apps/api/src/app.module.ts:25-27` `ConfigModule.forRoot({ isGlobal: true })` sin `validationSchema`; ni joi/zod/envalid en `apps/api/package.json` ni `apps/mobile/package.json` (grep → 0). Solo `getOrThrow('SUPABASE_URL')` (`auth/auth.service.ts:26`) falla al arrancar; el resto cae en defaults silenciosos.
  Diff `.env.example` vs usadas en la API: faltan 7 en el ejemplo → NODE*ENV, PORT, FRONTEND_URL, CORS_ORIGIN, DATABASE_CONNECTION_LIMIT, SENTRY_TRACES_SAMPLE_RATE, ENABLE_NOTIFICATIONS_DEBUG. Mobile: diff vacío (10 `VITE*\*`usadas = 10 documentadas). Arreglo:`ConfigModule.forRoot({ isGlobal: true, validationSchema: Joi.object({ DATABASE_URL: Joi.string().uri().required(), … }) })`y completar`apps/api/.env.example` con las 7 variables y su default.
- **INF-03** `PENDING` — `.github/workflows/deploy.yml:50` publica solo `ghcr.io/…/quedamos-api:latest` (sin tag SHA ni `docker/metadata-action`); en el VPS `docker inspect quedamos-api` → labels `org.opencontainers.image.revision`/`created`/`version` vacías y `docker images --digests` muestra un único tag `latest` (sha256:b719a1b3…, hace 17 h). Sí existe verificación de que corre lo que se acaba de pushear (L71-80: `.Image` == `latest` y `RepoDigests` contiene `EXPECTED_DIGEST`) y comprobación de `/health` con `firebaseInitialized:true` (L82-93); cero pasos manuales.
  Último deploy: `gh run list` → 2026-09-02T22:19Z success headSha b03440e = `origin/main` b03440e. Web: Vercel despliega desde el repo (`gh api …/deployments` → `vercel[bot]` Production sha b03440e; `curl -sI $PROD` → `server: Vercel`, `x-vercel-cache: HIT`). Arreglo: paso `docker/metadata-action` con `tags: type=sha,prefix=sha- + latest` y labels OCI; `/health` devolviendo `commit` (hoy solo `{status, firebaseInitialized}`, `health.controller.ts:11-16`).
- **INF-04** `PENDING` — No hay runbook (`grep -rliE "rollback|runbook" README.md docs/ CHANGELOG.md .github/` → 0). Con un único tag `:latest` en el VPS no se puede volver a la versión anterior por tag; el `CMD` migra en cada arranque (`Dockerfile:57` `npx prisma migrate deploy && node dist/main`; log de arranque: "15 migrations found / No pending migrations to apply"), así que un rollback exige migraciones compatibles hacia atrás (DAT-04).
  Arreglo: `docs/runbook.md` con el comando exacto: `docker images --digests ghcr.io/alvarotorresc/quedamos-api` → editar `image:` a `ghcr.io/alvarotorresc/quedamos-api@sha256:<anterior>` (o tag `sha-…` tras INF-03) → `docker compose up -d quedamos-api` → `curl /health`. Web: Vercel "Instant Rollback" (dashboard) → HUMAN probarlo una vez.
- **INF-05** `PENDING` — Healthcheck sí: `Dockerfile:54-55` (`HEALTHCHECK --interval=30s --timeout=5s --start-period=30s --retries=3` contra `/health`) y compose L28-33 (mismo, puerto 3001); contenedor `healthy failing=0`. Apagado limpio no: `main.ts` no llama a `app.enableShutdownHooks()` (grep SIGTERM/enableShutdownHooks → solo `prisma.service.ts:26 onModuleDestroy`, que sin hooks nunca se ejecuta) y PID 1 es `sh -c "npx prisma migrate deploy && node dist/main"` con `init=<nil>` → SIGTERM no llega a node, SIGKILL a los 10 s.
  Caddy no espera al healthcheck: el único `depends_on` del compose es umami→umami-db (L41-43) y `reverse_proxy quedamos-api:3001` sin `lb_try_duration`; `docker compose up -d` recrea el contenedor → ~30 s de 502 en cada deploy. Arreglo: `app.enableShutdownHooks()`, `init: true` en el servicio, `reverse_proxy … { lb_try_duration 30s }` en Caddy. Prueba real con bucle de curl durante un deploy → HUMAN.
- **INF-06** `DONE` — compose `restart: unless-stopped` en los 4 servicios (L5 caddy, L20 quedamos-api, L38 umami, L56 umami-db); `docker inspect` → `restart=unless-stopped restarts=0`; `systemctl is-enabled docker` → `enabled`, `is-active` → `active`; `docker ps` → `quedamos-api Up 17 hours (healthy)`, `caddy Up 5 months`. Reinicio real del servidor nunca probado (`uptime` → 170 días) y el host pide reboot (`/var/run/reboot-required`): aprovechar ese reboot para verificar que todo vuelve (HUMAN, ver INF-12).
- **INF-07** `PENDING` — Sin límites: `grep -nE "mem_limit|cpus|deploy:|resources:|logging:|max-size"` en compose → 0; `docker inspect` → `mem=0 nanocpus=0 log=json-file {}` en los 18 contenedores; no existe `/etc/docker/daemon.json`. `free -h` → Swap 0B. `df -h /` → 45G/75G (63 %); `docker system df` → imágenes 44 GB (6.7 GB reclamables, 42 imágenes/15 activas). Logs sin rotación: ficheros json de 86M, 76M y 39M (la API 27 KB en 17 h).
  El VPS comparte host con Plane (12 contenedores, `plane-app-space-1` unhealthy), Umami y FreshRSS sin cuota → cualquiera puede dejar sin RAM a la API. Alerta de disco: no existe (OBS-05). Arreglo: `daemon.json` `{"log-driver":"json-file","log-opts":{"max-size":"10m","max-file":"3"}}` + `mem_limit: 512m`/`cpus: 1` en quedamos-api (y cuotas a plane/umami), swap 2G con `swappiness=10`, `docker image prune -a --filter until=168h` semanal y alerta de disco >80 % a Telegram.
- **INF-08** `PENDING` — Renovación automática sí: Caddy log `got renewal info … quedamos.api.alvarotc.com window 2026-09-12→09-14` (cert LE caduca 2026-10-13, `openssl s_client`); web por Vercel (LE, caduca 2026-12-01); analytics 2026-10-22. Dominio: registrador Cloudflare, `Registry Expiry Date: 2027-04-25` (whois). Alerta a 14 días no: `.upptimerc.yml` no tiene vigilancia de caducidad (Upptime solo comprueba la URL; avisaría ya caducado y con horas de retraso, OBS-01).
  Arreglo: monitor SSL con aviso a 14 días (UptimeRobot/Better Stack gratuitos) o job semanal en el repo `upptime`: `openssl s_client … | openssl x509 -checkend 1209600` → Telegram. Dominio → HUMAN: confirmar auto-renew y tarjeta válida en Cloudflare Registrar.
- **INF-09** `HUMAN` — Proveedores deducidos: Cloudflare (registrador + DNS, whois), Hetzner Cloud (VPS `vps-prod`), Vercel, Supabase, GitHub (`alvarotorresc`, repo público), Firebase/Google Cloud (FCM), Sentry, hCaptcha, Tally, Telegram (bot beta + alertas Upptime), Railway (beta-bot), Umami self-hosted (`https://analytics.alvarotc.com` 200; HTTP → 308 a HTTPS; sin 2FA posible). Instrucción: confirmar 2FA + email de recuperación en cada uno; contraseña fuerte y única en Umami (guardada en el gestor); ningún secreto compartido por chat (recordar BETA_INVITE_CODE filtrado, brief).
- **INF-10** `HUMAN` — No existe documento de continuidad: `grep -rliE "continuidad|bus factor|accesos" docs/ *.md .github/` → 0 hits (tampoco en BACKLOG.md). Instrucción: escribirlo fuera del repo (es público), p. ej. nota segura del gestor de contraseñas: lista de proveedores (INF-09), dónde viven la clave SSH del VPS, el keystore Android, `/opt/services/.env.*` y los secretos de GitHub Actions, y quién de confianza puede entrar si Álvaro no está.
- **INF-11** `PENDING` — Nada en el repo: `ls docker-compose*.yml Caddyfile infra/ terraform/ ansible/` → vacío; en el VPS `ls -a /opt/services | grep .git` → nada. Inventario VPS: `docker-compose.yml` (1.7 KB, 26-mar), `caddy/Caddyfile` (239 B; 4 sites: plane, quedamos.api, analytics, news) + `.bak-2026-09-02`, `.env.quedamos`, `.env.umami`, `freshrss/`, `plane/`; `ufw` activo 22/80/443; cron solo `/etc/cron.d/freshrss-backup`. README sin sección de reconstrucción.
  Arreglo: carpeta `infra/` en el repo con `docker-compose.yml` (secretos vía `env_file`, no commiteados) + `Caddyfile` + `docs/runbook.md` "reconstruir el VPS" (ufw, docker, login ghcr, restaurar env desde el gestor); si debe incluir Plane/FreshRSS, repo privado `alvarotc-infra`. Reconstrucción real → HUMAN.
- **INF-12** `PENDING` — `systemctl is-active unattended-upgrades` → active/enabled; `20auto-upgrades` → `Update-Package-Lists "1"; Unattended-Upgrade "1"`; Ubuntu 24.04.4 LTS; 45 paquetes actualizables y `*** System restart required ***` con 170 días de uptime → kernel con parches sin aplicar. Imágenes: Dockerfile `node:22-slim` cubierto por Dependabot docker semanal (`dependabot.yml:51-61`); pero el compose del VPS usa `caddy:2-alpine` (imagen de hace 6 meses), `umami:latest` (8 meses) y `postgres:15-alpine` (6 meses) sin re-pull ni Dependabot (compose no está en el repo). Sin plan mensual escrito.
  Arreglo: reboot en ventana (y verificar INF-06); `Unattended-Upgrade::Automatic-Reboot "true"` + `Automatic-Reboot-Time "04:30"` en `50unattended-upgrades`; entrada mensual en el runbook `docker compose pull caddy umami && docker compose up -d`.
- **INF-13** `HUMAN` — Ninguna cifra en README/BACKLOG (`grep -niE "€|coste|precio|billing"` → solo BACKLOG:277 "coste/esfuerzo"). Estimación deducida: Hetzner vServer 4 vCPU/7.6 GB/75 GB (≈ CX32, ~7 €/mes, compartido con Plane/Umami/FreshRSS), dominio Cloudflare ~10 €/año, Vercel Hobby 0, Supabase Free/Pro (0 o 25 $/mes), GitHub Free, FCM 0, Sentry Developer 0, Umami/Upptime 0, Railway beta-bot ~5 $/mes → 8-40 €/mes. Instrucción: escribir la cifra en el runbook y activar alertas de gasto (Vercel usage, Supabase spend cap, Railway limit; Hetzner no tiene: revisar factura mensual).
- **INF-14** `PENDING` — `timedatectl`: `NTP service: active`, `System clock synchronized: yes`, `RTC in local TZ: no`, pero `Time zone: Europe/Madrid (CEST, +0200)`. Contenedor en UTC: `docker exec quedamos-api date` → `Thu Sep 3 15:41:38 UTC 2026` = `date -u` del host. Arreglo (1 comando, R): `sudo timedatectl set-timezone UTC` (o documentar que el host queda en Madrid y los contenedores en UTC).

### OBS — Observabilidad y alertas

- **OBS-01** `PENDING` — Upptime (`alvarotorresc/upptime`, `.upptimerc.yml`) vigila `https://quedamos.api.alvarotc.com/health` y `https://quedamos.alvarotc.com` (el brief decía que faltaba el web: ya está, `history/quedamos-web.yml` existe y `summary.json` → ambos up 100 %), `expectedStatusCodes: [200]`, `workflowSchedule.uptime: "*/5 * * * *"`, `notifications: telegram` con secrets `NOTIFICATION_TELEGRAM_BOT_KEY`/`CHAT_ID`; `status.alvarotc.com` → 200.
  Pero la cadencia real de `uptime.yml` (`gh run list --workflow uptime.yml`) es 13:08, 08:54, 04:10, 00:01, 22:01, 19:51, 17:18, 13:35 → una comprobación cada 2-4,5 h (GitHub retrasa los crons), no cada 5 min: una caída puede pasar horas sin aviso. Arreglo: monitor externo real de 1-5 min (UptimeRobot 5 min / Better Stack 3 min, gratuitos) sobre las dos URLs con alerta al mismo Telegram; Upptime se queda como página de estado. Prueba de alerta (<5 min) → HUMAN.
- **OBS-02** `PENDING` — API: `apps/api/src/instrument.ts:41-45` `environment: NODE_ENV`, `tracesSampleRate` env o 0.2, `enabled: !!SENTRY_DSN`; `beforeSend` (L46-77) redacta JWT, tokens widget, bearer, query keys, emails, cabeceras sensibles y cookies; `sendDefaultPii` no se toca (default false). Sin `release` (grep `release` en instrument.ts → 0; no hay SENTRY_RELEASE/GIT_SHA en Dockerfile ni deploy.yml). Source maps se generan (`apps/api/tsconfig.json:10 sourceMap: true`) pero no se suben (`grep -rniE "sourcemaps|sentry-cli" .github/` → 0). `AllExceptionsFilter` captura ≥500 (`all-exceptions.filter.ts:24-26,38`).
  Frontend: sin SDK (`@sentry/*` ausente en `apps/mobile/package.json`; grep en src → 0) y sin ErrorBoundary. Arreglo: build-arg `GIT_SHA` en deploy.yml → `release: process.env.SENTRY_RELEASE` + paso `sentry-cli sourcemaps upload dist`; en mobile `@sentry/react` + `@sentry/vite-plugin` (release = SHA, `sendDefaultPii: false`) y `<Sentry.ErrorBoundary>` en `App.tsx`. Alerta "nuevo issue" → HUMAN en el panel de Sentry.
- **OBS-03** `PENDING` — Logger: `Logger` de Nest (ConsoleLogger, texto con ANSI, no JSON) en 11 servicios; sin pino/winston en `apps/api/package.json`; sin request-id (`grep -rniE "x-request-id|requestId|correlation"` → 0) ni middleware/interceptor de logging (0). `NestFactory.create(AppModule)` sin `logger:` → nivel DEBUG en producción: `docker logs quedamos-api --tail 5` → `[Nest] 66 - 09/03/2026, 3:41:12 PM DEBUG [AuthService] Token validation failed: JsonWebTokenError: invalid algorithm`.
  Caddyfile sin directiva `log` → el proxy no registra accesos; rotación inexistente (INF-07); imposible seguir una petición fallida por id. Arreglo: `nestjs-pino` (`genReqId` desde `X-Request-Id` o uuid, `level: 'info'` en prod, `redact: ['req.headers.authorization']`), Caddy `log { output stdout format json }` + `reverse_proxy … { header_up X-Request-Id {http.request.uuid} }`, `daemon.json` con max-size.
- **OBS-04** `PENDING` — Sin `/metrics` ni exportación (`grep -rniE "prometheus|prom-client|statsd|opentelemetry|otel"` en `apps/api/src` y package.json → 0 hits reales). Métrica manual: `docker stats --no-stream` → `quedamos-api cpu=0.01% mem=78.5MiB/7.57GiB`; load 0.01; disco 63 %. Arreglo barato: gráficas de Hetzner Cloud Console (CPU/red/disco, gratis) + `@willsoto/nestjs-prometheus` en `/metrics` protegido por token (peticiones, 5xx, p95, pool Prisma), o Beszel en el VPS. Panel consultado → HUMAN.
- **OBS-05** `PENDING` — Existen: Upptime → Telegram en caída (con horas de retraso, OBS-01) y Sentry → email en issue nuevo (default; confirmar en panel). No existen: tasa de errores/latencia (sin métricas, OBS-04), disco (63 % sin alerta, INF-07), backup fallido (DB gestionada en Supabase, sin aviso propio), certificado (INF-08), cola (NA: crons in-process con `@nestjs/schedule`), gasto (INF-13). Ningún runbook con "qué hacer" (`grep -rliE "runbook"` → 0).
  Arreglo: `docs/runbook.md` con una entrada por alerta (síntoma → comando → rollback); script cron en el VPS `df -P / | awk 'NR==2 && $5+0>80'` → Telegram; reglas Sentry (issue nuevo + pico >10/h); monitor SSL; alertas de gasto. Que cada una se dispare una vez en pruebas → HUMAN.
- **OBS-06** `PENDING` — Página de estado existe y está fuera de la infra propia: `curl -sI https://status.alvarotc.com` → `HTTP/2 200`; `dig` → CNAME `alvarotorresc.github.io.` (185.199.109/111.153, GitHub Pages). Pero nadie la enlaza: `curl -s $PROD | grep -ciE "status\.alvarotc|estado"` → 0; `grep -rn status.alvarotc apps/mobile/src README.md` → 0. Arreglo: enlace "Estado del servicio" en el pie de la landing (`components/landing2`), en Perfil › Acerca de, en el README y en el toast de "no se puede conectar" de la app.
- **OBS-07** `PENDING` — Eventos en código (`logEvent` → web `umami.track`, nativo Firebase Analytics; `lib/firebase.ts:61-70`): `join_group` (useGroups.ts:40), `mark_availability` (useAvailability.ts:32), `create_event`/`respond_event` (useEvents.ts:31,116), `create_poll`/`respond_poll` (usePolls.ts:29,45), `create_proposal`/`vote_proposal` (useProposals.ts:28,55), `share_group`, `share_tarjeta`, `screen_view`. Cubiertos: activación (unirse) y acción principal (disponibilidad, quedada).
  Faltan: registro (RegisterPage/LoginPage → 0 eventos), activación "crear grupo" (`useCreateGroup` useGroups.ts:21-30 sin evento), cancelación (`useLeaveGroup` L45-55 y borrar cuenta → 0). Pago: NA. Arreglo: añadir `sign_up`, `login`, `create_group`, `leave_group`, `delete_account`. Llegada en producción con cuenta nueva → HUMAN en el panel de Umami.
- **OBS-08** `PENDING` — Script servido: `curl -s $PROD` → `<script defer src="https://analytics.alvarotc.com/script.js" data-website-id=…>` (1 hit; `apps/mobile/index.html:14`); CSP lo permite (`vercel.json:17`); `https://analytics.alvarotc.com` → 200, HTTP → 308 a HTTPS. Exclusión del tráfico propio: no hay `data-domains` (se cuentan previews `*.vercel.app` y `localhost`), ni `data-exclude-*`, ni está documentado el opt-out `localStorage umami.disabled` (grep en README/BACKLOG → 0).
  Arreglo: `data-domains="quedamos.alvarotc.com"` en `index.html`; documentar en README `localStorage.setItem('umami.disabled','1')` para los dispositivos propios; en Umami › Settings › Ignore IPs la IP de casa. Comprobación en el panel → HUMAN.

### QA — Calidad y pruebas

- **QA-01** `PENDING` — CI verde: `gh run list --branch main --limit 5` → 5/5 `success` (CI @ b03440e 2026-09-02T22:17Z, Deploy API 22:19Z). Local en el worktree: API `jest` 50 suites / 699 tests OK (`launch-gate/test-api.txt`), mobile `vitest` 102 ficheros / 910 tests OK (`test-mobile.txt`); `pnpm test` en raíz falla en `@quedamos/api#build` porque turbo `test` dependsOn `build` y este ejecuta `prisma migrate deploy` (`Environment variable not found: DIRECT_URL`, `test-all.txt`) — CI lo evita llamando a `pnpm --filter … test` (`ci.yml:46,49`). Un test por flujo: login `apps/mobile/src/stores/auth.test.ts:99` + `apps/api/src/auth/auth.service.spec.ts:186`; crear/unirse a grupo `apps/api/src/groups/groups.service.spec.ts:37,119` + `apps/mobile/src/pages/JoinGroupPage.test.tsx:134`; disponibilidad `apps/api/src/availability/availability.service.spec.ts:53` + `CalendarPage.test.tsx`; quedada `apps/api/src/events/events.service.spec.ts:69`; push `apps/api/src/notifications/notifications.service.spec.ts:296`. Falta E2E: `apps/api/test/` solo contiene `jest-e2e.json` (183 B), no hay Playwright ni supertest contra DB. Arreglo: smoke e2e de la API con supertest + `services: postgres:15` en CI (registro→grupo→disponibilidad→quedada) y, si hay tiempo, Playwright contra Supabase local para login/join.
- **QA-02** `PENDING` — Anónimo: `auth.guard.spec.ts:49,55,61` (sin header / no Bearer / token nulo) y `widget-token.guard.spec.ts:21,27,33`. Puerta de pertenencia = `groupsService.findById(groupId,userId)` con `members:{some:{userId}}` → 404 (`groups.service.ts:104-116`), llamada en availability ×6, events ×3, proposals ×6, polls ×4; testada solo en `groups.service.spec.ts:111`. Negativos por endpoint: groups leave creador `:282`, kick no-admin/otro admin/a sí mismo/creador `:613,626,891,904`, delete no-creador `:769`, invite/refresh `:794`, role `:864`, cities `:1005` (POST; DELETE city solo 404 `:1074`); events PATCH/DELETE/cancel/confirm no-creador `events.service.spec.ts:788,1049,1103,1172`, respond no invitado `:607`; proposals PATCH/convert/close no-creador `:536,406,488`; polls close solo creador `:311`, respond cerrado `:278`; availability PUT/DELETE acotados por `where:{…,userId}` (`availability.service.ts:102`) con 404 `:230,440`. Sin test negativo de "A no miembro" en POST events, POST/PUT/DELETE availability, POST proposals, vote, POST polls, respond, DELETE cities-no-admin (dependen del mock de `findById`); PATCH /auth/me, /notifications/\*, /widget/token operan solo sobre el usuario del JWT (sin ruta cruzada). Arreglo: por cada uno de esos 8, un caso `groupsService.findById.mockRejectedValue(new NotFoundException())` + `rejects.toThrow`, y `should reject removing city from non-admin`.
- **QA-03** `PENDING` — `.github/workflows/ci.yml` no tiene `services:` ni paso `migrate` (`grep -rnE "migrate|services:|postgres" .github/workflows/*.yml` → 0 hits); `apps/api/src/common/prisma/rls-migration.spec.ts:1-16` solo concatena el SQL de `prisma/migrations` y hace `toContain`, no lo aplica. Copia anonimizada: NA (no existe, ver DAT-08). Arreglo: job `migrations` en CI con `services: postgres: image: postgres:15` (`DATABASE_URL`/`DIRECT_URL` al servicio), `pnpm --filter @quedamos/api exec prisma migrate deploy` y `prisma migrate diff --from-migrations prisma/migrations --to-schema-datamodel prisma/schema.prisma --shadow-database-url $DATABASE_URL --exit-code` (detecta drift schema↔migraciones).
- **QA-04** `PENDING` — `pnpm lint` EXIT=0: 0 errores / 24 warnings (17 mobile: `no-unused-vars`, `react-refresh`, 2 `exhaustive-deps`; 7 api en specs) (`launch-gate/lint.txt`). `pnpm typecheck` EXIT=0 tras `prisma generate` (sin él, 177 errores por cliente Prisma ausente, `typecheck-sin-prisma-generate.txt`). Formato: `npx prettier --check "apps/**/*.{ts,tsx,json,css}"` → **85 ficheros sin formatear** (55 tsx, 29 ts, 1 css; ningún JSON de i18n), incl. `apps/mobile/src/lib/api.ts`, `lib/auth-errors.ts` (`prettier.txt`); lefthook solo formatea staged y los commits van con `--no-verify`. Supresiones en auth/permisos (`apps/api/src/{auth,widget,groups,common}`, `mobile/src/{stores/auth.ts,lib/api.ts,lib/supabase.ts}`): 0 (grep `ts-ignore|ts-expect-error|eslint-disable|: any|as any`). Strict: mobile `tsconfig.json:14 "strict": true`; API `apps/api/tsconfig.json:15-17` solo `strictNullChecks`, `noImplicitAny`, `strictBindCallApply` (faltan `strictFunctionTypes`, `strictPropertyInitialization`, `useUnknownInCatchVariables`). Arreglo: `pnpm -w exec prettier --write "apps/**/*.{ts,tsx,css}"` (no tocar `i18n/locales/*.json`), añadir `prettier --check` a `ci.yml`, y `"strict": true` en `apps/api/tsconfig.json`.
- **QA-05** `PENDING` — Umbral: `apps/api/package.json` → `jest.coverageThreshold.global` 80/80/80/80; mobile: `vitest.config.ts` sin `coverage`/`thresholds` y sin `@vitest/coverage-*` en `package.json`. CI ejecuta `jest` sin `--coverage` (`ci.yml:46`) → el umbral **nunca se aplica**. Medido: API 88.16 % stmts / 91.28 % branches / 88.73 % funcs / 89.12 % lines; `auth.guard.ts` 100, `auth.controller.ts` 100, `auth.service.ts` 88/92/66.7/87.2 (líneas 35-40,51 sin cubrir), `widget-token.{guard,service}.ts` 100, `groups.service.ts` 92.3 (`test-api.txt`). Sin umbral específico auth/permisos. Arreglo: en CI `pnpm --filter @quedamos/api test -- --coverage`; añadir a `coverageThreshold` entradas `"./src/auth/**": {branches:90,lines:90}` y `"./src/widget/**"`; en mobile instalar `@vitest/coverage-v8` y `test.coverage.thresholds` para `src/stores/auth.ts` y `src/lib/api.ts`.
- **QA-06** `HUMAN` — `git log v0.3.3..origin/main --oneline | wc -l` → **286 commits** desde el último tag (no hay tag v1.0.0). `gh pr list --state merged --limit 15 --json reviews` → las 15 últimas PR (#86…#47, todas 2026-09-02) tienen `reviews: 0` (self-merge sin revisión). Instrucción: Álvaro revisa el diff `git diff v0.3.3..origin/main -- apps/api/src apps/mobile/src/stores apps/mobile/src/lib` con la OWASP Top 10 a mano (auth, IDOR por grupo, validación DTO, secretos, CORS/CSP, rate limits) y deja nota fechada en README/runbook «Revisión v1.0.0 — fecha — alcance» o aprueba una PR de release.
- **QA-07** `PENDING` — Existe paso post-deploy en `.github/workflows/deploy.yml:82-93` «Verify /health»: 20 intentos × 6 s a `https://quedamos.api.alvarotc.com/health` exigiendo `"status":"ok"` y `"firebaseInitialized":true`; además verifica digest de imagen (`:71-80`). No comprueba home 200, login ni endpoint principal; el deploy web (Vercel) no tiene smoke; sin rollback automático (ver INF-04). Sondeo manual hoy: `curl` `$PROD/` 200, `/login` 200, `$API/health` 200 `{"status":"ok","firebaseInitialized":true}`, `$API/join/0000-0000` 400, `$API/join/12345678` 404. Arreglo: `scripts/smoke.sh` llamado desde `deploy.yml` (y desde un workflow `deployment_status` de Vercel): `curl -fsS $PROD/ | grep -q '<div id="root"'`, `curl -fsS $API/health`, `curl -s -o /dev/null -w '%{http_code}' $API/join/12345678` == 404, y `curl -s -o /dev/null -w '%{http_code}' -H 'Authorization: Bearer x' $API/groups` == 401 (auth activa); fallo → job rojo + aviso (OBS).
- **QA-08** `PENDING` — Sin guion en el repo: `ls docs/` → `pendiente-iconos.md`, `state-flows.md` (0 ficheros `qa*`). En el checkout principal hay informes sin versionar: `qa-report-2026-04-16.md` y `qa-verification-2026-04-16.md` (Playwright sobre v0.3.3 en prod; cubren login vacío, i18n, empty states) e `informe-revision-2026-08-11.md` (revisión de código); ninguno cubre acentos/emojis en nombres, 5.000 caracteres, 0/10.000 elementos, sesión caducada a mitad de formulario ni dos pestañas. Límites de servidor: título 200 / descripción 1.000 / lugar 200 (`events/dto/create-event.dto.ts:21,27,33`), propuesta igual (`proposals/dto/create-proposal.dto.ts:16,22,28`), nombre grupo 100 y `emoji` 10 (`groups/dto/create-group.dto.ts:6,12`; un emoji ZWJ de familia ocupa >10 unidades UTF-16 → posible 400), perfil 100/10 (`auth/dto/update-profile.dto.ts:7,13`), ciudad 200, slots 50. Propuesta de guion `docs/qa-manual.md` (tabla caso · pasos · esperado · resultado · fecha): nombre grupo «Peña d'Ávila 🎉👨‍👩‍👧‍👦», título 201 y 5.000 chars (ver 400 legible), grupo sin miembros/sin quedadas/sin disponibilidad, 10.000 disponibilidades (PERF-08), miembro expulsado con la app abierta, token caducado al enviar «Nueva quedada», dos pestañas votando la misma propuesta/sondeo.
- **QA-09** `HUMAN` — Sin throttling en el MCP. Tiempos sin limitar: landing 1280 TTFB 24 ms, DOMContentLoaded/load 133 ms; splash 390 TTFB 25 ms, load 119 ms. Carga inicial cubierta por `App.tsx:198-207` (spinner hasta resolver sesión) y skeletons por página (UX-01). Instrucción: DevTools → Network «Slow 3G» + Performance «CPU 4× slowdown» con cuenta de pruebas: recorrer calendario, quedadas, grupo, perfil y crear quedada; debe verse skeleton/spinner (nunca blanco ni saltos) y los botones deben responder; capturar cada estado.
- **QA-10** `NA` — condición no cumplida: no se cobra (brief: «Cobros: NO»).
- **QA-11** `PENDING` — 11 navegaciones / 8 pantallas en chromium: 0 errores, 0 `pageerror`, 0 respuestas ≥400 (200/304; terceros umami, Google Fonts, hCaptcha OK; sin `/auth/me` porque la sesión vive en el cliente de Supabase), sin contenido mixto ni bloqueos CSP. Warning en cada carga: `<meta name="apple-mobile-web-app-capable">` deprecado (`index.html:7`); INFO promo i18next/Locize; VERBOSE sugerencias `autocomplete`. Registro completo en `launch-gate/console.txt`. Arreglo: añadir `<meta name="mobile-web-app-capable" content="yes">` junto a la actual (1 línea) y cerrar UX-06 para los VERBOSE.

### OPS — Operativa, soporte y lanzamiento

- **OPS-01** `PENDING` — No existe runbook: `ls RUNBOOK.md docs/runbook* docs/ops*` → nada; `docs/` = `pendiente-iconos.md`, `state-flows.md`; README/README.en solo «Montarlo en local», «Android» y una fila de tabla «Deployment | Web on Vercel · API in Docker…» (README.en.md:101); `grep -niE "rollback|restaur|rotar|rotate|mantenimiento|maintenance|logs|contacto" README*.md docs/*.md` → 0 hits. Faltan las 7: desplegar (solo implícito en `deploy.yml`), rollback (imposible por tag: imagen solo `:latest`, deploy.yml:44-56), restaurar backup, rotar secreto (rotación pendiente desde abril según BACKLOG), mantenimiento, logs (`docker logs quedamos-api`, Sentry, Vercel), contactos (Hetzner, Supabase, Vercel, Firebase, hCaptcha, Tally, registrador DNS). Arreglo: `RUNBOOK.md` en la raíz con esas 7 secciones y comandos literales; etiquetar la imagen también con `:sha-<git sha>` en `deploy.yml` para que el rollback sea `docker compose up -d` con la etiqueta anterior.
- **OPS-02** `HUMAN` — Canal publicado: Tally `https://tally.so/r/ODMzOa` (`lib/constants.ts:8`) en pie de la landing (`landing2/Cierre.tsx:50`), Perfil → Ayuda → «Reportar un error» (`ProfilePage.tsx:404`) y README.md:157; `curl` → 200. Sin `mailto:` en src/README. Compromiso de respuesta NO escrito (`grep -niE "48 ?h|respuesta|respond"` en i18n/README → solo copies de sondeos). Instrucción: Álvaro confirma que las respuestas de Tally le llegan por email y las revisa a diario, y añade «Respondemos en 48 h» junto al enlace en `Cierre.tsx`, `ProfilePage.tsx` y en la cabecera del formulario Tally.
- **OPS-03** `PENDING` — Existe el enlace desde la app (`ProfilePage.tsx:399-411`, tile «Ayuda» → Tally, `target=_blank rel=noopener`), pero la URL va sin contexto: sin `?version=`, plataforma, página ni navegador; además no hay ninguna constante de versión en el frontend (`grep -rn "__APP_VERSION__|APP_VERSION|appVersion" apps/mobile/src vite.config.ts` → 0). Arreglo: campos ocultos en Tally (`version`, `platform`, `page`, `ua`) y construir `FEEDBACK_FORM_URL + '?version=' + __APP_VERSION__ + '&platform=' + Capacitor.getPlatform() + '&page=' + location.pathname + '&ua=' + encodeURIComponent(navigator.userAgent)`; `define: {__APP_VERSION__: JSON.stringify(pkg.version)}` en `vite.config.ts`.
- **OPS-04** `PENDING` — Detección y canal público existen: `https://status.alvarotc.com` → `HTTP/2 200`; `.upptimerc.yml` monitoriza `quedamos.api.alvarotc.com/health` y `quedamos.alvarotc.com` cada 5 min (`workflowSchedule.uptime: "*/5 * * * *"`) con `notifications: telegram`. Falta el procedimiento: `grep -rniE "incident|incidente|post-?mortem|plantilla" README*.md docs/ .github/` → 0; sin plantilla de mensaje ni post-mortem. Arreglo: sección «Incidentes» en el RUNBOOK (quién detecta = Telegram de Upptime; comunicar = incidencia en el repo `upptime` que Upptime publica en la status page + aviso en el grupo de la beta; plantilla de 3 líneas: qué falla, desde cuándo, próximo update; post-mortem de 5 preguntas en `docs/incidents/AAAA-MM-DD.md`). HUMAN: Álvaro es la única persona nombrada; confirmar que sigue el Telegram de Upptime.
- **OPS-05** `PENDING` — Único flag: `ENABLE_NOTIFICATIONS_DEBUG` (`notifications.controller.ts:57-63`, solo protege el endpoint de prueba). FCM: se degrada sin credenciales (`notifications.service.ts:29,36,54` «push notifications disabled») pero no es un interruptor: `deploy.yml:87` exige `firebaseInitialized:true` en `/health`, así que apagarlo rompe el despliegue. Tiempo: sin flag; degrada bien (API 503 `weather_unavailable` `weather.service.ts:139,146`; `WeatherWidget.tsx:37,46` muestra «Sin datos de clima»/nada); Nominatim se llama desde el navegador (`services/weather.ts:69`) sin fallback. hCaptcha: sitekey fija en `RegisterPage.tsx:15`/`LoginPage.tsx:14`/`ForgotPasswordPage.tsx:10`, sin interruptor: una caída de hCaptcha bloquea registro, login y reset. Arreglo: `WEATHER_ENABLED`/`PUSH_ENABLED` leídos por `ConfigService` en cada petición (503 con mensaje i18n `weather.noData`), `/health` que no exija Firebase cuando `PUSH_ENABLED=false`, y en los formularios tratar el fallo de `captchaRef.execute()` como «reintentar» con mensaje claro en vez de bloqueo mudo.
- **OPS-06** `PENDING` — El registro web está abierto a cualquiera: `RegisterPage.tsx` pide nombre, email, contraseña + hCaptcha, sin código de invitación (`grep -niE "invite|beta|allowlist"` en la página → 0); `curl $PROD/register` → 200; sin allowlist, waitlist ni cupo en app/API (`grep -rniE "allowlist|whitelist|waitlist|MAX_USERS"` → solo `whitelist: true` del ValidationPipe). `tools/beta-bot` (`src/index.ts:270`) solo cierra el flujo Telegram → APK con `BETA_INVITE_CODE` (filtrado en el historial público según BACKLOG). Incoherencia: CHANGELOG 1.0.0 «En producción para la beta» y BACKLOG «Beta privada» frente a landing pública y alta libre. Arreglo: decidir por escrito. Si se lanza abierto → NA, retirar el beta-bot y rotar el código; si sigue cerrada → cupo (`MAX_USERS` comprobado en `POST /groups` o trigger en `auth.users`) con mensaje de lista de espera y registro solo desde `/join/:code`.
- **OPS-07** `PENDING` — `git tag --sort=-v:refname | head -3` → v0.3.3, v0.3.2, v0.3.1; `git describe origin/main` → `v0.3.3-286-gb03440e`; `CHANGELOG.md` tiene «1.0.0 — 2026-09-02» (+ «Sin publicar») pero no existe tag `v1.0.0`; `gh release list` → última «Quedamos v0.3.3» (2026-04-09). Versiones descuadradas: `apps/mobile/package.json` y `apps/api/package.json` `0.0.1`, `build.gradle` `versionName "1.0"` `versionCode 1`, y ninguna versión visible en la app (`grep __APP_VERSION__|APP_VERSION` → 0). Arreglo: subir los tres a `1.0.0` (`versionCode 2`), `__APP_VERSION__` desde `package.json` en `vite.config.ts` y mostrarla en Perfil → Cuenta; mover «Sin publicar» a 1.0.1 o 1.1.0; crear el tag (dispara `release.yml`) una vez exista el keystore de release.
- **OPS-08** `PENDING` — Afirmaciones de la landing (`es.json` `landing2.*`) y README comprobadas en código: «hace la pregunta y os avisa cuando podéis todos» → `polls.service.ts:193` push `poll_completed`; «Una pregunta al día, como mucho» → `polls.service.ts:86,114` (ConflictException + un push por grupo/día); «Tres pantallas» → `App.tsx:63-71` (calendar/plans/group); «Seis colores» → `constants.ts:10-17` + `lib/member-colors.ts`; «Código abierto» → repo 200 + `LICENSE` MIT; «Nunca una IA» → sin deps openai/anthropic; README: widgets (`apps/api/src/widget/`), ICS (`lib/ics-utils.ts`), tarjeta (`lib/tarjeta.ts`), mapa (`locationLat/Lon`), recordatorio 24 h (`event-reminder.service.ts`), avisos por tipo (`update-preference.dto.ts`). Enlaces: repo, web, /login, /register, api/health, Tally → 200. FALLA: README.md:24 «descarga el APK de la última release» → `releases/latest` 302 a `v0.3.3` (2026-04-09, `quedamos-v0.3.3.apk`, 5,7 MB): APK de hace 5 meses, anterior al rediseño y a la 1.0.0 que está en producción. Capturas: landing = maquetas en código (`Pantallas.tsx` sin `<img>`, siempre actuales), README `.github/readme/*.png` del commit 2026-09-02 → actuales; actualidad visual: juicio humano. Arreglo: release `v1.0.0` con APK firmado (tras keystore) o quitar el enlace/avisar «APK 1.0.0 próximamente» en el README hasta entonces.
- **OPS-09** `PENDING` — No hay FAQ ni guía enlazada: `grep -rniE "faq|ayuda|help|cómo funciona|guía"` en i18n/landing → solo `profile.tiles.help: "Ayuda"` (`es.json:408`), tile que contiene únicamente el enlace a Tally. Guía implícita: README «Qué hace» (3 pasos + lista) y landing «Proceso» (`Proceso.tsx`, 3 pasos). API para terceros: NA (Swagger apagado en producción, sin consumidores externos). Arreglo: página `/ayuda` (o sección del README enlazada desde el tile «Ayuda») con 6 preguntas reales: cómo instalar el APK y por qué avisa Android, por qué no llegan avisos en iOS/Safari, cómo entrar con código o enlace, quién puede borrar un grupo/quedada, cómo apagar un tipo de aviso, qué pasa si no respondo a la pregunta. Que responda a lo que preguntan de verdad: juicio humano tras la primera semana.
- **OPS-10** `HUMAN` — Lista de día 1 para este proyecto (marcar con hora en el RUNBOOK): (1) `status.alvarotc.com` con «Quedamos API» y «Quedamos Web» en verde y sin incidencia abierta; (2) registrarse con un email real nuevo en `$PROD/register`, recibir el email de confirmación de Supabase y entrar; (3) crear grupo, entrar con otra cuenta por `/join/:code`, marcar disponibilidad y lanzar «la pregunta» hasta que llegue el push de aro cerrado (web y Android); (4) Supabase → Database → Backups: backup de la noche presente (o primer `pg_dump` del cron de DAT-01 en el destino remoto); (5) Sentry sin errores nuevos en las últimas 24 h y `docker logs quedamos-api --since 24h | grep -iE "error|warn"` limpio; (6) Umami registra las visitas del día; (7) bandeja de Tally vacía o contestada. El agente puede repetir OBS-01, MAIL-02, DAT-01 y OBS-02 al día siguiente y adjuntar evidencia.
- **OPS-11** `HUMAN` — Parte automatizable OK: `.github/dependabot.yml` activo (github-actions + npm en `/`, `/apps/api`, `/apps/mobile` + docker, semanal los lunes, majors ignorados, límite 3/3/1 PRs); ahora mismo 12 PRs de Dependabot abiertas (`gh pr list --author app/dependabot` → 12) → hay que vaciar la cola. Sentry sin `release` (`instrument.ts:43-44` solo `environment` y `tracesSampleRate`). Plan del primer mes para Álvaro: semana 1 → cada día 10 min: Sentry, Upptime, Tally, `docker logs`, Umami; semanas 2-4 → lunes: mergear Dependabot y comprobar `pnpm audit`, revisar Supabase (uso de DB, backups, usuarios nuevos), rotar las credenciales pendientes del BACKLOG (service_role, password DB, clave Firebase, VPS_SSH_KEY) una por semana; reservar 2 h/semana de mantenimiento en el calendario y crear los recordatorios ahora.

## Resumen legible por máquina

```yaml
launch_gate:
  project: quedamos-app
  date: 2026-09-03
  scope: L+A+P
  commit: b03440e
  production: https://quedamos.alvarotc.com
  api: https://quedamos.api.alvarotc.com
  staging: none
  verdict: NO_LISTO
  blocking: { done: 20, pending: 63, na: 8, human: 19, total: 110 }
  recommended: { done: 9, pending: 40, na: 7, human: 11, total: 67 }
  pending_blocking_ids:
    [
      SEC-10,
      AUTH-11,
      SEC-11,
      DAT-01,
      DAT-03,
      INF-07,
      OBS-01,
      OBS-05,
      PAG-12,
      MAIL-01,
      DNS-06,
      ABU-04,
      ABU-02,
      ABU-06,
      LEG-02,
      LEG-01,
      LEG-04,
      LEG-03,
      AUTH-10,
      LEG-06,
      LEG-05,
      DAT-07,
      INF-03,
      INF-04,
      OPS-01,
      INF-02,
      INF-01,
      INF-08,
      OBS-02,
      OBS-03,
      DAT-04,
      PAG-02,
      QA-07,
      OPS-08,
      SEC-08,
      QA-02,
      QA-01,
      QA-03,
      AUTH-06,
      SEC-01,
      QA-04,
      QA-08,
      UX-03,
      PERF-01,
      PERF-04,
      PERF-08,
      PERF-07,
      PAG-01,
      UX-15,
      PAG-10,
      PAG-05,
      PAG-06,
      PAG-07,
      SEO-01,
      SEO-02,
      A11Y-03,
      A11Y-02,
      A11Y-07,
      UX-06,
      UX-07,
      UX-09,
      UX-01,
      QA-11,
    ]
  pending_recommended_ids:
    [
      LEG-09,
      LEG-13,
      DNS-05,
      DNS-07,
      DNS-08,
      PAG-03,
      PAG-08,
      PAG-09,
      SEO-03,
      SEO-04,
      SEC-14,
      AUTH-09,
      ABU-08,
      DAT-06,
      DAT-09,
      PERF-05,
      PERF-11,
      UX-04,
      UX-05,
      UX-11,
      UX-16,
      A11Y-04,
      A11Y-05,
      MAIL-05,
      MAIL-06,
      INF-05,
      INF-11,
      INF-12,
      INF-14,
      OBS-04,
      OBS-06,
      OBS-07,
      OBS-08,
      QA-05,
      OPS-03,
      OPS-04,
      OPS-05,
      OPS-06,
      OPS-07,
      OPS-09,
    ]
  human_ids:
    [
      LEG-07,
      LEG-08,
      LEG-12,
      DNS-01,
      DNS-03,
      PAG-11,
      SEO-05,
      SEC-13,
      SEC-20,
      AUTH-01,
      AUTH-02,
      AUTH-04,
      AUTH-05,
      ABU-05,
      DAT-02,
      PERF-09,
      UX-12,
      UX-13,
      UX-14,
      A11Y-06,
      MAIL-02,
      MAIL-03,
      INF-09,
      INF-10,
      INF-13,
      QA-06,
      QA-09,
      OPS-02,
      OPS-10,
      OPS-11,
    ]
  na_ids:
    [
      LEG-10,
      LEG-11,
      PAG-13,
      SEC-03,
      SEC-04,
      SEC-09,
      SEC-15,
      AUTH-07,
      AUTH-08,
      DAT-08,
      UX-10,
      UX-17,
      A11Y-08,
      MAIL-04,
      QA-10,
    ]
```
